# 整体设计说明

1. **主键策略**：核心业务表（用户、订单等流水表）优先 BIGINT 自增，保证写入性能；日志、关联表等对 ID 顺序不敏感的场景使用 VARCHAR (32) 压缩 UUID，避免 ID 泄露与爬虫遍历。
2. **索引原则**：唯一键优先保障业务唯一性，普通索引覆盖高频查询场景，避免过度建索引影响写入性能。
3. **扩展性**：所有业务表预留扩展字段与关联表设计，支持后续业务迭代无需大改表结构。
4. **数据量级**：日志、流水类表预留分表方案，数据量达到千万级后可按时间 / 用户维度分表，保障查询性能。





1. **所有表必须有 `id` 主键**：自增 BIGINT 或业务 VARCHAR(32)
2. **所有表必须有 `created_at` / `updated_at`**：自动维护时间
3. **所有金额字段用 `INT(分)` 或 `DECIMAL(10,2)`**：禁止用 FLOAT/DOUBLE
4. **所有布尔字段用 `TINYINT(1)`**：取 0/1
5. **状态字段用 `VARCHAR(20)`**：不用 ENUM（修改需 ALTER TABLE）
6. **所有表使用 `InnoDB` 引擎 + `utf8mb4` 字符集**
7. **所有表必须 `DROP TABLE IF EXISTS` 开头**：保证脚本可重跑
8. **索引命名规范**：`uk_` 唯一索引 / `idx_` 普通索引
9. **必须加 `COMMENT` 注释**：表注释 + 字段注释
10. **禁止外键约束**：完整性由业务层 + UNIQUE 索引保障

# 新增 修改

## 新增货币系统

```
-- ============================================================================

-- 货币系统（硬币）SQL —— 幂等，可重复执行

-- 不修改 algovize.sql，仅新增表和字段

-- ============================================================================



USE `algoviz`;

SET NAMES utf8mb4;



-- 1. user 表新增 coins 字段（初始 1000 硬币）

SET @col_exists = (SELECT COUNT(*) FROM information_schema.COLUMNS

  WHERE TABLE_SCHEMA = 'algoviz' AND TABLE_NAME = 'user' AND COLUMN_NAME = 'coins');

SET @sql = IF(@col_exists = 0,

  'ALTER TABLE `user` ADD COLUMN `coins` INT NOT NULL DEFAULT 1000 COMMENT ''硬币余额'' AFTER `status`',

  'SELECT ''coins 字段已存在'' AS msg');

PREPARE stmt FROM @sql;

EXECUTE stmt;

DEALLOCATE PREPARE stmt;



-- 已有用户补充硬币（仅 coins 为 NULL 或未设置的）

UPDATE `user` SET `coins` = 1000 WHERE `coins` IS NULL OR `coins` = 0;



-- 2. 硬币商品表

CREATE TABLE IF NOT EXISTS `coin_product` (

  `id`       BIGINT     NOT NULL AUTO_INCREMENT,

  `product_id`   VARCHAR(64)  NOT NULL         COMMENT '商品编号',

  `product_name`  VARCHAR(200)  NOT NULL         COMMENT '商品名称',

  `description`  TEXT                  COMMENT '商品描述',

  `coin_price`   INT      NOT NULL         COMMENT '硬币价格',

  `category`    VARCHAR(50)  DEFAULT 'coin'      COMMENT '分类',

  `icon`      VARCHAR(20)  DEFAULT '🪙'       COMMENT '图标',

  `status`     VARCHAR(20)  DEFAULT 'ACTIVE'     COMMENT 'ACTIVE=上架 INACTIVE=下架',

  `created_at`   DATETIME    DEFAULT CURRENT_TIMESTAMP,

  `updated_at`   DATETIME    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  PRIMARY KEY (`id`),

  UNIQUE KEY `uk_coin_product_id` (`product_id`)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='硬币商品表';



-- 3. 硬币购买记录表

CREATE TABLE IF NOT EXISTS `coin_purchase` (

  `id`       BIGINT     NOT NULL AUTO_INCREMENT,

  `user_id`    BIGINT     NOT NULL         COMMENT '用户ID',

  `username`    VARCHAR(100)  DEFAULT NULL       COMMENT '冗余用户名',

  `product_id`   VARCHAR(64)  NOT NULL         COMMENT '商品编号',

  `product_name`  VARCHAR(200)  NOT NULL         COMMENT '商品名称',

  `coin_price`   INT      NOT NULL         COMMENT '消耗硬币数',

  `coin_before`  INT      DEFAULT NULL       COMMENT '购买前余额',

  `coin_after`   INT      DEFAULT NULL       COMMENT '购买后余额',

  `status`     VARCHAR(20)  DEFAULT 'SUCCESS'    COMMENT 'SUCCESS=成功',

  `created_at`   DATETIME    DEFAULT CURRENT_TIMESTAMP,

  PRIMARY KEY (`id`),

  KEY `idx_coin_purchase_user_id` (`user_id`),

  KEY `idx_coin_purchase_product_id` (`product_id`),

  KEY `idx_coin_purchase_created_at` (`created_at`)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='硬币购买记录表';



-- 4. 初始化 9 个硬币商品（每次启动覆盖更新，修复乱码）

INSERT INTO `coin_product` (`product_id`, `product_name`, `description`, `coin_price`, `category`, `icon`, `status`)

VALUES

  ('coin-ds-visual',     '数据结构图解手册',  '图文并茂讲解数组/链表/树/图等核心数据结构', 120,  'coin', '📊', 'ACTIVE'),

  ('coin-algo-notes',    '算法基础笔记',    '常见排序/查找/递归算法详细笔记',      100,  'coin', '📝', 'ACTIVE'),

  ('coin-java-notes',    'JavaSE核心笔记',   '面向对象/集合/多线程/IO知识点总结',    150,  'coin', '☕', 'ACTIVE'),

  ('coin-interview100',   '面试必刷100题',   '高频面试题集含详细解析',          350,  'coin', '🎯', 'ACTIVE'),

  ('coin-algo-tutorial',   '算法入门教程',    '零基础入门含视频讲解和实战',        300,  'coin', '🎓', 'ACTIVE'),

  ('coin-advanced-tutorial','算法进阶教程',    '面试竞赛级高级算法教程',          500,  'coin', '🚀', 'ACTIVE'),

  ('coin-viz-project',    '可视化项目源码',   'AlgoViz算法可视化项目完整源码',       800,  'coin', '💻', 'ACTIVE'),

  ('coin-ai-project',    'AI辅助算法项目',   '结合AI的智能算法分析系统',         1000, 'coin', '🌟', 'ACTIVE'),

  ('coin-dp-master',     '动态规划专题',    '从入门到精通DP经典题集',          280,  'coin', '🧩', 'ACTIVE')

ON DUPLICATE KEY UPDATE

  `product_name` = VALUES(`product_name`),

  `description`  = VALUES(`description`),

  `coin_price`  = VALUES(`coin_price`),

  `icon`     = VALUES(`icon`);



SELECT '货币系统初始化完成' AS message;
```



## 屏蔽词

```

-- 关键词屏蔽审核系统建表脚本
-- 依赖: MySQL 8.0 + algoviz 库

-- 1. 敏感词表（工作区，可增删改；发布版本后快照冻结）
CREATE TABLE IF NOT EXISTS sensitive_word (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    word        VARCHAR(128) NOT NULL COMMENT '敏感词',
    category    VARCHAR(32)  NOT NULL DEFAULT 'ABUSE' COMMENT '分类: ABUSE辱骂/POLITICS涉政/ADVERTISING广告/PORN色情/OTHER',
    LEVEL       VARCHAR(16)  NOT NULL DEFAULT 'MEDIUM' COMMENT '等级: HIGH拦截/MEDIUM待审/LOW仅记录',
    match_mode  VARCHAR(16)  NOT NULL DEFAULT 'EXACT' COMMENT '匹配模式: EXACT/FUZZY',
    enabled     TINYINT      NOT NULL DEFAULT 1 COMMENT '是否启用',
    create_time DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_word (word)
) ENGINE=INNODB DEFAULT CHARSET=utf8mb4 COMMENT='敏感词库（工作区）';

-- 2. 敏感词版本表（每次"发布版本"将当前词库快照冻结存档，支持回滚）
CREATE TABLE IF NOT EXISTS sensitive_word_version (
    id            BIGINT AUTO_INCREMENT PRIMARY KEY,
    version_no    INT          NOT NULL COMMENT '版本号（递增）',
    word_count    INT          NOT NULL DEFAULT 0 COMMENT '该版本词条数',
    snapshot_json LONGTEXT     COMMENT '词库快照 JSON [{word,category,level}]',
    remark        VARCHAR(256) COMMENT '版本说明',
    created_by    VARCHAR(64)  COMMENT '发布人',
    create_time   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_version (version_no)
) ENGINE=INNODB DEFAULT CHARSET=utf8mb4 COMMENT='敏感词版本快照';

-- 3. 危险代码规则表（正则规则，检测提交代码中的危害模式）
CREATE TABLE IF NOT EXISTS dangerous_code_rule (
    id           BIGINT AUTO_INCREMENT PRIMARY KEY,
    rule_code    VARCHAR(64)  NOT NULL COMMENT '规则编码',
    rule_name    VARCHAR(128) COMMENT '规则名称',
    LANGUAGE     VARCHAR(32)  NOT NULL DEFAULT 'ALL' COMMENT '适用语言: ALL/JAVA/PYTHON/JS/CPP',
    rule_type    VARCHAR(32)  NOT NULL DEFAULT 'REGEX' COMMENT '规则类型: REGEX/KEYWORD',
    rule_content TEXT         NOT NULL COMMENT '规则内容（正则或关键词）',
    risk_level   VARCHAR(16)  NOT NULL DEFAULT 'HIGH' COMMENT '风险等级: HIGH/MEDIUM/LOW',
    score        INT          NOT NULL DEFAULT 80 COMMENT '风险分值',
    enabled      TINYINT      NOT NULL DEFAULT 1,
    DESCRIPTION  VARCHAR(256),
    create_time  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_rule_code (rule_code)
) ENGINE=INNODB DEFAULT CHARSET=utf8mb4 COMMENT='危险代码检测规则';

-- 4. 内容审核记录表（人工审核完毕后写入；BLOCK 拦截记录也落此表）
CREATE TABLE IF NOT EXISTS content_audit_record (
    id               BIGINT AUTO_INCREMENT PRIMARY KEY,
    submit_id        VARCHAR(64)  NOT NULL COMMENT '提交唯一标识',
    user_id          BIGINT       COMMENT '提交用户ID',
    problem_id       BIGINT       COMMENT '关联题目ID',
    problem_no       VARCHAR(64)  COMMENT '关联题目编号',
    content_type     VARCHAR(32)  COMMENT '内容类型: QUESTION/CODE/COMMENT',
    LANGUAGE         VARCHAR(32),
    risk_level       VARCHAR(16)  COMMENT 'HIGH/MEDIUM/LOW/NONE',
    hit_details      JSON         COMMENT '命中详情',
    total_score      INT          DEFAULT 0,
    content_snapshot TEXT         COMMENT '内容快照（截断）',
    pre_check_status VARCHAR(16)  COMMENT 'BLOCK/PASS',
    audit_status     VARCHAR(16)  COMMENT 'pending/pass/reject/blocked/logonly',
    audit_remark     VARCHAR(512),
    auditor_id       VARCHAR(64),
    audit_time       DATETIME,
    es_doc_id        VARCHAR(64)  COMMENT '来源 ES 文档ID',
    submit_time      DATETIME,
    create_time      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_submit_id (submit_id),
    KEY idx_audit_status (audit_status),
    KEY idx_submit_time (submit_time)
) ENGINE=INNODB DEFAULT CHARSET=utf8mb4 COMMENT='内容审核记录';

-- 敏感词（示例库：广告/违规类，等级含义 HIGH=拦截 MEDIUM=待审 LOW=仅记录）
INSERT INTO sensitive_word (word, category, LEVEL) VALUES
('赌博', 'OTHER', 'HIGH'),
('博彩', 'OTHER', 'HIGH'),
('代考', 'ADVERTISING', 'HIGH'),
('代写作业', 'ADVERTISING', 'HIGH'),
('刷单', 'ADVERTISING', 'MEDIUM'),
('外挂', 'OTHER', 'MEDIUM'),
('翻墙', 'OTHER', 'MEDIUM'),
('发票代开', 'ADVERTISING', 'MEDIUM'),
('加微信', 'ADVERTISING', 'LOW'),
('私聊接单', 'ADVERTISING', 'LOW')
ON DUPLICATE KEY UPDATE update_time = update_time;

-- 危险代码规则
INSERT INTO dangerous_code_rule (rule_code, rule_name, LANGUAGE, rule_type, rule_content, risk_level, score, DESCRIPTION) VALUES
('DC-001', 'Java命令执行', 'JAVA', 'REGEX', 'Runtime\\.getRuntime\\(\\)\\.exec', 'HIGH', 95, '检测 Java 执行系统命令'),
('DC-002', 'Java进程构建', 'JAVA', 'REGEX', 'new\\s+ProcessBuilder', 'HIGH', 90, '检测 Java 进程构建器'),
('DC-003', 'Python命令执行', 'PYTHON', 'REGEX', 'os\\.(system|popen)\\s*\\(', 'HIGH', 95, '检测 Python 执行系统命令'),
('DC-004', 'eval动态执行', 'ALL', 'REGEX', '\\beval\\s*\\(', 'HIGH', 85, '检测 eval 动态代码执行'),
('DC-005', '删除根目录', 'ALL', 'REGEX', 'rm\\s+-rf\\s+/', 'HIGH', 100, '检测 rm -rf / 破坏性命令'),
('DC-006', 'Python删目录', 'PYTHON', 'REGEX', 'shutil\\.rmtree', 'HIGH', 85, '检测递归删除目录'),
('DC-007', '反向Shell', 'ALL', 'REGEX', '/bin/(ba)?sh\\s+-i', 'HIGH', 95, '检测反弹Shell特征'),
('DC-008', '死循环攻击', 'ALL', 'REGEX', 'while\\s*\\(\\s*true\\s*\\)\\s*\\{\\s*\\}', 'MEDIUM', 60, '检测空死循环占用CPU'),
('DC-009', '端口扫描', 'ALL', 'REGEX', '(port|PORT)\\s*(_?)+\\d{2,5}.*for.*range', 'MEDIUM', 70, '疑似端口扫描循环'),
('DC-010', '矿池地址', 'ALL', 'REGEX', '(stratum\\+tcp|miner|矿池)', 'HIGH', 90, '挖矿特征')
ON DUPLICATE KEY UPDATE update_time = update_time;

```



## 新增用户题解表

```
-- 10. 用户题解表
DROP TABLE IF EXISTS `oj_solution`;
CREATE TABLE `oj_solution` (
    `id`               BIGINT         NOT NULL AUTO_INCREMENT COMMENT '主键',
    `problem_id`       BIGINT         NOT NULL COMMENT '题目ID',
    `problem_title`    VARCHAR(200)   DEFAULT NULL COMMENT '题目标题冗余',
    `user_id`          BIGINT         NOT NULL COMMENT '发布用户ID',
    `username`         VARCHAR(100)   DEFAULT NULL COMMENT '用户名冗余',
    `avatar`           VARCHAR(500)   DEFAULT NULL COMMENT '头像冗余',

    `title`            VARCHAR(200)   NOT NULL COMMENT '题解标题',
    `format`           VARCHAR(100)   DEFAULT NULL COMMENT '解题格式（双指针/DP/回溯等）',
    `idea`             MEDIUMTEXT     COMMENT '思路',
    `process`          MEDIUMTEXT     COMMENT '解题过程',
    `complexity`       VARCHAR(500)   DEFAULT NULL COMMENT '复杂度分析',
    `code_lang`        VARCHAR(50)    DEFAULT 'java' COMMENT '代码语言',
    `code`             MEDIUMTEXT     COMMENT '题解代码',

    `like_count`       INT            DEFAULT 0 COMMENT '点赞数',
    `view_count`       INT            DEFAULT 0 COMMENT '观看数',
    `comment_count`    INT            DEFAULT 0 COMMENT '评论数',

    `is_passed`        TINYINT(1)     DEFAULT 0 COMMENT '是否已AC该题',
    `is_featured`      TINYINT(1)     DEFAULT 0 COMMENT '精选置顶',

    `audit_status`     VARCHAR(20)    DEFAULT 'none' COMMENT '审核状态 none/pending/blocked/passed/rejected',
    `risk_level`       VARCHAR(10)    DEFAULT 'NONE' COMMENT '风险等级 NONE/LOW/MEDIUM/HIGH',
    `detect_summary`   VARCHAR(1000)  DEFAULT NULL COMMENT '检测命中摘要',

    `status`           VARCHAR(20)    DEFAULT 'PUBLISHED' COMMENT 'PUBLISHED/HIDDEN/DELETED',
    `created_at`       DATETIME       DEFAULT CURRENT_TIMESTAMP,
    `updated_at`       DATETIME       DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    PRIMARY KEY (`id`),
    KEY `idx_solution_problem` (`problem_id`, `status`, `created_at` DESC),
    KEY `idx_solution_user` (`user_id`, `created_at` DESC),
    KEY `idx_solution_audit` (`audit_status`, `created_at` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='OJ用户题解表';

```

## RBAC

```
-- 0台管理员用户表 id为入职公司的员工超级管理员为1 长度为char 无服务
CREATE TABLE `sys_user` (
    `id`              BIGINT       NOT NULL AUTO_INCREMENT,
    `username`        VARCHAR(64)  NOT NULL COMMENT '登录账号',
    `password`        VARCHAR(128) NOT NULL COMMENT 'bcrypt 加密密码',
    `real_name`       VARCHAR(64)  NOT NULL COMMENT '真实姓名',
    `email`           VARCHAR(128) DEFAULT NULL COMMENT '邮箱',
    `phone`           VARCHAR(20)  DEFAULT NULL COMMENT '手机号',
    `avatar`          VARCHAR(256) DEFAULT NULL COMMENT '头像 URL',
    `status`          TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-正常 2-封禁',
    `account_type`    TINYINT      NOT NULL DEFAULT 1 COMMENT '账号类型: 1-内部管理员 2-外部出题人',
    `last_login_time` DATETIME     DEFAULT NULL COMMENT '最后登录时间',
    `last_login_ip`   VARCHAR(45)  DEFAULT NULL COMMENT '最后登录 IP',
    `created_by`      BIGINT       DEFAULT NULL COMMENT '创建人 ID',
    `created_at`      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `deleted`         TINYINT      NOT NULL DEFAULT 0 COMMENT '软删除: 0-未删除 1-已删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    KEY `idx_status` (`status`),
    KEY `idx_account_type` (`account_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='后台用户表';


-- 1. 角色表
-- ============================================================
CREATE TABLE `sys_role` (
    `id`             BIGINT      NOT NULL AUTO_INCREMENT,
    `role_code`      VARCHAR(64) NOT NULL COMMENT '角色编码',
    `role_name`      VARCHAR(64) NOT NULL COMMENT '角色名称',
    `role_level`     TINYINT     NOT NULL COMMENT '角色层级: 1-超级 2-一级 3-二级',
    `parent_role_id` BIGINT      DEFAULT NULL COMMENT '父角色 ID（二级角色归属，NULL 表示顶级）',
    `data_scope`     TINYINT     NOT NULL DEFAULT 1 COMMENT '数据范围: 1-全部数据 2-本部门 3-本人数据',
    `description`    VARCHAR(256) DEFAULT NULL COMMENT '角色描述',
    `status`         TINYINT     NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-正常',
    `is_system`      TINYINT     NOT NULL DEFAULT 0 COMMENT '是否系统内置: 0-否 1-是（不可删除）',
    `created_at`     DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`     DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_code` (`role_code`),
    KEY `idx_role_level` (`role_level`),
    KEY `idx_parent_role` (`parent_role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 2. 菜单表（树形结构，含目录/菜单/按钮三种类型）
-- ============================================================
CREATE TABLE `sys_menu` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `parent_id`  BIGINT       NOT NULL DEFAULT 0 COMMENT '父菜单 ID，0 为根节点',
    `menu_name`  VARCHAR(64)  NOT NULL COMMENT '菜单名称',
    `menu_type`  TINYINT      NOT NULL COMMENT '类型: 1-目录 2-菜单 3-按钮',
    `path`       VARCHAR(128) DEFAULT NULL COMMENT '路由路径',
    `component`  VARCHAR(128) DEFAULT NULL COMMENT '前端组件路径',
    `perms`      VARCHAR(128) DEFAULT NULL COMMENT '权限标识（如 content:ds:list）',
    `icon`       VARCHAR(64)  DEFAULT NULL COMMENT '菜单图标',
    `sort_order` INT          NOT NULL DEFAULT 0 COMMENT '排序号',
    `visible`    TINYINT      NOT NULL DEFAULT 1 COMMENT '是否可见: 0-隐藏 1-显示',
    `status`     TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-正常',
    `created_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_parent` (`parent_id`),
    KEY `idx_type` (`menu_type`),
    KEY `idx_sort` (`sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单权限表';

-- 3. 权限点表（按钮级权限）
CREATE TABLE `sys_permission` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `menu_id`     BIGINT       NOT NULL COMMENT '所属菜单 ID',
    `perm_code`   VARCHAR(128) NOT NULL COMMENT '权限编码（如 content:ds:add）',
    `perm_name`   VARCHAR(64)  NOT NULL COMMENT '权限名称',
    `perm_type`   TINYINT      NOT NULL COMMENT '类型: 1-新增 2-编辑 3-删除 4-导出 5-审核 6-其他',
    `api_method`  VARCHAR(10)  DEFAULT NULL COMMENT 'API 方法: GET/POST/PUT/DELETE',
    `api_path`    VARCHAR(256) DEFAULT NULL COMMENT 'API 路径',
    `status`      TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-正常',
    `created_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_perm_code` (`perm_code`),
    KEY `idx_menu` (`menu_id`),
    KEY `idx_type` (`perm_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限点表（按钮级）';

-- 4 用户-角色关联表
CREATE TABLE `sys_user_role` (
    `id`         BIGINT   NOT NULL AUTO_INCREMENT,
    `user_id`    BIGINT   NOT NULL COMMENT '用户 ID',
    `role_id`    BIGINT   NOT NULL COMMENT '角色 ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_role` (`user_id`, `role_id`),
    KEY `idx_user` (`user_id`),
    KEY `idx_role` (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';

-- 5 角色-菜单关联表
CREATE TABLE `sys_role_menu` (
    `id`         BIGINT   NOT NULL AUTO_INCREMENT,
    `role_id`    BIGINT   NOT NULL COMMENT '角色 ID',
    `menu_id`    BIGINT   NOT NULL COMMENT '菜单 ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_menu` (`role_id`, `menu_id`),
    KEY `idx_role` (`role_id`),
    KEY `idx_menu` (`menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';

-- 6 角色-权限关联表
CREATE TABLE `sys_role_permission` (
    `id`            BIGINT   NOT NULL AUTO_INCREMENT,
    `role_id`       BIGINT   NOT NULL COMMENT '角色 ID',
    `permission_id` BIGINT   NOT NULL COMMENT '权限 ID',
    `created_at`    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_perm` (`role_id`, `permission_id`),
    KEY `idx_role` (`role_id`),
    KEY `idx_perm` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';

-- 7 数据权限规则表（行级数据权限）
CREATE TABLE `sys_data_permission_rule` (
    `id`              BIGINT      NOT NULL AUTO_INCREMENT,
    `role_id`         BIGINT      NOT NULL COMMENT '角色 ID',
    `target_table`    VARCHAR(64) NOT NULL COMMENT '目标表名（如 t_algorithm, t_solution）',
    `rule_type`       TINYINT     NOT NULL COMMENT '规则类型: 1-全部数据 2-本人创建 3-指定部门 4-自定义 SQL',
    `rule_expression` TEXT        DEFAULT NULL COMMENT '规则表达式（如 created_by = {current_user_id}）',
    `status`          TINYINT     NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-正常',
    `created_at`      DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`      DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_role` (`role_id`),
    KEY `idx_table` (`target_table`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数据权限规则表';

-- 8 审计日志表（不可删除，仅超级管理员和安全审计管理员可查）
CREATE TABLE `sys_audit_log` (
    `id`             BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`        BIGINT       NOT NULL COMMENT '操作人 ID',
    `username`       VARCHAR(64)  NOT NULL COMMENT '操作人账号',
    `role_name`      VARCHAR(64)  DEFAULT NULL COMMENT '操作时角色',
    `module`         VARCHAR(64)  NOT NULL COMMENT '操作模块',
    `operation`      VARCHAR(128) NOT NULL COMMENT '操作行为',
    `method`         VARCHAR(10)  DEFAULT NULL COMMENT '请求方法',
    `request_url`    VARCHAR(512) DEFAULT NULL COMMENT '请求 URL',
    `request_params` TEXT         DEFAULT NULL COMMENT '请求参数',
    `response_code`  INT          DEFAULT NULL COMMENT '响应状态码',
    `ip`             VARCHAR(45)  NOT NULL COMMENT '操作 IP',
    `location`       VARCHAR(128) DEFAULT NULL COMMENT '操作地点',
    `cost_time`      BIGINT       DEFAULT NULL COMMENT '耗时(ms)',
    `operation_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    PRIMARY KEY (`id`),
    KEY `idx_user` (`user_id`),
    KEY `idx_module` (`module`),
    KEY `idx_time` (`operation_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审计日志表';


-- 10. 管理员日志登录表
-- ============================================================
CREATE TABLE `sys_login_log` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`    BIGINT       DEFAULT NULL COMMENT '用户 ID',
    `username`   VARCHAR(64)  NOT NULL COMMENT '登录账号',
    `login_type` TINYINT      NOT NULL COMMENT '登录类型: 1-登录 2-退出',
    `status`     TINYINT      NOT NULL COMMENT '状态: 0-失败 1-成功',
    `ip`         VARCHAR(45)  NOT NULL COMMENT '登录 IP',
    `location`   VARCHAR(128) DEFAULT NULL COMMENT '登录地点',
    `browser`    VARCHAR(64)  DEFAULT NULL COMMENT '浏览器',
    `os`         VARCHAR(64)  DEFAULT NULL COMMENT '操作系统',
    `message`    VARCHAR(256) DEFAULT NULL COMMENT '提示消息',
    `login_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '登录时间',
    PRIMARY KEY (`id`),
    KEY `idx_user` (`user_id`),
    KEY `idx_time` (`login_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='登录日志表';

-- 11.管理员业务日志表
CREATE TABLE `sys_operation_log` (
    `id`             BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`        BIGINT       NOT NULL COMMENT '操作人 ID',
    `username`       VARCHAR(64)  NOT NULL COMMENT '操作人账号',
    `business_type`  VARCHAR(64)  NOT NULL COMMENT '业务类型（内容发布/金币调整/订单处理等）',
    `operation_type` VARCHAR(32)  NOT NULL COMMENT '操作类型（新增/编辑/删除/审核/导出）',
    `target_id`      BIGINT       DEFAULT NULL COMMENT '操作对象 ID',
    `target_name`    VARCHAR(128) DEFAULT NULL COMMENT '操作对象名称',
    `before_data`    TEXT         DEFAULT NULL COMMENT '变更前数据（JSON）',
    `after_data`     TEXT         DEFAULT NULL COMMENT '变更后数据（JSON）',
    `remark`         VARCHAR(256) DEFAULT NULL COMMENT '备注',
    `operation_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    PRIMARY KEY (`id`),
    KEY `idx_user` (`user_id`),
    KEY `idx_business` (`business_type`),
    KEY `idx_time` (`operation_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='业务操作日志表';
```

## oj题解 品论审核接口权限注册

```
-- ============================================================================
-- 迁移说明：OJ 题解/评论审核接口权限注册
--
-- 背景：
--   OJSolutionAdminController (/api/admin/solutions) 原先未纳入 Sa-Token 登录拦截，
--   未携带 satoken 的请求也能 200 通过。现已：
--     Step1: SaTokenConfig.match 新增 /api/admin/** 强制登录校验
--     Step2: Controller 6+3 个方法加 @SaCheckPermission 注解
--            - 题解审核：oj:solution:audit
--            - 评论审核：oj:solution:comment:audit
--   本脚本（Step3）负责把这两个权限码注册到 DB，并绑定到已创建的审核角色
--   sol_audit_01，使"审核评论管理员"登录后能通过注解校验。
--
-- 执行前提：
--   - 角色已创建：sys_role.role_code = 'sol_audit_01'
--   - 超级管理员（sys_user.id=1）无需执行本脚本，StpInterfaceImpl 返回 ["*"] 自动通配
--
-- 幂等性：所有 INSERT 先 DELETE 去重，可重复执行
-- ============================================================================

USE algoviz;

-- 取审核角色 id（SOLUTION_COMMENT_AUDITOR），用于后续绑定
SET @role_audit_id = (SELECT id FROM sys_role WHERE role_code = 'SOLUTION_COMMENT_AUDITOR' LIMIT 1);

-- ============================================================================
-- 方案 A：走 sys_menu（menu_type=3 按钮级权限）+ sys_role_menu 绑定
--   StpInterfaceImpl.findPermissionCodesByUserId 的 UNION 第二支会读 sys_menu.perms
-- ============================================================================

-- A.1 清理旧记录（幂等）
DELETE FROM sys_role_menu
 WHERE role_id = @role_audit_id
   AND menu_id IN (
       SELECT id FROM sys_menu WHERE perms IN ('oj:solution:audit', 'oj:solution:comment:audit')
   );

DELETE FROM sys_menu WHERE perms IN ('oj:solution:audit', 'oj:solution:comment:audit');

-- A.2 注册按钮级权限菜单（parent_id=0 顶层；如已有 OJ 父菜单可按需调整 parent_id）
INSERT INTO sys_menu (parent_id, menu_name, menu_type, path, component, perms, icon, sort_order, visible, status, created_at, updated_at)
VALUES
  (0, '题解审核权限', 3, NULL, NULL, 'oj:solution:audit',         NULL, 0, 1, 1, NOW(), NOW()),
  (0, '评论审核权限', 3, NULL, NULL, 'oj:solution:comment:audit', NULL, 0, 1, 1, NOW(), NOW());

-- A.3 绑定到 sol_audit_01 角色
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT @role_audit_id, id FROM sys_menu
 WHERE perms IN ('oj:solution:audit', 'oj:solution:comment:audit');

-- ============================================================================
-- 方案 B：走 sys_permission（perm_type=5 审核）+ sys_role_permission 绑定
--   StpInterfaceImpl.findPermissionCodesByUserId 的 UNION 第一支会读 sys_permission.perm_code
--   与方案 A 双保险，任意一张表有权限码都能通过 @SaCheckPermission 校验
-- ============================================================================

-- B.1 清理旧记录（幂等）
DELETE FROM sys_role_permission
 WHERE role_id = @role_audit_id
   AND permission_id IN (
       SELECT id FROM sys_permission WHERE perm_code IN ('oj:solution:audit', 'oj:solution:comment:audit')
   );

DELETE FROM sys_permission WHERE perm_code IN ('oj:solution:audit', 'oj:solution:comment:audit');

-- B.2 注册权限点（perm_type=5 表示审核类；menu_id 关联到方案 A 刚插入的菜单记录）
SET @menu_sol_id = (SELECT id FROM sys_menu WHERE perms = 'oj:solution:audit' LIMIT 1);
SET @menu_cmt_id = (SELECT id FROM sys_menu WHERE perms = 'oj:solution:comment:audit' LIMIT 1);

INSERT INTO sys_permission (menu_id, perm_code, perm_name, perm_type, api_method, api_path, status, created_at, updated_at)
VALUES
  (@menu_sol_id, 'oj:solution:audit',         'OJ题解审核', 5, 'GET/PUT', '/api/admin/solutions',           1, NOW(), NOW()),
  (@menu_cmt_id, 'oj:solution:comment:audit', 'OJ评论审核', 5, 'GET/PUT', '/api/admin/solutions/comments',  1, NOW(), NOW());

-- B.3 绑定到 sol_audit_01 角色
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT @role_audit_id, id FROM sys_permission
 WHERE perm_code IN ('oj:solution:audit', 'oj:solution:comment:audit');

-- ============================================================================
-- 验证查询（执行后应看到 2 行 menu + 2 行 permission + 4 行绑定）
-- ============================================================================

SELECT '== sys_menu ==' AS section;
SELECT id, parent_id, menu_name, menu_type, perms, status FROM sys_menu
 WHERE perms IN ('oj:solution:audit', 'oj:solution:comment:audit');

SELECT '== sys_permission ==' AS section;
SELECT id, perm_code, perm_name, perm_type, status FROM sys_permission
 WHERE perm_code IN ('oj:solution:audit', 'oj:solution:comment:audit');

SELECT '== sys_role_menu (SOLUTION_COMMENT_AUDITOR) ==' AS section;
SELECT rm.role_id, r.role_code, rm.menu_id, m.perms
  FROM sys_role_menu rm
  JOIN sys_role r ON r.id = rm.role_id
  JOIN sys_menu m ON m.id = rm.menu_id
 WHERE r.role_code = 'SOLUTION_COMMENT_AUDITOR';

SELECT '== sys_role_permission (SOLUTION_COMMENT_AUDITOR) ==' AS section;
SELECT rp.role_id, r.role_code, rp.permission_id, p.perm_code
  FROM sys_role_permission rp
  JOIN sys_role r ON r.id = rp.role_id
  JOIN sys_permission p ON p.id = rp.permission_id
 WHERE r.role_code = 'SOLUTION_COMMENT_AUDITOR';

```

## **增加索引**

```
-- ============================================================================
-- 迁移说明：题解/评论审核管理列表查询索引优化
--
-- 背景：
--   后台管理员按 audit_status 筛选评论/题解列表时，COUNT 查询耗时 ~90ms
--   根因：现有索引 (audit_status, created_at) 不含 status 列，
--   MySQL 判定 status != 'DELETED' 时必须回表（随机 I/O），4.6 万行 = 36K 次回表
--
-- 优化：
--   将 status 作为索引尾列加入，变为覆盖索引 (audit_status, created_at, status)
--   - COUNT: 纯索引扫描，零回表
--   - SELECT + ORDER BY created_at DESC + LIMIT: 索引正序读 → created_at DESC，
--     status 从索引取 → 零回表，LIMIT 20 只读 20 条
--   - status 放在末尾不改变索引排序，不影响 ORDER BY 优化
--
-- 幂等：先 DROP IF EXISTS 再 CREATE
-- ============================================================================

-- USE algoviz;  -- 按实际库名修改，或命令行 -D 指定

-- ① oj_solution_comment：评论审核列表覆盖索引
DROP INDEX idx_comment_audit ON oj_solution_comment;
CREATE INDEX idx_comment_audit ON oj_solution_comment (audit_status, created_at DESC, status);

-- ② oj_solution：题解审核列表覆盖索引
DROP INDEX idx_solution_audit ON oj_solution;
CREATE INDEX idx_solution_audit ON oj_solution (audit_status, created_at DESC, status);

```



# 后续新增数据表

1. `coin_record` 金币流水表（重中之重！）

> 现在只有 `user.coins` 余额字段，**无流水 = 无法对账、无法排查扣减异常、无法做回滚**

- 用途：每一次金币增减（充值、AI 消耗、购买、退款、补偿）都落一条流水
- 字段示例：user_id、change_amount、balance_after、biz_type、biz_id、remark、created_at

3. `interview_category` 面试题分类表

> 现在只有 `interview_tag`（标签），缺少**一级 / 二级分类**（Java、计算机网络、操作系统、MySQL） tag 是多标签、category 是归属分类，二者用途不一样

4. `oj_language` OJ 编程语言表

> 代码提交支持 Java/Python/Go/C++ 等，统一维护语言、编译命令、运行限制，不要硬编码

6. `user_session` / `token_store`（可选，看登录方案）

如果不用 Redis 存 token，数据库持久化登录会话、单点登出、设备管理

7. `refund_record` 退款记录表

`orders/payment_record` 只有支付，退款、原路退款、退款状态单独记录，财务对账刚需

8. `ai_usage_record` AI 消耗流水

`ai_prompt` 是提示词模板，**AI 调用扣费记录**要单独流水：用户、prompt_id、消耗金币、调用时间、消耗 token

## 🟢 【可选，看你产品规划】

- `exam` / `exam_question`：模拟套卷、在线模考（批量抽八股题组成试卷）
- `user_achievement` / `user_badge`：刷题勋章、成就、打卡
- `notice_read`：公告已读记录（announcement 是公告，要记录哪些用户读过）
- `oj_problem_tag`：OJ 题目标签（和面试八股标签隔离，两套题库标签分开）
- `dictionary` 通用字典表（状态、常量枚举，替代硬编码）

## ⚠️ 同时发现现有表的 2 个潜在问题

1. coin_purchase /orders/payment_record 职责容易混淆
   - 建议清晰划分：orders (订单主单) → coin_purchase (金币商品订单扩展) → payment_record (支付渠道回执) → coin_record (金币入账流水)
2. app_user 和 user 是什么关系？
   - 如果是一套用户：建议合并，避免双用户体系、数据不一致；
   - 如果是区分后台 admin 和 C 端用户：`user=C端`、`admin=后台管理员`，app_user 多余。

### 1. 财务 & 资产类（大厂付费业务硬性底线，重中之重）

- `coin_record` **金币变更流水表**

  > 你现在只有 user.coins 余额字段。大厂资产类一律**不相信内存 / 缓存计算，只认流水**，所有增减（充值、AI 扣费、购买商品、退款、补偿、扣罚）强制落流水，对账、回滚、客诉排查唯一依据。 关键字段：user_id、biz_type、biz_id、change_coins、after_balance、operator、created_at

- `refund_record` **退款主表**

  > orders/payment_record 只覆盖正向支付；退款、部分退款、渠道退款回执、退款失败重试、退款对账单独落地，财务审计必备。

- `ai_usage_record` **AI 调用 & 扣费流水**

  > ai_prompt 是模板，**每一次用户实际调用、token 消耗、扣金币记录**单独流水，可按用户 / 提示词 / 日期统计消耗、对账。

### 3. 题库体系缺失基础分类（牛客 / LeetCode 标准分层）

- `interview_category` 面试题分类（计算机网络 / MySQL/Java 等一级二级分类）

> `interview_tag` 是多标签，**分类是归属域**，二者职责完全不同，不能混用

- `oj_problem_tag` OJ 题目标签表（和面试题标签隔离，两套题库标签分开维护）

### 4. 会话 & 风控基础

- ```
  user_session
  ```

   用户会话表

  > 账号多设备登录、单点下线、设备指纹、登录有效期、异地登录风控，大厂账号体系标配（也可 Redis + 持久化兜底）

### 5. 公告配套

- `announcement_read` 公告已读记录表

> announcement 只是公告内容；要区分哪些用户已读、未读、弹窗标记，做消息触达统计

------

## 🟡 推荐，

### 题库 & 社区

- `interview_comment` 面试题评论表（支持回复、点赞、折叠）

- ```
  oj_problem_version
  ```

   OJ 题目版本表

  > 题目修改、样例变更、判题规则变更保留版本快照，线上改错题目可以回滚，大厂 OJ 标配

- `exam`、`exam_problem` 模拟试卷 / 模考表（按分类 / 标签组卷、限时考试、自动判分）

### OJ 判题配套（工业级判题必备）

- ```
  judge_server
  ```

   判题机集群表

  > 多判题节点管理、负载、状态上报、限流隔离，分布式判题平台必用

- ```
  oj_language
  ```

   编程语言配置表

  > 语言名称、编译命令、时间 / 内存限制、环境镜像，不要硬编码在代码里

### 用户成长体系

- `user_achievement` / `user_badge` 勋章、成就、打卡、刷题积分
- `user_level` 用户等级、经验值流水（配套积分记录表，和 coin_record 思路一致，资产流水化）

### 数据字典（规范枚举，大厂规范）

- `sys_dict`、`sys_dict_item` 通用字典表

> 状态、业务类型、支付渠道、判题结果等枚举统一维护，不再硬编码字符串 / 数字

# 完整数据库

引擎  字符集 统一为 InnoDB utf8mb4

| #    | 表名               | 主键        | 关键索引                        | 行数 (预期) | 业务注释                                        |
| ---- | ------------------ | ----------- | ------------------------------- | ----------- | ----------------------------------------------- |
| 1    | user               | BIGINT 自增 | uk_username,uk_email            | 100w+       | C 端用户主表，存储账号基础信息、金币余额        |
| 4    | permission         | BIGINT 自增 | uk_perm_code                    | 200 以内    | 权限点表，菜单 / 接口权限标识                   |
| 5    | role_permission    | BIGINT 自增 | idx_role_id,idx_perm_id         | 500 以内    | 角色 - 权限多对多关联表                         |
| 6    | user_session       | VARCHAR(32) | idx_user_id,idx_expire_time     | 150w+       | 用户登录会话表，管理设备登录、单点下线          |
| 7    | login_log          | VARCHAR(32) | idx_user_id,idx_login_time      | 500w+       | 用户登录日志，记录 IP、设备、登录结果，风控溯源 |
| 8    | api_log            | VARCHAR(32) | idx_user_id,idx_req_time        | 千万级      | API 访问日志，接口耗时、入参摘要，监控排查      |
| 9    | operation_log      | VARCHAR(32) | idx_admin_id,idx_op_time        | 10w+        | 后台管理员敏感操作审计日志                      |
| 10   | sys_dict           | BIGINT 自增 | uk_dict_code                    | 50 以内     | 系统字典主表，统一管理业务枚举分类              |
| 11   | sys_dict_item      | BIGINT 自增 | idx_dict_id,uk_dict_val         | 500 以内    | 字典项明细，替代硬编码状态值                    |
| 12   | statistics         | BIGINT 自增 | idx_stat_date,idx_stat_type     | 3650+       | 业务统计表，日活、提交量、充值汇总              |
| 13   | system_config      | BIGINT 自增 | uk_config_key                   | 100 以内    | 全局系统配置项（开关、阈值、渠道配置）          |
| 14   | gray_release       | BIGINT 自增 | uk_gray_key                     | 20 以内     | 灰度放量策略表，新功能 / AI 能力灰度控制        |
| 15   | user_risk_log      | VARCHAR(32) | idx_user_id,idx_risk_time       | 50w+        | 用户风控日志，爬虫、高频提交、异常登录记录      |
| 16   | user_message       | VARCHAR(32) | idx_user_id,idx_read            | 300w+       | 用户站内信 / 系统通知（区别于公告）             |
| 17   | announcement       | BIGINT 自增 | idx_publish_time                | 200 以内    | 平台公告主内容表                                |
| 18   | announcement_read  | VARCHAR(32) | uk_ann_user,idx_user_id         | 500w+       | 公告已读记录，统计用户是否查看公告              |
| 19   | feedback           | VARCHAR(32) | idx_user_id,idx_create_time     | 10w+        | 用户意见反馈表                                  |
| 20   | file_storage       | VARCHAR(32) | uk_file_key,idx_user_id         | 50w+        | 文件资源元数据，存储图片、附件、头像地址        |
| 21   | product            | BIGINT 自增 | uk_product_id                   | 20 以内     | 商品基础表（金币套餐、会员等）                  |
| 22   | coin_product       | BIGINT 自增 | uk_coin_product_id              | 10 以内     | 金币商品扩展表，绑定金币数量、赠送权益          |
| 23   | coin_record        | VARCHAR(32) | idx_user_id,idx_biz_type        | 500w+       | 金币流水主表，所有金币增减记录，对账唯一可信源  |
| 24   | orders             | BIGINT 自增 | uk_order_id,uk_order_no         | 100w+       | 订单主表，所有付费订单统一入口                  |
| 25   | coin_purchase      | BIGINT 自增 | uk_purchase_id,idx_order_id     | 100w+       | 金币充值订单扩展表                              |
| 26   | payment_record     | VARCHAR(32) | uk_pay_no,idx_order_id          | 100w+       | 支付渠道回执记录，微信 / 支付宝回调流水         |
| 27   | refund_record      | VARCHAR(32) | uk_refund_no,idx_order_id       | 10w+        | 退款记录表，正向订单逆向退款、渠道回执、对账    |
| 28   | ai_prompt          | BIGINT 自增 | uk_prompt_code                  | 200 以内    | AI 提示词模板库                                 |
| 29   | ai_usage_record    | VARCHAR(32) | idx_user_id,idx_prompt_id       | 300w+       | AI 调用扣费流水，记录 token 消耗、扣减金币      |
| 30   | oj_problem         | VARCHAR(32) | uk_problem_no                   | 2000 以内   | OJ 编程题目主表                                 |
| 31   | oj_problem_tag     | BIGINT 自增 | idx_problem_id,idx_tag_id       | 1w 以内     | OJ 题目 - 标签关联表                            |
| 32   | oj_problem_version | VARCHAR(32) | idx_problem_id,idx_version_time | 5000 以内   | OJ 题目版本快照，题目变更可回滚                 |
| 33   | oj_language        | BIGINT 自增 | uk_lang_code                    | 20 以内     | OJ 支持编程语言配置（编译命令、资源限制）       |
| 34   | test_case          | VARCHAR(32) | idx_problem_id                  | 5w 以内     | OJ 题目测试用例表                               |
| 35   | submission         | BIGINT 自增 | uk_submission_id,idx_user_id    | 500w+       | 用户代码提交记录、判题结果                      |
| 36   | judge_server       | BIGINT 自增 | uk_server_code                  | 10 以内     | 分布式判题机节点管理表，负载与状态上报          |
| 37   | oj_discussion      | VARCHAR(32) | idx_problem_id,idx_user_id      | 10w+        | OJ 题目题解、讨论评论区                         |
| 38   | interview_problem  | VARCHAR(32) | uk_problem_no                   | 5000 以内   | 面试八股题主表                                  |
| 39   | interview_category | BIGINT 自增 | uk_category_code                | 50 以内     | 面试题一级 / 二级分类（Java、网络、MySQL）      |
| 40   | interview_tag      | BIGINT 自增 | uk_tag_name                     | 200 以内    | 面试题标签表                                    |
| 41   | interview_favorite | VARCHAR(32) | uk_user_problem,idx_user_id     | 100w+       | 面试题用户收藏记录                              |
| 42   | interview_like     | VARCHAR(32) | uk_user_problem,idx_user_id     | 80w+        | 面试题点赞记录                                  |
| 43   | interview_history  | VARCHAR(32) | idx_user_id,idx_problem_id      | 200w+       | 面试题浏览历史                                  |
| 44   | interview_comment  | VARCHAR(32) | idx_problem_id,idx_user_id      | 10w+        | 面试八股题评论讨论                              |
| 45   | exam               | BIGINT 自增 | uk_exam_code                    | 200 以内    | 模拟试卷主表，组卷、限时模考                    |
| 46   | exam_problem       | BIGINT 自增 | idx_exam_id,idx_problem_id      | 5000 以内   | 试卷 - 题目关联表，一套试卷包含哪些八股 / OJ 题 |
| 47   | algorithm          | BIGINT 自增 | uk_alg_code                     | 200 以内    | 算法专题基础配置表                              |
| 48   | data_structure     | BIGINT 自增 | uk_ds_code                      | 100 以内    | 数据结构专题配置表                              |
| 49   | user_achievement   | BIGINT 自增 | uk_achievement_code             | 50 以内     | 用户成就 / 勋章定义配置                         |
| 50   | user_badge         | VARCHAR(32) | uk_user_ach,idx_user_id         | 80w+        | 用户已获得勋章、成就记录                        |
| 51   | user_level         | BIGINT 自增 | uk_level_code                   | 20 以内     | 用户等级配置、经验阈值                          |
| 53   | coin_product       | BIGINT 自增 | uk_coin_product_id              | 10 以内     | 金币商品扩展表                                  |

# 完整数据库建表设计

本章节覆盖 AlgoVize 平台全部 53 张业务数据表，统一采用 InnoDB 引擎、utf8mb4 字符集，支持中文、emoji 存储。每张表依次输出：用途说明 → 完整建表 SQL → 字段详解 → 索引设计理由 → 扩展点。

------

## 7.1 用户账号域

### 1. user — Web 端用户表

用途：AlgoVize 平台 Web 端注册用户，OJ 提交、笔记购买等行为的归属主体。

```
DROP TABLE IF EXISTS `user`;
CREATE TABLE `user` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `username`      VARCHAR(50)  NOT NULL,
    `email`         VARCHAR(100) NOT NULL,
    `password`      VARCHAR(255) DEFAULT NULL,
    `age`           INT          DEFAULT NULL COMMENT '年龄',
    `gender`        VARCHAR(10)  DEFAULT '未知',
    `nickname`      VARCHAR(100) DEFAULT NULL,
    `avatar_url`    VARCHAR(500) DEFAULT NULL COMMENT '头像URL',
    `login_status`  VARCHAR(20)  DEFAULT 'offline' COMMENT '登录状态: online/offline',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:正常 0:封禁',
    `coins`         INT          NOT NULL DEFAULT 0 COMMENT '金币余额',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `last_login_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_username` (`username`),
    UNIQUE KEY `uk_user_email` (`email`),
    KEY `idx_user_nickname` (`nickname`),
    KEY `idx_user_status` (`status`),
    KEY `idx_user_login_status` (`login_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

字段扩展：

- `password VARCHAR(255)` — 预留 255 字符用于存储 BCrypt 哈希（原始 60 字符，未来切 SSO 凭证时也够用）
- `age INT` — 用户年龄，替代原 avatar 字段（头像改为 URL 方式存储）
- `avatar_url VARCHAR(500)` — 头像 URL，默认从 `https://i.pravatar.cc/150?u={id}` 获取占位头像
- `login_status VARCHAR(20)` — 登录状态（online /offline），用于后台用户列表在线状态展示
- `status TINYINT` — 账号状态（1 正常 / 0 封禁），管理员可通过后台一键封禁 / 解封用户
- `last_login_at` 每次登录由 LoginService 刷新，配合 login_log 实现登录分析

索引设计：

- `uk_username / uk_email` — 登录时按用户名或邮箱查询，必须唯一（账号体系基本要求）
- `idx_nickname` — 用于后台按昵称模糊搜索
- `idx_status` — 后台按账号状态（正常 / 封禁）筛选用户
- `idx_login_status` — 后台按登录状态（在线 / 离线）筛选用户

扩展点：

- 增加 phone /wechat_unionid 字段支持手机号 / 微信登录
- 增加 vip_level /vip_expire_at 对接会员体系（与 orders 联动）
- 拆出 user_profile 子表存储扩展资料（生日、地区等）

------

### 2. admin — 后台管理员表

用途：平台后台运营、审核、运维人员的账号主体，用于后台管理系统登录与权限校验。

```
DROP TABLE IF EXISTS `admin`;
CREATE TABLE `admin` (
    `id`            VARCHAR(32)  NOT NULL COMMENT '主键UUID',
    `username`      VARCHAR(50)  NOT NULL COMMENT '管理员用户名',
    `password`      VARCHAR(255) NOT NULL COMMENT '密码BCrypt哈希',
    `nickname`      VARCHAR(100) DEFAULT NULL COMMENT '管理员昵称',
    `avatar_url`    VARCHAR(500) DEFAULT NULL COMMENT '头像URL',
    `role_id`       BIGINT       DEFAULT NULL COMMENT '关联角色ID',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:正常 0:禁用',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `last_login_at` DATETIME     DEFAULT NULL COMMENT '最后登录时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_admin_username` (`username`),
    KEY `idx_admin_role_id` (`role_id`),
    KEY `idx_admin_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='后台管理员表';
```

字段扩展：

- `id VARCHAR(32)` — 采用压缩 UUID 作为主键，避免后台 ID 序号泄露管理员数量
- `role_id BIGINT` — 关联 RBAC 角色表，一个管理员绑定一个角色，简化权限管控
- `status TINYINT` — 账号状态，离职或权限回收时直接禁用账号，无需物理删除
- `last_login_at` — 记录管理员最后登录时间，配合操作日志做安全审计

索引设计：

- `uk_admin_username` — 后台登录唯一校验，保证用户名全局唯一
- `idx_admin_role_id` — 按角色筛选管理员，快速查询某角色下所有账号
- `idx_admin_status` — 后台按账号状态筛选，快速统计在职 / 禁用账号

扩展点：

- 增加 phone、email 字段支持手机号 / 邮箱登录与告警通知
- 增加 google_secret 字段支持谷歌二次验证（2FA），提升后台安全性
- 拆出 admin_profile 表存储扩展信息，如所属部门、工号、联系方式

------

### 3. user_session — 用户登录会话表

用途：存储 C 端用户登录态凭证，支持多设备登录管理、单点下线、会话过期控制。

```
DROP TABLE IF EXISTS `user_session`;
CREATE TABLE `user_session` (
    `id`            VARCHAR(32)  NOT NULL COMMENT '会话ID',
    `user_id`       BIGINT       NOT NULL COMMENT '关联用户ID',
    `token`         VARCHAR(255) NOT NULL COMMENT '登录凭证token',
    `device_type`   VARCHAR(20)  DEFAULT 'web' COMMENT '设备类型: web/app/h5',
    `device_info`   VARCHAR(500) DEFAULT NULL COMMENT '设备指纹/UA信息',
    `ip`            VARCHAR(50)  DEFAULT NULL COMMENT '登录IP',
    `expire_time`   DATETIME     NOT NULL COMMENT '过期时间',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_session_token` (`token`),
    KEY `idx_user_session_user_id` (`user_id`),
    KEY `idx_user_session_expire_time` (`expire_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户登录会话表';
```

字段扩展：

- `token VARCHAR(255)` — 存储登录态凭证，支持 JWT 或随机字符串两种方案
- `device_type / device_info` — 区分登录设备，用于多端登录管理与异常登录风控
- `expire_time` — 会话过期时间，定时任务定期清理过期会话，减少表数据量

索引设计：

- `uk_session_token` — 接口鉴权时按 token 快速查询会话有效性
- `idx_user_id` — 查询用户所有登录设备，实现单点下线、设备管理功能
- `idx_expire_time` — 定时清理过期会话时快速筛选，避免全表扫描

扩展点：

- 增加 refresh_token 字段支持双 token 机制，延长登录有效期
- 增加 login_location 字段记录登录地域，辅助异地登录风控
- 接入 Redis 做热点会话缓存，数据库作为持久化兜底

------

### 4. app_user — APP 端扩展用户表

用途：APP 端用户扩展信息表，存储移动端专属属性，与主用户表一对一关联。

```
DROP TABLE IF EXISTS `app_user`;
CREATE TABLE `app_user` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`    BIGINT       NOT NULL COMMENT '关联主用户ID',
    `app_uid`    VARCHAR(64)  NOT NULL COMMENT 'APP端唯一标识',
    `client_id`  VARCHAR(64)  DEFAULT NULL COMMENT '客户端ID/设备唯一标识',
    `push_token` VARCHAR(255) DEFAULT NULL COMMENT '推送令牌',
    `os_type`    VARCHAR(20)  DEFAULT NULL COMMENT '系统类型: Android/iOS',
    `os_version` VARCHAR(50)  DEFAULT NULL COMMENT '系统版本',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_app_uid` (`app_uid`),
    UNIQUE KEY `uk_app_user_id` (`user_id`),
    KEY `idx_app_client_id` (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='APP端扩展用户表';
```

字段扩展：

- `user_id` 与主 user 表一对一关联，保证用户体系统一，避免双账号体系
- `app_uid` — APP 端对外暴露的用户标识，避免暴露主表自增 ID
- `push_token` — 移动端消息推送凭证，厂商推送服务使用

索引设计：

- `uk_app_uid` — APP 端按业务标识查询用户
- `uk_user_id` — 关联主用户表查询，保证一对一关系
- `idx_client_id` — 按设备 ID 查询，用于未登录状态下的设备行为追踪

扩展点：

- 增加 vip 相关字段同步主用户会员权益
- 增加夜间模式、字体大小等 APP 个性化配置字段
- 拆出 user_device 表支持多设备绑定同一账号

------

### 5. user_third_account — 第三方登录账号表

用途：存储用户第三方平台授权绑定关系，支持微信、GitHub、手机号等快捷登录。

```
DROP TABLE IF EXISTS `user_third_account`;
CREATE TABLE `user_third_account` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`      BIGINT       NOT NULL COMMENT '关联主用户ID',
    `platform`     VARCHAR(30)  NOT NULL COMMENT '第三方平台: wechat/github/phone',
    `open_id`      VARCHAR(128) NOT NULL COMMENT '第三方平台唯一标识',
    `union_id`     VARCHAR(128) DEFAULT NULL COMMENT '多端统一标识(如微信unionid)',
    `nickname`     VARCHAR(100) DEFAULT NULL COMMENT '第三方昵称',
    `avatar_url`   VARCHAR(500) DEFAULT NULL COMMENT '第三方头像',
    `bind_time`    DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '绑定时间',
    `created_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_third_platform_openid` (`platform`,`open_id`),
    KEY `idx_third_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='第三方登录账号表';
```

字段扩展：

- `platform + open_id` 唯一标识一个第三方账号，支持多平台绑定同一主账号
- `union_id` — 适配微信等多端统一账号体系，实现公众号、小程序、APP 账号互通

索引设计：

- `uk_platform_openid` — 第三方登录时，根据平台 + openid 快速匹配本地用户
- `idx_user_id` — 查询用户绑定的所有第三方账号，支持解绑管理

扩展点：

- 增加 access_token、expire_time 字段存储第三方授权凭证
- 支持更多平台（QQ、微博、Apple ID）登录
- 增加绑定状态字段，支持解绑 / 冻结绑定关系

------

## 7.2 后台权限域（RBAC）

### 6. role — 角色表

用途：RBAC 权限体系核心，定义管理员角色，批量分配权限，简化权限管理。

```
DROP TABLE IF EXISTS `role`;
CREATE TABLE `role` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `role_code`   VARCHAR(50)  NOT NULL COMMENT '角色编码',
    `role_name`   VARCHAR(100) NOT NULL COMMENT '角色名称',
    `description` VARCHAR(255) DEFAULT NULL COMMENT '角色描述',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色表';
```

字段扩展：

- `role_code` — 角色唯一编码，代码中权限校验使用，避免依赖自增 ID
- `status` — 角色状态，禁用后所有绑定该角色的管理员自动失去对应权限

索引设计：

- `uk_role_code` — 全局唯一角色编码，代码层面硬编码校验时使用，稳定可靠

扩展点：

- 增加 data_scope 字段控制数据权限（全部 / 本部门 / 本人）
- 增加角色排序字段，后台列表按优先级展示
- 支持角色继承，实现多级角色权限复用

------

### 7. permission — 权限点表

用途：定义系统所有接口、菜单、按钮级权限点，是权限管控的最小单元。

```
DROP TABLE IF EXISTS `permission`;
CREATE TABLE `permission` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `perm_code`   VARCHAR(100) NOT NULL COMMENT '权限标识',
    `perm_name`   VARCHAR(100) NOT NULL COMMENT '权限名称',
    `type`        TINYINT      DEFAULT 1 COMMENT '1:菜单 2:接口 3:按钮',
    `parent_id`   BIGINT       DEFAULT 0 COMMENT '父级权限ID',
    `path`        VARCHAR(255) DEFAULT NULL COMMENT '前端路由/后端接口路径',
    `sort`        INT          DEFAULT 0 COMMENT '排序号',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_perm_code` (`perm_code`),
    KEY `idx_perm_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='权限点表';
```

字段扩展：

- `perm_code` — 权限唯一标识，如 `user:add`、`order:export`，接口拦截器校验使用
- `type` — 区分菜单、接口、按钮，适配前后端统一权限管控
- `parent_id` — 构建权限树形结构，支持多级菜单

索引设计：

- `uk_perm_code` — 权限标识全局唯一，代码层面校验时精准匹配
- `idx_parent_id` — 快速查询子权限，构建树形菜单

扩展点：

- 增加 icon、component 字段适配前端菜单渲染
- 增加权限分组字段，按业务模块管理权限
- 支持接口级别的限流、黑白名单配置

------

### 8. role_permission — 角色权限关联表

用途：角色与权限的多对多关联表，实现角色批量授权。

```
DROP TABLE IF EXISTS `role_permission`;
CREATE TABLE `role_permission` (
    `id`            BIGINT NOT NULL AUTO_INCREMENT,
    `role_id`       BIGINT NOT NULL COMMENT '角色ID',
    `permission_id` BIGINT NOT NULL COMMENT '权限ID',
    `created_at`    DATETIME DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_perm` (`role_id`,`permission_id`),
    KEY `idx_rp_role_id` (`role_id`),
    KEY `idx_rp_permission_id` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色权限关联表';
```

字段扩展：

- `role_id + permission_id` 联合唯一，避免重复授权
- 纯关联表，无多余业务字段，保证授权操作轻量高效

索引设计：

- `uk_role_perm` — 联合唯一索引，防止同一角色重复绑定同一权限
- `idx_role_id` — 查询角色拥有的所有权限，登录时加载权限列表
- `idx_permission_id` — 查询权限绑定的所有角色，权限变更时联动校验

扩展点：

- 增加授权人、授权时间字段，记录授权操作审计
- 支持半选 / 全选状态，适配树形权限授权交互

------

## 7.3 资产支付域

### 9. product — 商品基础表

用途：平台所有付费商品的基础抽象，包括金币套餐、会员、虚拟商品等统一入口。

```
DROP TABLE IF EXISTS `product`;
CREATE TABLE `product` (
    `id`             BIGINT        NOT NULL AUTO_INCREMENT,
    `product_id`     VARCHAR(64)   NOT NULL COMMENT '商品业务ID',
    `product_name`   VARCHAR(100)  NOT NULL COMMENT '商品名称',
    `product_type`   VARCHAR(30)   NOT NULL COMMENT '商品类型: coin/vip/goods',
    `price`          DECIMAL(10,2) NOT NULL COMMENT '售价(元)',
    `original_price` DECIMAL(10,2) DEFAULT NULL COMMENT '原价',
    `status`         TINYINT       DEFAULT 1 COMMENT '1:上架 0:下架',
    `sort`           INT           DEFAULT 0 COMMENT '排序号',
    `description`    VARCHAR(500)  DEFAULT NULL COMMENT '商品描述',
    `created_at`     DATETIME      DEFAULT CURRENT_TIMESTAMP,
    `updated_at`     DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_product_id` (`product_id`),
    KEY `idx_product_type` (`product_type`),
    KEY `idx_product_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='商品基础表';
```

字段扩展：

- `product_id` — 对外暴露的商品业务 ID，避免暴露自增主键
- `product_type` — 商品类型，后续扩展不同品类商品时统一入口
- `price / original_price` — 售价与原价，支持促销折扣展示

索引设计：

- `uk_product_id` — 商品业务 ID 唯一，下单、商品详情查询使用
- `idx_product_type` — 按商品类型筛选列表，如金币商城、会员专区
- `idx_product_status` — 前台只展示上架商品，快速过滤下架商品

扩展点：

- 增加库存字段支持实物 / 限量商品
- 增加标签字段，标记热销、新品、限时折扣
- 增加有效期字段，支持限时商品自动下架

------

### 10. coin_product — 金币商品扩展表

用途：金币充值专属商品扩展表，绑定金币数量、赠送比例等金币专属属性。

```
DROP TABLE IF EXISTS `coin_product`;
CREATE TABLE `coin_product` (
    `id`              BIGINT      NOT NULL AUTO_INCREMENT,
    `coin_product_id` VARCHAR(64) NOT NULL COMMENT '金币商品ID',
    `product_id`      VARCHAR(64) NOT NULL COMMENT '关联基础商品ID',
    `coin_amount`     INT         NOT NULL COMMENT '到账金币数量',
    `give_coin`       INT         DEFAULT 0 COMMENT '赠送金币数量',
    `is_first_charge` TINYINT     DEFAULT 0 COMMENT '是否首充专属: 1是 0否',
    `created_at`      DATETIME    DEFAULT CURRENT_TIMESTAMP,
    `updated_at`      DATETIME    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_coin_product_id` (`coin_product_id`),
    UNIQUE KEY `uk_coin_product_pid` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='金币商品扩展表';
```

字段扩展：

- `coin_amount + give_coin` — 实到金币 + 赠送金币，首充、活动可配置不同赠送比例
- `is_first_charge` — 首充专属标记，新用户首充优惠校验使用

索引设计：

- `uk_coin_product_id` — 金币商品业务 ID 唯一
- `uk_product_id` — 与基础商品表一对一关联，保证一个基础商品对应一个金币扩展

扩展点：

- 增加有效期字段，支持限时金币礼包
- 增加每日限购、每人限购字段
- 增加会员折扣比例配置

------

### 11. orders — 订单主表

用途：平台所有付费订单的统一入口，承载订单状态流转、金额、归属等核心信息。

```
DROP TABLE IF EXISTS `orders`;
CREATE TABLE `orders` (
    `id`           BIGINT        NOT NULL AUTO_INCREMENT,
    `order_id`     VARCHAR(64)   NOT NULL COMMENT '订单号',
    `order_no`     VARCHAR(64)   NOT NULL COMMENT '对外展示订单号',
    `user_id`      BIGINT        NOT NULL COMMENT '下单用户ID',
    `product_id`   VARCHAR(64)   NOT NULL COMMENT '关联商品ID',
    `product_name` VARCHAR(100)  NOT NULL COMMENT '商品名称快照',
    `order_type`   VARCHAR(30)   NOT NULL COMMENT '订单类型: coin/vip',
    `total_amount` DECIMAL(10,2) NOT NULL COMMENT '订单总金额',
    `pay_amount`   DECIMAL(10,2) NOT NULL COMMENT '实付金额',
    `status`       TINYINT       DEFAULT 0 COMMENT '0:待支付 1:已支付 2:已取消 3:已退款',
    `pay_time`     DATETIME      DEFAULT NULL COMMENT '支付时间',
    `expire_time`  DATETIME      DEFAULT NULL COMMENT '订单过期时间',
    `created_at`   DATETIME      DEFAULT CURRENT_TIMESTAMP,
    `updated_at`   DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_id` (`order_id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_orders_user_id` (`user_id`),
    KEY `idx_orders_status` (`status`),
    KEY `idx_orders_create_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='订单主表';
```

字段扩展：

- `order_id / order_no` — 内部主键订单号 + 对外展示订单号分离，兼顾性能与安全
- `product_name` — 商品名称快照，避免后续商品改名导致订单历史不一致
- `status` — 订单状态机，控制待支付→已支付 / 已取消→已退款的完整流转

索引设计：

- `uk_order_id / uk_order_no` — 双唯一键，内部流转用 order_id，对外交互用 order_no
- `idx_user_id` — 用户中心查询我的订单列表，高频查询
- `idx_status` — 按订单状态筛选，如待支付订单、已完成订单
- `idx_create_time` — 后台按时间范围统计订单、对账筛选

扩展点：

- 增加优惠券、抵扣金额字段，支持营销活动
- 增加收货人信息字段，支持实物商品发货
- 拆出 order_item 订单明细表，支持一单多商品场景

------

### 12. coin_purchase — 金币充值订单扩展表

用途：金币充值订单的扩展信息表，与订单主表一对一关联，记录金币到账明细。

```
DROP TABLE IF EXISTS `coin_purchase`;
CREATE TABLE `coin_purchase` (
    `id`              BIGINT      NOT NULL AUTO_INCREMENT,
    `purchase_id`     VARCHAR(64) NOT NULL COMMENT '充值记录ID',
    `order_id`        VARCHAR(64) NOT NULL COMMENT '关联订单号',
    `user_id`         BIGINT      NOT NULL COMMENT '用户ID',
    `coin_product_id` VARCHAR(64) NOT NULL COMMENT '关联金币商品ID',
    `total_coin`      INT         NOT NULL COMMENT '总到账金币(含赠送)',
    `base_coin`       INT         NOT NULL COMMENT '基础金币',
    `give_coin`       INT         DEFAULT 0 COMMENT '赠送金币',
    `arrive_status`   TINYINT     DEFAULT 0 COMMENT '到账状态: 0未到账 1已到账',
    `arrive_time`     DATETIME    DEFAULT NULL COMMENT '到账时间',
    `created_at`      DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_purchase_id` (`purchase_id`),
    UNIQUE KEY `uk_coin_purchase_order_id` (`order_id`),
    KEY `idx_coin_purchase_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='金币充值订单扩展表';
```

字段扩展：

- `total_coin = base_coin + give_coin`，清晰拆分本金与赠送，对账清晰
- `arrive_status` — 金币到账状态，支付回调后异步到账，支持失败重试

索引设计：

- `uk_purchase_id` — 充值记录业务主键
- `uk_order_id` — 与订单主表一对一关联，一个订单对应一条充值记录
- `idx_user_id` — 查询用户充值历史记录

扩展点：

- 增加渠道补贴、活动赠送字段，区分不同来源的赠送金币
- 增加到账失败重试次数字段，支持幂等重试机制

------

### 13. payment_record — 支付流水表

用途：第三方支付渠道的回调回执记录，是财务对账、支付校验的核心凭证。

```
DROP TABLE IF EXISTS `payment_record`;
CREATE TABLE `payment_record` (
    `id`          VARCHAR(32)   NOT NULL COMMENT '支付流水ID',
    `pay_no`      VARCHAR(64)   NOT NULL COMMENT '支付渠道流水号',
    `order_id`    VARCHAR(64)   NOT NULL COMMENT '关联业务订单号',
    `user_id`     BIGINT        NOT NULL COMMENT '支付用户ID',
    `pay_channel` VARCHAR(30)   NOT NULL COMMENT '支付渠道: wechat/alipay',
    `pay_amount`  DECIMAL(10,2) NOT NULL COMMENT '支付金额',
    `pay_status`  TINYINT       NOT NULL COMMENT '支付状态: 1成功 0失败',
    `pay_time`    DATETIME      DEFAULT NULL COMMENT '渠道支付时间',
    `callback_raw` TEXT         DEFAULT NULL COMMENT '渠道回调原始报文',
    `created_at`  DATETIME      DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_pay_no` (`pay_no`),
    KEY `idx_payment_order_id` (`order_id`),
    KEY `idx_payment_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='支付流水表';
```

字段扩展：

- `pay_no` — 第三方支付渠道返回的流水号，对账核心凭证
- `callback_raw` — 存储原始回调报文，出现对账差异时可回溯排查
- `pay_channel` — 区分支付渠道，支持多渠道对账统计

索引设计：

- `uk_pay_no` — 渠道流水号唯一，防止重复回调、重复记账
- `idx_order_id` — 根据订单号查询支付记录，订单状态流转校验
- `idx_user_id` — 查询用户支付历史

扩展点：

- 增加退款金额、退款流水关联字段
- 增加手续费字段，财务核算成本
- 增加对账标记字段，支持每日自动对账

------

### 14. refund_record — 退款记录表

用途：订单逆向退款的核心凭证，记录退款申请、渠道退款回执、退款状态流转。

```
DROP TABLE IF EXISTS `refund_record`;
CREATE TABLE `refund_record` (
    `id`                VARCHAR(32)   NOT NULL COMMENT '退款记录ID',
    `refund_no`         VARCHAR(64)   NOT NULL COMMENT '退款单号',
    `order_id`          VARCHAR(64)   NOT NULL COMMENT '关联原订单号',
    `user_id`           BIGINT        NOT NULL COMMENT '用户ID',
    `refund_amount`     DECIMAL(10,2) NOT NULL COMMENT '退款金额',
    `refund_reason`     VARCHAR(255)  DEFAULT NULL COMMENT '退款原因',
    `refund_status`     TINYINT       DEFAULT 0 COMMENT '0:待退款 1:退款成功 2:退款失败',
    `pay_channel`       VARCHAR(30)   NOT NULL COMMENT '退款渠道',
    `channel_refund_no` VARCHAR(64)   DEFAULT NULL COMMENT '渠道退款流水号',
    `refund_time`       DATETIME      DEFAULT NULL COMMENT '退款完成时间',
    `created_at`        DATETIME      DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_refund_no` (`refund_no`),
    KEY `idx_refund_order_id` (`order_id`),
    KEY `idx_refund_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='退款记录表';
```

字段扩展：

- `refund_no` — 业务退款单号，对外交互、渠道申请使用
- `refund_status` — 退款状态机，支持申请→成功 / 失败的流转，失败可重试
- `channel_refund_no` — 渠道返回的退款流水，对账凭证

索引设计：

- `uk_refund_no` — 退款单号全局唯一
- `idx_order_id` — 查询订单的退款记录，防止超额退款
- `idx_user_id` — 用户端查询退款进度

扩展点：

- 增加审核人、审核时间字段，支持人工审核退款
- 增加部分退款支持，一笔订单多次退款
- 增加退款到账账户信息

------

### 15. coin_record — 金币流水表

用途：用户金币所有增减变动的流水记录，是金币资产对账的唯一可信数据源。

```
DROP TABLE IF EXISTS `coin_record`;
CREATE TABLE `coin_record` (
    `id`            VARCHAR(32) NOT NULL COMMENT '流水ID',
    `user_id`       BIGINT      NOT NULL COMMENT '用户ID',
    `change_coins`  INT         NOT NULL COMMENT '变动数量(正为增加,负为扣减)',
    `after_balance` INT         NOT NULL COMMENT '变动后余额',
    `biz_type`      VARCHAR(30) NOT NULL COMMENT '业务类型: recharge/ai_buy/refund/compensate',
    `biz_id`        VARCHAR(64) DEFAULT NULL COMMENT '关联业务ID',
    `remark`        VARCHAR(255) DEFAULT NULL COMMENT '变动备注',
    `operator`      VARCHAR(50) DEFAULT NULL COMMENT '操作人(系统/管理员ID)',
    `created_at`    DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_coin_record_user_id` (`user_id`),
    KEY `idx_coin_record_biz_type` (`biz_type`),
    KEY `idx_coin_record_create_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='金币流水表';
```

字段扩展：

- `change_coins` — 正负值区分增减，统一字段，不用分收入支出两列
- `after_balance` — 每次变动后的余额快照，方便回溯任意时间点用户余额
- `biz_type + biz_id` — 关联业务场景，每一笔流水都可追溯到具体业务单据

索引设计：

- `idx_user_id` — 用户金币明细查询，高频场景（我的金币明细）
- `idx_biz_type` — 按业务类型统计收支，运营分析
- `idx_create_time` — 按时间范围对账、统计

扩展点：

- 增加流水号唯一索引，保证幂等性，防止重复记账
- 增加冻结金币字段，支持预扣减、解冻场景
- 按月份分表，千万级数据后提升查询性能

------

## 7.4 OJ 判题域

### 16. oj_problem — OJ 编程题目表

用途：在线编程题库主表，存储题目描述、输入输出规范、难度等核心信息。

```
DROP TABLE IF EXISTS `oj_problem`;
CREATE TABLE `oj_problem` (
    `id`            VARCHAR(32)  NOT NULL COMMENT '题目ID',
    `problem_no`    VARCHAR(30)  NOT NULL COMMENT '题目编号',
    `title`         VARCHAR(200) NOT NULL COMMENT '题目标题',
    `difficulty`    TINYINT      DEFAULT 1 COMMENT '难度: 1简单 2中等 3困难',
    `description`   TEXT         DEFAULT NULL COMMENT '题目描述',
    `input_desc`    TEXT         DEFAULT NULL COMMENT '输入说明',
    `output_desc`   TEXT         DEFAULT NULL COMMENT '输出说明',
    `sample_input`  TEXT         DEFAULT NULL COMMENT '样例输入',
    `sample_output` TEXT         DEFAULT NULL COMMENT '样例输出',
    `hint`          TEXT         DEFAULT NULL COMMENT '提示',
    `time_limit`    INT          DEFAULT 1000 COMMENT '时间限制(ms)',
    `memory_limit`  INT          DEFAULT 64 COMMENT '内存限制(MB)',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:公开 0:隐藏',
    `submit_count`  INT          DEFAULT 0 COMMENT '提交次数',
    `accept_count`  INT          DEFAULT 0 COMMENT '通过次数',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_problem_no` (`problem_no`),
    KEY `idx_oj_difficulty` (`difficulty`),
    KEY `idx_oj_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='OJ编程题目表';
```

字段扩展：

- `problem_no` — 对外展示的题目编号，如 T1001，用户按题号搜索
- `time_limit / memory_limit` — 判题机资源限制，不同语言、不同难度可配置
- `submit_count / accept_count` — 提交与通过数，实时计算通过率，不用实时统计

索引设计：

- `uk_problem_no` — 题号唯一，用户检索、题目跳转使用
- `idx_difficulty` — 按难度筛选题目，刷题练习场景
- `idx_status` — 前台只展示公开题目，隐藏题目仅后台可见

扩展点：

- 增加来源字段，标记题目来源（真题 / 原创 / 竞赛）
- 增加题目点赞、收藏数冗余字段
- 增加题目版本号，关联题目版本表

------

### 17. oj_problem_tag — OJ 题目标签关联表

用途：题目与标签的多对多关联，支持按算法标签分类筛选题目。

```
DROP TABLE IF EXISTS `oj_problem_tag`;
CREATE TABLE `oj_problem_tag` (
    `id`         BIGINT      NOT NULL AUTO_INCREMENT,
    `problem_id` VARCHAR(32) NOT NULL COMMENT '题目ID',
    `tag_id`     BIGINT      NOT NULL COMMENT '标签ID',
    `created_at` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_oj_problem_tag` (`problem_id`,`tag_id`),
    KEY `idx_oj_tag_id` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='OJ题目标签关联表';
```

字段扩展：

- `problem_id + tag_id` 联合唯一，避免重复打标签
- 纯关联表，轻量化设计，标签变更不影响题目主表

索引设计：

- `uk_problem_tag` — 联合唯一索引，防止重复绑定
- `idx_problem_id` — 查询单道题的所有标签
- `idx_tag_id` — 按标签查询对应题目列表，分类刷题核心场景

扩展点：

- 增加标签权重字段，标记主标签 / 副标签
- 增加打标人、打标时间字段，后台审计

------

### 18. oj_problem_version — OJ 题目版本表

用途：题目修改历史快照，题目内容、测试用例变更可追溯、可回滚。

```
DROP TABLE IF EXISTS `oj_problem_version`;
CREATE TABLE `oj_problem_version` (
    `id`               VARCHAR(32) NOT NULL COMMENT '版本ID',
    `problem_id`       VARCHAR(32) NOT NULL COMMENT '关联题目ID',
    `version_no`       VARCHAR(30) NOT NULL COMMENT '版本号',
    `title_snapshot`   VARCHAR(200) DEFAULT NULL COMMENT '题目标题快照',
    `content_snapshot` TEXT         DEFAULT NULL COMMENT '题目内容快照',
    `change_log`       VARCHAR(500) DEFAULT NULL COMMENT '变更说明',
    `operator`         VARCHAR(50)  DEFAULT NULL COMMENT '操作人',
    `created_at`       DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_oj_version_problem_id` (`problem_id`),
    KEY `idx_oj_version_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='OJ题目版本表';
```

字段扩展：

- `content_snapshot` — 题目完整内容快照，回滚时直接恢复
- `change_log` — 变更日志，记录修改点，方便后台查看历史

索引设计：

- `idx_problem_id` — 查询单道题的所有历史版本
- `idx_version_time` — 按时间排序版本，默认最新在前

扩展点：

- 增加测试用例版本快照，联动测试用例变更
- 增加版本状态，支持预发布、灰度题目

------

### 19. oj_language — 编程语言配置表

用途：OJ 支持的编程语言统一配置，定义编译命令、运行命令、资源限制。

```
DROP TABLE IF EXISTS `oj_language`;
CREATE TABLE `oj_language` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `lang_code`     VARCHAR(30)  NOT NULL COMMENT '语言编码',
    `lang_name`     VARCHAR(50)  NOT NULL COMMENT '语言名称',
    `compile_cmd`   VARCHAR(500) DEFAULT NULL COMMENT '编译命令',
    `run_cmd`       VARCHAR(500) DEFAULT NULL COMMENT '运行命令',
    `file_suffix`   VARCHAR(20)  NOT NULL COMMENT '代码文件后缀',
    `time_factor`   DECIMAL(3,2) DEFAULT 1.0 COMMENT '时间倍率',
    `memory_factor` DECIMAL(3,2) DEFAULT 1.0 COMMENT '内存倍率',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_lang_code` (`lang_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='编程语言配置表';
```

字段扩展：

- `lang_code` — 语言唯一编码，判题机调用时传递参数
- `time_factor / memory_factor` — 语言资源倍率，Java 等语言适当放宽限制
- `file_suffix` — 代码保存的文件后缀，判题机生成文件使用

索引设计：

- `uk_lang_code` — 语言编码唯一，判题服务加载配置、提交代码校验使用

扩展点：

- 增加判题机镜像地址，支持容器化判题环境
- 增加版本字段，支持同一语言多版本（如 Python3.9/Python3.11）
- 增加代码模板字段，提交页面默认填充模板

------

### 20. test_case — 测试用例表

用途：OJ 题目对应的判题测试用例，包含输入输出数据，是判题的核心依据。

```
DROP TABLE IF EXISTS `test_case`;
CREATE TABLE `test_case` (
    `id`          VARCHAR(32) NOT NULL COMMENT '用例ID',
    `problem_id`  VARCHAR(32) NOT NULL COMMENT '关联题目ID',
    `case_no`     INT         NOT NULL COMMENT '用例序号',
    `input_data`  TEXT        DEFAULT NULL COMMENT '输入数据',
    `output_data` TEXT        NOT NULL COMMENT '预期输出数据',
    `is_sample`   TINYINT     DEFAULT 0 COMMENT '是否样例用例: 1是 0否',
    `score`       INT         DEFAULT 10 COMMENT '用例分值',
    `created_at`  DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_test_case_problem_id` (`problem_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='测试用例表';
```

字段扩展：

- `case_no` — 用例序号，判题时按顺序执行
- `is_sample` — 标记样例用例，题目详情页展示给用户，其他用例隐藏
- `score` — 用例分值，支持按用例得分，部分通过得部分分

索引设计：

- `idx_problem_id` — 查询一道题的所有测试用例，判题时批量加载

扩展点：

- 大输入输出改为文件存储，数据库只存文件路径，避免表体积过大
- 增加用例类型字段（普通 / 边界 / 极端数据）
- 增加用例版本号，联动题目版本

------

### 21. submission — 代码提交记录表

用途：用户代码提交的主记录表，存储提交代码、判题结果、耗时内存等信息。

```
DROP TABLE IF EXISTS `submission`;
CREATE TABLE `submission` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `submission_id` VARCHAR(64)  NOT NULL COMMENT '提交ID',
    `user_id`       BIGINT       NOT NULL COMMENT '提交用户ID',
    `problem_id`    VARCHAR(32)  NOT NULL COMMENT '题目ID',
    `lang_code`     VARCHAR(30)  NOT NULL COMMENT '编程语言',
    `code`          TEXT         NOT NULL COMMENT '提交代码',
    `status`        VARCHAR(30)  DEFAULT 'pending' COMMENT '判题状态: pending/accept/wrong_answer',
    `run_time`      INT          DEFAULT NULL COMMENT '运行耗时(ms)',
    `run_memory`    INT          DEFAULT NULL COMMENT '运行内存(MB)',
    `error_msg`     TEXT         DEFAULT NULL COMMENT '错误信息/编译错误',
    `judge_server`  VARCHAR(50)  DEFAULT NULL COMMENT '判题节点',
    `judge_time`    DATETIME     DEFAULT NULL COMMENT '判题完成时间',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_submission_id` (`submission_id`),
    KEY `idx_submission_user_id` (`user_id`),
    KEY `idx_submission_problem_id` (`problem_id`),
    KEY `idx_submission_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='代码提交记录表';
```

字段扩展：

- `submission_id` — 业务提交 ID，判题服务异步回调使用
- `status` — 判题状态枚举，覆盖等待、编译错误、答案错误、超时、通过等全场景
- `run_time / run_memory` — 运行耗时与内存，排行榜、最优解统计使用

索引设计：

- `uk_submission_id` — 提交记录唯一标识，判题回调更新状态使用
- `idx_user_id` — 用户提交记录列表，我的提交页面高频查询
- `idx_problem_id` — 题目提交记录统计、查看题解
- `idx_status` — 按状态筛选，如查看所有通过的提交

扩展点：

- 增加代码长度字段，统计代码简洁度
- 增加分享开关，支持公开分享提交代码
- 按时间 / 用户分表，千万级数据后优化查询性能

------

### 22. judge_server — 判题机节点表

用途：分布式判题集群的节点管理，记录节点状态、负载、心跳，实现负载均衡。

```
DROP TABLE IF EXISTS `judge_server`;
CREATE TABLE `judge_server` (
    `id`              BIGINT       NOT NULL AUTO_INCREMENT,
    `server_code`     VARCHAR(50)  NOT NULL COMMENT '节点编号',
    `server_name`     VARCHAR(100) DEFAULT NULL COMMENT '节点名称',
    `ip`              VARCHAR(50)  DEFAULT NULL COMMENT '节点IP',
    `port`            INT          DEFAULT NULL COMMENT '服务端口',
    `status`          TINYINT      DEFAULT 1 COMMENT '1:在线 0:离线',
    `load_level`      INT          DEFAULT 0 COMMENT '负载等级(当前任务数)',
    `max_task`        INT          DEFAULT 10 COMMENT '最大并发任务数',
    `last_heartbeat`  DATETIME     DEFAULT NULL COMMENT '最后心跳时间',
    `created_at`      DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_judge_server_code` (`server_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='判题机节点表';
```

字段扩展：

- `load_level / max_task` — 当前负载与最大容量，调度器根据负载分配任务
- `last_heartbeat` — 心跳时间，超时未上报标记为离线，不再分配任务

索引设计：

- `uk_server_code` — 节点编号唯一，心跳上报、任务分配时标识节点

扩展点：

- 增加支持的语言列表，不同节点部署不同判题环境
- 增加区域、机房字段，实现就近调度
- 增加节点权重，高性能节点分配更多任务

------

### 23. oj_discussion — OJ 题解讨论区表

用途：每道题目下的题解、评论、讨论，构建刷题社区氛围。

```
DROP TABLE IF EXISTS `oj_discussion`;
CREATE TABLE `oj_discussion` (
    `id`         VARCHAR(32)  NOT NULL COMMENT '评论ID',
    `problem_id` VARCHAR(32)  NOT NULL COMMENT '关联题目ID',
    `user_id`    BIGINT       NOT NULL COMMENT '发布用户ID',
    `parent_id`  VARCHAR(32)  DEFAULT NULL COMMENT '父评论ID',
    `title`      VARCHAR(200) DEFAULT NULL COMMENT '标题(题解帖有标题)',
    `content`    TEXT         NOT NULL COMMENT '评论/题解内容',
    `like_count` INT          DEFAULT 0 COMMENT '点赞数',
    `is_top`     TINYINT      DEFAULT 0 COMMENT '是否置顶: 1是 0否',
    `status`     TINYINT      DEFAULT 1 COMMENT '1:正常 0:删除',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_discuss_problem_id` (`problem_id`),
    KEY `idx_discuss_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='OJ题解讨论区表';
```

字段扩展：

- `parent_id` — 支持二级评论，楼中楼回复
- `is_top` — 管理员可置顶优质题解，提升内容曝光
- `like_count` — 点赞数冗余，不用实时统计

索引设计：

- `idx_problem_id` — 题目下的讨论列表，高频场景
- `idx_user_id` — 用户发布的所有题解 / 评论

扩展点：

- 增加采纳功能，题主可采纳最优回答
- 增加举报、审核状态，社区内容风控
- 拆出评论点赞表，记录用户点赞行为，防止重复点赞

------

## 7.5 面试八股题库域

### 24. interview_problem — 面试八股题主表

用途：面试题、八股文题库主表，存储题目、答案、解析等核心内容。

```
DROP TABLE IF EXISTS `interview_problem`;
CREATE TABLE `interview_problem` (
    `id`              VARCHAR(32)  NOT NULL COMMENT '题目ID',
    `problem_no`      VARCHAR(30)  NOT NULL COMMENT '题目编号',
    `category_id`     BIGINT       DEFAULT NULL COMMENT '所属分类ID',
    `title`           VARCHAR(200) NOT NULL COMMENT '题目标题',
    `question`        TEXT         DEFAULT NULL COMMENT '题干',
    `answer`          TEXT         DEFAULT NULL COMMENT '参考答案',
    `analysis`        TEXT         DEFAULT NULL COMMENT '答案解析',
    `difficulty`      TINYINT      DEFAULT 1 COMMENT '难度: 1初级 2中级 3高级',
    `frequency`       TINYINT      DEFAULT 2 COMMENT '考频: 1低频 2中频 3高频',
    `status`          TINYINT      DEFAULT 1 COMMENT '1:公开 0:隐藏',
    `view_count`      INT          DEFAULT 0 COMMENT '浏览次数',
    `like_count`      INT          DEFAULT 0 COMMENT '点赞数',
    `favorite_count`  INT          DEFAULT 0 COMMENT '收藏数',
    `created_at`      DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_interview_problem_no` (`problem_no`),
    KEY `idx_interview_category` (`category_id`),
    KEY `idx_interview_difficulty` (`difficulty`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试八股题主表';
```

字段扩展：

- `problem_no` — 题目编号，支持按编号检索
- `frequency` — 面试考频，标记高频考点，用户优先刷高频题
- `view/like/favorite_count` — 统计数据冗余，避免实时关联统计

索引设计：

- `uk_problem_no` — 题目编号唯一，检索、跳转使用
- `idx_category_id` — 按分类刷题，核心学习场景
- `idx_difficulty` — 按难度筛选，分阶段学习

扩展点：

- 增加公司标签，标记哪些公司考过
- 增加题目来源字段（历年真题 / 原创 / 用户投稿）
- 增加音频 / 视频讲解字段，多媒体解析

------

### 25. interview_category — 面试题分类表

用途：面试题一级 / 二级分类，构建知识体系树，如 Java→集合→并发。

```
DROP TABLE IF EXISTS `interview_category`;
CREATE TABLE `interview_category` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `category_code` VARCHAR(50)  NOT NULL COMMENT '分类编码',
    `category_name` VARCHAR(100) NOT NULL COMMENT '分类名称',
    `parent_id`     BIGINT       DEFAULT 0 COMMENT '父分类ID',
    `sort`          INT          DEFAULT 0 COMMENT '排序号',
    `icon`          VARCHAR(255) DEFAULT NULL COMMENT '分类图标',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_category_code` (`category_code`),
    KEY `idx_category_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题分类表';
```

字段扩展：

- `parent_id` — 支持多级分类，构建知识体系树
- `sort` — 分类排序，前台按优先级展示

索引设计：

- `uk_category_code` — 分类编码唯一，稳定可靠
- `idx_parent_id` — 查询子分类，构建树形结构

扩展点：

- 增加分类描述、学习路径说明
- 增加题目数量冗余字段，展示分类下题目数
- 增加分类难度标签，适配不同学习阶段

------

### 26. interview_tag — 面试题标签表

用途：面试题多标签体系，补充分类维度，更细粒度筛选题目。

```
DROP TABLE IF EXISTS `interview_tag`;
CREATE TABLE `interview_tag` (
    `id`        BIGINT       NOT NULL AUTO_INCREMENT,
    `tag_name`  VARCHAR(50)  NOT NULL COMMENT '标签名称',
    `tag_color` VARCHAR(20)  DEFAULT NULL COMMENT '标签颜色',
    `sort`      INT          DEFAULT 0 COMMENT '排序号',
    `created_at` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_tag_name` (`tag_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题标签表';
```

字段扩展：

- `tag_name` — 标签名称全局唯一，避免重复标签
- `tag_color` — 前端展示标签颜色，提升视觉区分度

索引设计：

- `uk_tag_name` — 标签名唯一，防止重复创建

扩展点：

- 增加标签分组字段，按维度管理标签（公司 / 知识点 / 场景）
- 增加引用次数字段，展示热门标签

------

### 27. interview_favorite — 面试题收藏表

用途：用户收藏面试题，构建个人题库，方便后续复习。

```
DROP TABLE IF EXISTS `interview_favorite`;
CREATE TABLE `interview_favorite` (
    `id`         VARCHAR(32) NOT NULL COMMENT '收藏ID',
    `user_id`    BIGINT      NOT NULL COMMENT '用户ID',
    `problem_id` VARCHAR(32) NOT NULL COMMENT '题目ID',
    `created_at` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_problem` (`user_id`,`problem_id`),
    KEY `idx_favorite_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题收藏表';
```

字段扩展：

- `user_id + problem_id` 联合唯一，一个用户不能重复收藏同一道题

索引设计：

- `uk_user_problem` — 联合唯一索引，防止重复收藏，取消收藏时精准定位
- `idx_user_id` — 查询用户收藏列表，我的收藏页面

扩展点：

- 增加收藏分组，用户可创建收藏夹分类管理
- 增加收藏备注字段，记录个人笔记

------

### 28. interview_like — 面试题点赞表

用途：用户对面试题的点赞记录，防止重复点赞，统计点赞数。

```
DROP TABLE IF EXISTS `interview_like`;
CREATE TABLE `interview_like` (
    `id`         VARCHAR(32) NOT NULL COMMENT '点赞ID',
    `user_id`    BIGINT      NOT NULL COMMENT '用户ID',
    `problem_id` VARCHAR(32) NOT NULL COMMENT '题目ID',
    `created_at` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_problem` (`user_id`,`problem_id`),
    KEY `idx_like_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题点赞表';
```

字段扩展：

- `user_id + problem_id` 联合唯一，实现一人一题一赞，取消点赞幂等

索引设计：

- `uk_user_problem` — 联合唯一，防止重复点赞
- `idx_user_id` — 查询用户点赞过的题目

扩展点：

- 增加点赞状态字段，支持点赞 / 取消点赞状态变更
- 扩展支持评论点赞，复用点赞体系

------

### 29. interview_history — 面试题浏览历史表

用途：记录用户浏览过的面试题，支持历史回顾、继续学习。

```
DROP TABLE IF EXISTS `interview_history`;
CREATE TABLE `interview_history` (
    `id`         VARCHAR(32) NOT NULL COMMENT '历史记录ID',
    `user_id`    BIGINT      NOT NULL COMMENT '用户ID',
    `problem_id` VARCHAR(32) NOT NULL COMMENT '题目ID',
    `view_time`  DATETIME    DEFAULT CURRENT_TIMESTAMP COMMENT '浏览时间',
    PRIMARY KEY (`id`),
    KEY `idx_history_user_id` (`user_id`),
    KEY `idx_history_problem_id` (`problem_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题浏览历史表';
```

字段扩展：

- `view_time` — 最近浏览时间，重复浏览更新时间，保留最新记录

索引设计：

- `idx_user_id` — 查询用户浏览历史，按时间倒序展示
- `idx_problem_id` — 统计题目浏览量，更新主表 view_count

扩展点：

- 增加学习进度字段，标记已掌握 / 学习中 / 未学习
- 按用户分表，大数据量优化
- 增加停留时长字段，分析用户学习行为

------

### 30. interview_comment — 面试题评论表

用途：面试题下的用户评论、讨论、答疑，构建学习社区。

```
DROP TABLE IF EXISTS `interview_comment`;
CREATE TABLE `interview_comment` (
    `id`         VARCHAR(32)  NOT NULL COMMENT '评论ID',
    `problem_id` VARCHAR(32)  NOT NULL COMMENT '关联题目ID',
    `user_id`    BIGINT       NOT NULL COMMENT '评论用户ID',
    `parent_id`  VARCHAR(32)  DEFAULT NULL COMMENT '父评论ID',
    `content`    TEXT         NOT NULL COMMENT '评论内容',
    `like_count` INT          DEFAULT 0 COMMENT '点赞数',
    `status`     TINYINT      DEFAULT 1 COMMENT '1:正常 0:删除',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_interview_comment_problem` (`problem_id`),
    KEY `idx_interview_comment_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='面试题评论表';
```

字段扩展：

- `parent_id` — 支持二级回复，楼中楼评论
- `status` — 软删除，违规评论后台隐藏，不物理删除

索引设计：

- `idx_problem_id` — 题目下的评论列表，高频查询
- `idx_user_id` — 用户发布的所有评论

扩展点：

- 增加举报、审核状态，社区内容风控
- 增加官方回复标记，管理员回复高亮
- 拆出评论点赞表，防止重复点赞

------

## 7.6 AI 能力域

### 31. ai_prompt — AI 提示词模板表

用途：平台 AI 功能的提示词模板库，统一管理不同场景的 Prompt，支持版本迭代。

```
DROP TABLE IF EXISTS `ai_prompt`;
CREATE TABLE `ai_prompt` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `prompt_code`   VARCHAR(50)  NOT NULL COMMENT '提示词编码',
    `prompt_name`   VARCHAR(100) NOT NULL COMMENT '提示词名称',
    `scene`         VARCHAR(30)  NOT NULL COMMENT '使用场景: code_explain/interview_answer',
    `model`         VARCHAR(50)  DEFAULT NULL COMMENT '适配大模型',
    `system_prompt` TEXT         NOT NULL COMMENT '系统提示词',
    `user_template` TEXT         NOT NULL COMMENT '用户提示词模板',
    `temperature`   DECIMAL(3,2) DEFAULT 0.7 COMMENT '模型温度',
    `max_tokens`    INT          DEFAULT 2000 COMMENT '最大输出token',
    `status`        TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `version`       VARCHAR(30)  DEFAULT 'v1.0' COMMENT '版本号',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_prompt_code` (`prompt_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI提示词模板表';
```

字段扩展：

- `prompt_code` — 提示词唯一编码，业务代码调用时指定，不依赖 ID
- `system_prompt / user_template` — 系统提示词 + 用户模板，变量占位符运行时替换
- `temperature / max_tokens` — 模型参数，不同场景配置不同效果

索引设计：

- `uk_prompt_code` — 提示词编码唯一，业务调用时精准匹配

扩展点：

- 增加提示词分类、标签，按场景管理
- 增加效果评分、示例输出，辅助选择模板
- 增加灰度配置，不同用户使用不同版本提示词

------

### 32. ai_usage_record — AI 调用流水表

用途：用户 AI 功能调用的扣费、token 消耗流水，是 AI 业务对账与计费的核心。

```
DROP TABLE IF EXISTS `ai_usage_record`;
CREATE TABLE `ai_usage_record` (
    `id`            VARCHAR(32) NOT NULL COMMENT '调用记录ID',
    `user_id`       BIGINT      NOT NULL COMMENT '用户ID',
    `prompt_id`     BIGINT      NOT NULL COMMENT '使用的提示词模板ID',
    `model`         VARCHAR(50) NOT NULL COMMENT '调用模型',
    `input_tokens`  INT         DEFAULT 0 COMMENT '输入token数',
    `output_tokens` INT         DEFAULT 0 COMMENT '输出token数',
    `cost_coins`    INT         NOT NULL COMMENT '消耗金币数',
    `request_id`    VARCHAR(64) DEFAULT NULL COMMENT '模型侧请求ID',
    `status`        TINYINT     DEFAULT 1 COMMENT '1:成功 0:失败',
    `error_msg`     VARCHAR(255) DEFAULT NULL COMMENT '失败原因',
    `created_at`    DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_ai_usage_user_id` (`user_id`),
    KEY `idx_ai_usage_prompt_id` (`prompt_id`),
    KEY `idx_ai_usage_create_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI调用流水表';
```

字段扩展：

- `input_tokens + output_tokens` — 真实 token 消耗，用于成本核算
- `cost_coins` — 本次调用扣减金币数，关联金币流水
- `request_id` — 模型侧请求 ID，出现问题可溯源排查

索引设计：

- `idx_user_id` — 用户 AI 调用记录查询，我的 AI 记录页面
- `idx_prompt_id` — 按提示词统计调用量，分析模板使用频率
- `idx_create_time` — 按时间范围统计调用量、消耗成本

扩展点：

- 增加用户输入、AI 输出快照，用于内容审核
- 增加调用耗时字段，分析模型响应性能
- 按月份分表，大数据量优化查询

------

## 7.7 运营与成长体系域

### 33. exam — 模拟试卷表

用途：面试模拟卷、专项练习卷，支持组卷、限时模考、自动判分。

```
DROP TABLE IF EXISTS `exam`;
CREATE TABLE `exam` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `exam_code`   VARCHAR(50)  NOT NULL COMMENT '试卷编码',
    `exam_name`   VARCHAR(200) NOT NULL COMMENT '试卷名称',
    `exam_type`   VARCHAR(30)  NOT NULL COMMENT '试卷类型: mock/special',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '试卷说明',
    `total_score` INT          DEFAULT 100 COMMENT '总分',
    `time_limit`  INT          DEFAULT 60 COMMENT '考试时长(分钟)',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:上架 0:下架',
    `start_time`  DATETIME     DEFAULT NULL COMMENT '开放开始时间',
    `end_time`    DATETIME     DEFAULT NULL COMMENT '开放结束时间',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_exam_code` (`exam_code`),
    KEY `idx_exam_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='模拟试卷表';
```

字段扩展：

- `exam_type` — 区分模拟卷、专项练习卷，适配不同学习场景
- `time_limit` — 考试时长，模考场景倒计时
- `start/end_time` — 限时开放考试，如月度模考

索引设计：

- `uk_exam_code` — 试卷编码唯一，稳定标识
- `idx_status` — 前台只展示上架试卷

扩展点：

- 增加难度标签、适用人群
- 增加考试次数限制，每人限考次数
- 增加排行榜开关，支持模考排名

------

### 34. exam_problem — 试卷题目关联表

用途：试卷与题目的多对多关联，定义每套试卷包含的题目、分值、排序。

```
DROP TABLE IF EXISTS `exam_problem`;
CREATE TABLE `exam_problem` (
    `id`          BIGINT      NOT NULL AUTO_INCREMENT,
    `exam_id`     BIGINT      NOT NULL COMMENT '试卷ID',
    `problem_id`  VARCHAR(32) NOT NULL COMMENT '题目ID',
    `problem_type` VARCHAR(30) DEFAULT 'interview' COMMENT '题目类型: interview/oj',
    `score`       INT         DEFAULT 10 COMMENT '题目分值',
    `sort`        INT         DEFAULT 0 COMMENT '题目排序',
    `created_at`  DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_exam_problem_exam_id` (`exam_id`),
    KEY `idx_exam_problem_problem_id` (`problem_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='试卷题目关联表';
```

字段扩展：

- `problem_type` — 支持八股题和 OJ 编程题混合组卷
- `score / sort` — 每题分值与排序，灵活组卷

索引设计：

- `idx_exam_id` — 查询试卷包含的所有题目，按排序展示
- `idx_problem_id` — 查询题目所属的试卷

扩展点：

- 增加题目必选 / 选做标记
- 增加题目难度分组，支持随机抽题组卷

------

### 35. algorithm — 算法专题表

用途：算法专题学习模块，聚合同类算法题目，体系化学习。

```
DROP TABLE IF EXISTS `algorithm`;
CREATE TABLE `algorithm` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `alg_code`    VARCHAR(50)  NOT NULL COMMENT '专题编码',
    `alg_name`    VARCHAR(100) NOT NULL COMMENT '专题名称',
    `description` TEXT         DEFAULT NULL COMMENT '专题介绍',
    `difficulty`  TINYINT      DEFAULT 2 COMMENT '难度',
    `sort`        INT          DEFAULT 0 COMMENT '排序号',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_alg_code` (`alg_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='算法专题表';
```

字段扩展：

- `alg_code` — 专题唯一编码，稳定标识
- `description` — 专题知识讲解、学习路线说明

索引设计：

- `uk_alg_code` — 专题编码全局唯一

扩展点：

- 增加前置专题依赖，构建学习路径
- 增加专题封面图、标签
- 增加题目数量、完成人数冗余统计

------

### 36. data_structure — 数据结构专题表

用途：数据结构专项学习模块，与算法专题配合，覆盖基础数据结构知识。

```
DROP TABLE IF EXISTS `data_structure`;
CREATE TABLE `data_structure` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `ds_code`     VARCHAR(50)  NOT NULL COMMENT '专题编码',
    `ds_name`     VARCHAR(100) NOT NULL COMMENT '数据结构名称',
    `description` TEXT         DEFAULT NULL COMMENT '知识点讲解',
    `sort`        INT          DEFAULT 0 COMMENT '排序号',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_ds_code` (`ds_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据结构专题表';
```

字段扩展：

- `ds_code` — 数据结构编码，如 array、linked_list、tree
- `description` — 原理讲解、复杂度分析、常见题型

索引设计：

- `uk_ds_code` — 编码唯一，稳定调用

扩展点：

- 增加动画演示、图解资源链接
- 增加关联题目列表，学练结合
- 增加难度分级，从入门到进阶

------

### 37. user_achievement — 成就勋章定义表

用途：用户成长体系的勋章、成就定义，激励用户刷题、学习。

```
DROP TABLE IF EXISTS `user_achievement`;
CREATE TABLE `user_achievement` (
    `id`               BIGINT       NOT NULL AUTO_INCREMENT,
    `achievement_code` VARCHAR(50)  NOT NULL COMMENT '成就编码',
    `achievement_name` VARCHAR(100) NOT NULL COMMENT '成就名称',
    `description`      VARCHAR(255) DEFAULT NULL COMMENT '成就描述',
    `icon`             VARCHAR(255) DEFAULT NULL COMMENT '成就图标',
    `condition_type`   VARCHAR(30)  NOT NULL COMMENT '达成条件类型',
    `condition_value`  INT          NOT NULL COMMENT '达成条件数值',
    `reward_coins`     INT          DEFAULT 0 COMMENT '奖励金币',
    `sort`             INT          DEFAULT 0 COMMENT '排序号',
    `created_at`       DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_achievement_code` (`achievement_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='成就勋章定义表';
```

字段扩展：

- `condition_type + condition_value` — 成就达成条件，如刷题 100 道、连续打卡 7 天
- `reward_coins` — 达成成就奖励金币，激励用户

索引设计：

- `uk_achievement_code` — 成就编码唯一，发放成就时校验

扩展点：

- 增加成就等级（铜 / 银 / 金），同一成就多阶段
- 增加稀有度标签，提升收集乐趣
- 增加成就分类，按学习 / 社交 / 付费分类

------

### 38. user_badge — 用户获得勋章记录表

用途：用户已获得的成就勋章记录，记录获得时间、达成情况。

```
DROP TABLE IF EXISTS `user_badge`;
CREATE TABLE `user_badge` (
    `id`             VARCHAR(32) NOT NULL COMMENT '记录ID',
    `user_id`        BIGINT      NOT NULL COMMENT '用户ID',
    `achievement_id` BIGINT      NOT NULL COMMENT '成就ID',
    `obtain_time`    DATETIME    DEFAULT CURRENT_TIMESTAMP COMMENT '获得时间',
    `is_new`         TINYINT     DEFAULT 1 COMMENT '是否未读: 1是 0否',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_ach` (`user_id`,`achievement_id`),
    KEY `idx_user_badge_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户勋章记录表';
```

字段扩展：

- `user_id + achievement_id` 联合唯一，一个成就只能获得一次
- `is_new` — 新获得成就标记，用户中心红点提示

索引设计：

- `uk_user_ach` — 联合唯一，防止重复发放成就
- `idx_user_id` — 查询用户所有获得的勋章

扩展点：

- 增加成就佩戴状态，用户主页展示精选勋章
- 增加成就进度字段，显示未完成成就的进度

------

### 39. user_level — 用户等级配置表

用途：用户等级体系定义，配置等级阈值、特权、称号。

```
DROP TABLE IF EXISTS `user_level`;
CREATE TABLE `user_level` (
    `id`        BIGINT       NOT NULL AUTO_INCREMENT,
    `level_code` VARCHAR(30)  NOT NULL COMMENT '等级编码',
    `level_name` VARCHAR(50)  NOT NULL COMMENT '等级名称',
    `level`     INT          NOT NULL COMMENT '等级数字',
    `exp_min`   BIGINT       NOT NULL COMMENT '最低经验值',
    `privilege` VARCHAR(500) DEFAULT NULL COMMENT '等级特权说明',
    `created_at` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_level_code` (`level_code`),
    UNIQUE KEY `uk_level_num` (`level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户等级配置表';
```

字段扩展：

- `level` — 等级数字，从 1 开始递增
- `exp_min` — 升级所需最低经验值，根据经验匹配等级
- `privilege` — 等级特权描述，如每日免费 AI 次数、专属称号

索引设计：

- `uk_level_code` — 等级编码唯一
- `uk_level` — 等级数字唯一，经验计算时快速匹配等级

扩展点：

- 增加等级图标、称号
- 增加每日奖励配置，等级越高每日奖励越多
- 增加专属功能权限，高等级解锁高级功能

------

### 40. announcement — 平台公告表

用途：平台官方公告、活动通知、更新说明，全用户触达。

```
DROP TABLE IF EXISTS `announcement`;
CREATE TABLE `announcement` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT,
    `title`        VARCHAR(200) NOT NULL COMMENT '公告标题',
    `content`      TEXT         NOT NULL COMMENT '公告内容',
    `type`         VARCHAR(30)  DEFAULT 'notice' COMMENT '公告类型: notice/activity/update',
    `is_top`       TINYINT      DEFAULT 0 COMMENT '是否置顶: 1是 0否',
    `publish_time` DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
    `status`       TINYINT      DEFAULT 1 COMMENT '1:发布 0:草稿',
    `created_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_announce_publish_time` (`publish_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='平台公告表';
```

字段扩展：

- `type` — 区分公告、活动、更新，分类展示
- `is_top` — 置顶公告优先展示，重要通知高曝光
- `publish_time` — 发布时间，按时间倒序展示

索引设计：

- `idx_publish_time` — 公告列表按发布时间倒序，高频排序

扩展点：

- 增加公告目标人群，定向推送
- 增加公告跳转链接，引导到活动页
- 增加公告封面图，提升视觉效果

------

### 41. announcement_read — 公告已读记录表

用途：记录用户是否已读公告，实现未读红点、已读状态同步。

```
DROP TABLE IF EXISTS `announcement_read`;
CREATE TABLE `announcement_read` (
    `id`              VARCHAR(32) NOT NULL COMMENT '记录ID',
    `announcement_id` BIGINT      NOT NULL COMMENT '公告ID',
    `user_id`         BIGINT      NOT NULL COMMENT '用户ID',
    `read_time`       DATETIME    DEFAULT CURRENT_TIMESTAMP COMMENT '阅读时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_ann_user` (`announcement_id`,`user_id`),
    KEY `idx_ann_read_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='公告已读记录表';
```

字段扩展：

- `announcement_id + user_id` 联合唯一，记录用户是否已读

索引设计：

- `uk_ann_user` — 联合唯一，精准判断单条公告用户是否已读
- `idx_user_id` — 查询用户所有已读公告，计算未读数量

扩展点：

- 增加全部已读标记，用户一键全部已读
- 按用户分表，大数据量优化

------

### 42. feedback — 用户意见反馈表

用途：收集用户问题反馈、功能建议、Bug 上报，辅助产品迭代。

```
DROP TABLE IF EXISTS `feedback`;
CREATE TABLE `feedback` (
    `id`         VARCHAR(32)  NOT NULL COMMENT '反馈ID',
    `user_id`    BIGINT       NOT NULL COMMENT '反馈用户ID',
    `type`       VARCHAR(30)  NOT NULL COMMENT '反馈类型: bug/suggest/other',
    `title`      VARCHAR(200) NOT NULL COMMENT '反馈标题',
    `content`    TEXT         NOT NULL COMMENT '反馈内容',
    `contact`    VARCHAR(100) DEFAULT NULL COMMENT '联系方式',
    `images`     VARCHAR(1000) DEFAULT NULL COMMENT '截图URL,逗号分隔',
    `status`     TINYINT      DEFAULT 0 COMMENT '0:待处理 1:处理中 2:已处理',
    `reply`      TEXT         DEFAULT NULL COMMENT '官方回复',
    `reply_time` DATETIME     DEFAULT NULL COMMENT '回复时间',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_feedback_user_id` (`user_id`),
    KEY `idx_feedback_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户意见反馈表';
```

字段扩展：

- `type` — 反馈分类，后台按类型分配处理人
- `status` — 处理状态流转，用户可查看反馈进度
- `reply` — 官方回复，形成闭环

索引设计：

- `idx_user_id` — 用户查询自己的反馈记录
- `idx_status` — 后台按状态筛选待处理反馈

扩展点：

- 增加反馈优先级、紧急程度
- 增加反馈标签，按模块分类
- 增加反馈评分，用户对处理结果评价

------

### 43. file_storage — 文件资源表

用途：平台所有上传文件的元数据管理，包括头像、图片、附件等。

```
DROP TABLE IF EXISTS `file_storage`;
CREATE TABLE `file_storage` (
    `id`           VARCHAR(32)  NOT NULL COMMENT '文件ID',
    `file_key`     VARCHAR(255) NOT NULL COMMENT '文件唯一标识/存储路径',
    `file_name`    VARCHAR(255) DEFAULT NULL COMMENT '原始文件名',
    `file_type`    VARCHAR(50)  DEFAULT NULL COMMENT '文件类型: image/doc/video',
    `file_size`    BIGINT       DEFAULT NULL COMMENT '文件大小(字节)',
    `user_id`      BIGINT       DEFAULT NULL COMMENT '上传用户ID',
    `storage_type` VARCHAR(30)  DEFAULT 'local' COMMENT '存储类型: local/oss',
    `url`          VARCHAR(500) NOT NULL COMMENT '访问URL',
    `created_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_file_key` (`file_key`),
    KEY `idx_file_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='文件资源表';
```

字段扩展：

- `file_key` — 文件在存储系统中的唯一路径 /key，用于读写文件
- `storage_type` — 区分本地存储、对象存储，支持多存储方案
- `file_size` — 文件大小，统计存储容量

索引设计：

- `uk_file_key` — 文件 key 唯一，访问文件时精准定位
- `idx_user_id` — 查询用户上传的所有文件

扩展点：

- 增加文件过期时间，临时文件自动清理
- 增加文件 MD5，实现秒传、去重
- 增加文件审核状态，违规内容自动拦截

------

### 44. user_message — 用户站内信表

用途：用户系统通知、私信消息，区别于全站公告，是用户维度的消息。

```
DROP TABLE IF EXISTS `user_message`;
CREATE TABLE `user_message` (
    `id`         VARCHAR(32)  NOT NULL COMMENT '消息ID',
    `user_id`    BIGINT       NOT NULL COMMENT '接收用户ID',
    `title`      VARCHAR(200) NOT NULL COMMENT '消息标题',
    `content`    VARCHAR(500) NOT NULL COMMENT '消息内容',
    `type`       VARCHAR(30)  DEFAULT 'system' COMMENT '消息类型: system/order/activity',
    `is_read`    TINYINT      DEFAULT 0 COMMENT '是否已读: 1是 0否',
    `read_time`  DATETIME     DEFAULT NULL COMMENT '阅读时间',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_message_user_id` (`user_id`),
    KEY `idx_message_read` (`is_read`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户站内信表';
```

字段扩展：

- `type` — 消息分类，用户可按分类查看
- `is_read` — 已读状态，未读红点提示

索引设计：

- `idx_user_id` — 用户消息列表，高频查询
- `idx_is_read` — 统计用户未读消息数，红点提示

扩展点：

- 增加消息跳转链接，点击直达业务页面
- 增加消息推送开关，用户可关闭某类通知
- 按用户分表，大数据量优化

------

## 7.8 日志审计域

### 45. login_log — 用户登录日志表

用途：记录用户所有登录行为，用于安全审计、异常登录风控、登录分析。

```
DROP TABLE IF EXISTS `login_log`;
CREATE TABLE `login_log` (
    `id`          VARCHAR(32)  NOT NULL COMMENT '日志ID',
    `user_id`     BIGINT       DEFAULT NULL COMMENT '用户ID',
    `username`    VARCHAR(50)  DEFAULT NULL COMMENT '登录用户名',
    `login_type`  VARCHAR(30)  DEFAULT 'password' COMMENT '登录方式: password/wechat/sms',
    `ip`          VARCHAR(50)  DEFAULT NULL COMMENT '登录IP',
    `location`    VARCHAR(100) DEFAULT NULL COMMENT '登录地域',
    `device_info` VARCHAR(500) DEFAULT NULL COMMENT '设备/UA信息',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:成功 0:失败',
    `fail_reason` VARCHAR(100) DEFAULT NULL COMMENT '失败原因',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_login_log_user_id` (`user_id`),
    KEY `idx_login_log_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户登录日志表';
```

字段扩展：

- `login_type` — 区分登录方式，统计各渠道登录占比
- `status + fail_reason` — 登录结果与失败原因，分析登录失败原因
- `ip / location` — 登录 IP 与地域，异地登录风控使用

索引设计：

- `idx_user_id` — 查询用户登录历史，安全审计
- `idx_create_time` — 按时间范围筛选日志，统计登录数据

扩展点：

- 增加风险等级标记，异常登录高亮
- 接入风控系统，自动拦截高危登录
- 按时间分表，日志数据量大后冷热分离

------

### 46. api_log — 接口访问日志表

用途：记录所有 API 请求信息，用于性能监控、问题排查、接口统计。

```
DROP TABLE IF EXISTS `api_log`;
CREATE TABLE `api_log` (
    `id`             VARCHAR(32)  NOT NULL COMMENT '日志ID',
    `user_id`        BIGINT       DEFAULT NULL COMMENT '操作用户ID',
    `request_uri`    VARCHAR(255) NOT NULL COMMENT '请求接口',
    `request_method` VARCHAR(10)  NOT NULL COMMENT '请求方法: GET/POST',
    `ip`             VARCHAR(50)  DEFAULT NULL COMMENT '请求IP',
    `params`         TEXT         DEFAULT NULL COMMENT '请求参数摘要',
    `result_code`    INT          DEFAULT NULL COMMENT '响应状态码',
    `cost_time`      INT          DEFAULT NULL COMMENT '耗时(ms)',
    `error_msg`      VARCHAR(500) DEFAULT NULL COMMENT '错误信息',
    `created_at`     DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_api_log_user_id` (`user_id`),
    KEY `idx_api_log_req_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='接口访问日志表';
```

字段扩展：

- `cost_time` — 接口耗时，性能监控核心指标
- `result_code / error_msg` — 响应状态与错误，问题排查回溯
- `params` — 请求参数摘要，不记录敏感信息，兼顾排查与安全

索引设计：

- `idx_user_id` — 查询用户操作轨迹
- `idx_req_time` — 按时间范围查询日志，故障定位

扩展点：

- 增加接口分类、模块字段，按维度统计性能
- 大日志量迁移到 ES/ClickHouse，MySQL 只存近 7 天数据
- 增加 trace_id，全链路追踪

------

### 47. operation_log — 后台操作日志表

用途：记录后台管理员所有敏感操作，审计追溯，防止违规操作。

```
DROP TABLE IF EXISTS `operation_log`;
CREATE TABLE `operation_log` (
    `id`          VARCHAR(32)  NOT NULL COMMENT '日志ID',
    `admin_id`    VARCHAR(32)  NOT NULL COMMENT '操作管理员ID',
    `admin_name`  VARCHAR(50)  DEFAULT NULL COMMENT '管理员名称',
    `module`      VARCHAR(50)  NOT NULL COMMENT '操作模块',
    `operation`   VARCHAR(100) NOT NULL COMMENT '操作描述',
    `ip`          VARCHAR(50)  DEFAULT NULL COMMENT '操作IP',
    `before_data` TEXT         DEFAULT NULL COMMENT '操作前数据快照',
    `after_data`  TEXT         DEFAULT NULL COMMENT '操作后数据快照',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_op_log_admin_id` (`admin_id`),
    KEY `idx_op_log_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='后台操作日志表';
```

字段扩展：

- `module + operation` — 操作模块与描述，快速了解操作内容
- `before_data / after_data` — 操作前后数据快照，变更可追溯，审计核心

索引设计：

- `idx_admin_id` — 查询管理员操作历史
- `idx_op_time` — 按时间范围审计

扩展点：

- 增加操作类型（增删改查）
- 增加风险等级，高危操作告警
- 按时间分表，长期归档

------

### 48. user_risk_log — 用户风控日志表

用途：记录用户异常行为，如爬虫、高频提交、作弊，是风控系统的核心记录。

```
DROP TABLE IF EXISTS `user_risk_log`;
CREATE TABLE `user_risk_log` (
    `id`            VARCHAR(32)  NOT NULL COMMENT '日志ID',
    `user_id`       BIGINT       DEFAULT NULL COMMENT '用户ID',
    `risk_type`     VARCHAR(30)  NOT NULL COMMENT '风险类型: crawl/frequent_commit/cheat',
    `risk_level`    TINYINT      DEFAULT 2 COMMENT '风险等级: 1低 2中 3高',
    `description`   VARCHAR(255) DEFAULT NULL COMMENT '风险描述',
    `ip`            VARCHAR(50)  DEFAULT NULL COMMENT '关联IP',
    `handle_result` VARCHAR(50)  DEFAULT NULL COMMENT '处理结果: 警告/限流/封禁',
    `created_at`    DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_risk_user_id` (`user_id`),
    KEY `idx_risk_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户风控日志表';
```

字段扩展：

- `risk_type / risk_level` — 风险类型与等级，分级处理
- `handle_result` — 风控处置结果，记录处置动作

索引设计：

- `idx_user_id` — 查询用户风险记录，评估用户信用
- `idx_risk_time` — 按时间范围统计风险事件

扩展点：

- 增加风控规则版本，记录触发的规则
- 增加申诉状态，用户可申诉风险判定
- 增加 IP 维度统计，识别批量作弊账号

------

## 7.9 系统配置与工具域

### 49. system_config — 系统配置表

用途：全局系统配置项，替代硬编码配置，支持后台动态修改。

```
DROP TABLE IF EXISTS `system_config`;
CREATE TABLE `system_config` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT,
    `config_key`   VARCHAR(100) NOT NULL COMMENT '配置键',
    `config_value` VARCHAR(500) NOT NULL COMMENT '配置值',
    `config_name`  VARCHAR(100) NOT NULL COMMENT '配置名称',
    `description`  VARCHAR(255) DEFAULT NULL COMMENT '配置说明',
    `config_type`  VARCHAR(30)  DEFAULT 'string' COMMENT '值类型: string/int/boolean/json',
    `created_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';
```

字段扩展：

- `config_key / config_value` — 键值对结构，统一管理所有配置
- `config_type` — 配置值类型，前端编辑表单适配不同控件

索引设计：

- `uk_config_key` — 配置键全局唯一，系统启动、运行时加载配置

扩展点：

- 增加配置分组，按模块管理配置
- 增加环境标识，区分开发 / 测试 / 生产配置
- 增加版本号，配置变更可回滚

------

### 50. sys_dict — 系统字典主表

用途：业务枚举字典分类，统一管理所有业务枚举值。

```
DROP TABLE IF EXISTS `sys_dict`;
CREATE TABLE `sys_dict` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `dict_code`   VARCHAR(50)  NOT NULL COMMENT '字典编码',
    `dict_name`   VARCHAR(100) NOT NULL COMMENT '字典名称',
    `description` VARCHAR(255) DEFAULT NULL COMMENT '字典描述',
    `status`      TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at`  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_dict_code` (`dict_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统字典主表';
```

字段扩展：

- `dict_code` — 字典分类编码，如 order_status、pay_type
- `status` — 字典分类状态，禁用后下属字典项全部失效

索引设计：

- `uk_dict_code` — 字典编码唯一，业务代码引用

扩展点：

- 增加字典排序，控制展示顺序
- 增加字典备注说明，辅助后台管理

------

### 51. sys_dict_item — 系统字典项表

用途：字典分类下的具体枚举值，替代代码硬编码，后台可动态修改。

```
DROP TABLE IF EXISTS `sys_dict_item`;
CREATE TABLE `sys_dict_item` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `dict_id`    BIGINT       NOT NULL COMMENT '关联字典ID',
    `dict_value` VARCHAR(100) NOT NULL COMMENT '字典值',
    `dict_label` VARCHAR(100) NOT NULL COMMENT '字典显示名称',
    `sort`       INT          DEFAULT 0 COMMENT '排序号',
    `status`     TINYINT      DEFAULT 1 COMMENT '1:启用 0:禁用',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_dict_val` (`dict_id`,`dict_value`),
    KEY `idx_dict_item_dict_id` (`dict_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统字典项表';
```

字段扩展：

- `dict_value / dict_label` — 存储值与显示名分离，代码用 value，前端展示 label
- `dict_id + dict_value` 联合唯一，一个分类下值不重复

索引设计：

- `uk_dict_val` — 联合唯一，保证字典值不重复
- `idx_dict_id` — 查询字典分类下所有选项，前端下拉框加载

扩展点：

- 增加字典项颜色、标签样式，前端展示更丰富
- 增加默认项标记，默认选中项

------

### 52. statistics — 业务统计表

用途：按天统计核心业务指标，如日活、提交量、充值额，数据大盘、报表使用。

```
DROP TABLE IF EXISTS `statistics`;
CREATE TABLE `statistics` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `stat_date`  DATE         NOT NULL COMMENT '统计日期',
    `stat_type`  VARCHAR(30)  NOT NULL COMMENT '统计类型: dau/submit/pay',
    `stat_value` BIGINT       DEFAULT 0 COMMENT '统计数值',
    `ext_data`   JSON         DEFAULT NULL COMMENT '扩展统计数据',
    `created_at` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_stat_date_type` (`stat_date`,`stat_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='业务统计表';
```

字段扩展：

- `stat_date + stat_type` — 按天按类型统计，每日一条
- `ext_data` — 扩展 JSON 字段，存储多维度细分统计数据

索引设计：

- `uk_stat_date_type` — 联合唯一，防止同一天同一指标重复统计
- `idx_stat_date` — 按日期范围查询报表

扩展点：

- 增加小时级、分钟级统计表，满足实时监控
- 增加统计维度，如新用户、老用户拆分
- 增加环比、同比计算字段

------

### 53. gray_release — 灰度放量策略表

用途：新功能、新配置的灰度发布控制，按用户比例、用户标签放量，降低上线风险。

```
DROP TABLE IF EXISTS `gray_release`;
CREATE TABLE `gray_release` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT,
    `gray_key`     VARCHAR(50)  NOT NULL COMMENT '灰度功能标识',
    `gray_name`    VARCHAR(100) NOT NULL COMMENT '灰度功能名称',
    `gray_percent` INT          DEFAULT 0 COMMENT '放量百分比 0-100',
    `gray_users`   TEXT         DEFAULT NULL COMMENT '白名单用户ID,逗号分隔',
    `status`       TINYINT      DEFAULT 0 COMMENT '0:关闭 1:灰度中 2:全量',
    `description`  VARCHAR(255) DEFAULT NULL COMMENT '灰度说明',
    `created_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `updated_at`   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_gray_key` (`gray_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='灰度放量策略表';
```

字段扩展：

- `gray_key` — 灰度功能唯一标识，代码中判断是否命中灰度
- `gray_percent` — 放量比例，逐步从 0 到 100 全量
- `gray_users` — 白名单用户，优先体验新功能

索引设计：

- `uk_gray_key` — 灰度标识唯一，系统加载灰度策略使用

扩展点：

- 增加灰度规则，按用户等级、注册时间等维度放量
- 增加灰度开关、回滚按钮，一键操作
- 增加灰度效果统计，对比灰度组与对照组数据
