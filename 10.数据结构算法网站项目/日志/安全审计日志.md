**和业务日志分开，不可篡改，用于等保、安全溯源**

> 重点：**所有账号的敏感操作必须独立审计日志，不能混普通业务日志**

- 用户侧：登录、登出、异地登录、密码修改、绑定邮箱手机
- 管理员 / 运营后台：登录后台、新增 / 删除题目、修改敏感词、批量导入、成绩修改、用户封禁、订单退款、权限变更
- 记录字段：`traceId、operator_id、operator_ip、操作类型、资源ID、变更前内容、变更后内容、操作结果`

> 你的 OJ：新增敏感词、审核候选词、批量导入题库，全部输出审计日志，存入 ES，禁止删除。