

# 目录

[toc]

# Java基础

## **javase 基础语法**  

- 变量与数据类型：8 种基本数据类型、引用类型、类型转换、自动装箱拆箱 
- 字符串 数学运算 位运算 编码
- 流程控制：分支、循环、跳转语句、循环嵌套
- 数组：一维 / 二维数组、内存分配、数组遍历与排序、Arrays 工具类
- 方法：定义、重载、参数传递、可变参数、递归
- 编码规范：标识符、关键字、注释、命名规范

## 面向对象  

- 类与对象：属性、方法、构造方法、this 关键字、对象内存模型 值传递/引用传递
- 三大特性：封装、继承、多态（向上 / 向下转型、方法重写）
- 抽象类与接口：定义、区别、使用场景、函数式接口
- 权限修饰符：public/protected/default/private 作用范围
- 内部类、代码块、final、static 关键字的用法与底层原理
- Object 类核心方法：equals、hashCode、toString、clone

## 高级应用开发

- **异常机制**：异常体系、受检 / 非受检异常、try-catch-finally、throw/throws、自定义异常
- **集合框架**：Collection/Map 体系，ArrayList/LinkedList/HashMap/HashSet/ConcurrentHashMap 底层原理、迭代器、Collections 工具类
- **IO 流**：字节流 / 字符流、缓冲流、转换流、序列化流、NIO 基础概念
- **流式输出**
- **多线程**：线程创建方式、线程生命周期、线程安全、synchronized、Lock、线程通信、线程池、ThreadLocal、volatile
- **泛型**：泛型类 / 方法 / 接口、通配符、类型擦除
- **反射**：Class 对象获取、反射操作构造 / 方法 / 字段、反射的应用场景
- **注解**：元注解、自定义注解、注解解析
- **Lambda & Stream**：函数式编程、流式计算、常用中间 / 终端操作
- **JVM 基础**：内存区域、类加载过程、垃圾回收算法与回收器、JVM 参数调优入门
- **正则表达式**
- **JDK新特性 ** 8-25
- **函数式编程**

## 并发编程

并发是 Java 面试**第一高频**模块，必须系统掌握。

#### 1.1 线程基础

- 线程创建的 4 种方式：继承 Thread、实现 Runnable、实现 Callable + FutureTask、线程池
- 线程生命周期：NEW → RUNNABLE → BLOCKED → WAITING → TIMED_WAITING → TERMINATED
- 线程常用方法：start/run、sleep/wait/notify/notifyAll、join、yield、interrupt、setDaemon
- 线程中断机制：interrupt 标志位、isInterrupted、interrupted 区别、优雅停止线程
- 线程优先级、守护线程、线程组

#### 1.2 线程安全核心

- **synchronized 关键字**
  - 作用范围：对象锁（方法/代码块） vs 类锁（静态方法/类.class）
  - 底层原理：monitorenter/monitorexit、Monitor 对象、对象头 MarkWord
  - 锁升级过程：无锁 → 偏向锁 → 轻量级锁 → 重量级锁（JDK6 后）
  - 锁消除、锁粗化、自适应自旋
- **volatile 关键字**
  - 两大特性：可见性（内存屏障）、禁止指令重排序
  - 不保证原子性的原因
  - 应用场景：状态标记、双重检查锁单例、防止指令重排
- **Lock 接口体系**
  - ReentrantLock 可重入锁：公平/非公平、lock/tryLock/lockInterruptibly
  - ReentrantReadWriteLock 读写锁：读读共享、读写互斥、写写互斥
  - Condition 条件变量：await/signal/signalAll，替代 wait/notify
  - 与 synchronized 的对比

#### 1.3 JUC 并发工具包

- **原子类 Atomic**：AtomicInteger、AtomicReference、LongAdder、AtomicStampedReference（ABA 问题解决）
- **并发集合**
  - ConcurrentHashMap：分段锁 → CAS + synchronized（JDK8）、size 计算、扩容transfer
  - CopyOnWriteArrayList：写时复制思想、适用场景
  - ConcurrentLinkedQueue：非阻塞队列、CAS 实现
  - BlockingQueue 家族：ArrayBlockingQueue、LinkedBlockingQueue、DelayQueue、SynchronousQueue、PriorityBlockingQueue
- **同步工具类**
  - CountDownLatch：倒计时门闩，一等多
  - CyclicBarrier：循环栅栏，多等一，可重置
  - Semaphore：信号量，限流
  - Exchanger：交换器，两线程交换数据
  - Phaser：阶段器，替代 CyclicBarrier

#### 1.4 线程池

- 七大核心参数：corePoolSize、maximumPoolSize、keepAliveTime、unit、workQueue、threadFactory、handler
- 四种拒绝策略：AbortPolicy、CallerRunsPolicy、DiscardPolicy、DiscardOldestPolicy
- 任务提交流程：核心池 → 队列 → 最大池 → 拒绝策略
- Executors 提供的四种线程池及各自问题（为什么《阿里规约》不推荐）
  - FixedThreadPool：队列无界 → OOM 风险
  - SingleThreadExecutor：队列无界 → OOM 风险
  - CachedThreadPool：最大线程数 Integer.MAX_VALUE → 线程过多 OOM
  - ScheduledThreadPool：最大线程数 Integer.MAX_VALUE → 同上
- 线程池监控：活跃线程数、任务完成数、队列大小
- 线程池配置最佳实践：CPU 密集型 vs IO 密集型、动态调整
- 优雅关闭：shutdown vs shutdownNow、awaitTermination

#### 1.5 ThreadLocal

- 底层原理：ThreadLocalMap、Entry（弱引用）、set/get/remove
- 内存泄漏原因及解决方案（为什么要手动 remove）
- 父子线程传递：InheritableThreadLocal
- 应用场景：用户上下文、事务连接、SimpleDateFormat 线程安全问题

#### 1.6 AQS 抽象队列同步器

- 核心思想：state 状态 + CLH 双向队列 + CAS
- 独占模式 vs 共享模式
- 模板方法：tryAcquire/tryRelease、tryAcquireShared/tryReleaseShared
- ReentrantLock、Semaphore、CountDownLatch 基于 AQS 的实现原理

#### 1.7 死锁

- 死锁四条件：互斥、请求与保持、不可剥夺、循环等待
- 死锁排查：jstack、死锁检测
- 避免死锁：破坏任一条件、按顺序加锁、限时锁

## jdbc

- JDBC 核心 API：DriverManager、Connection、Statement、PreparedStatement、ResultSet
- 数据库连接池：Druid/HikariCP 配置与使用，连接池原理
- 事务管理：手动提交 / 回滚事务
- 常见问题：SQL 注入、PreparedStatement 防注入原理

# 数据库

## MySQL 

- **SQL 语法**：DQL/DML/DDL/DCL，多表查询、子查询、聚合函数、分组排序、分页
- **数据库设计**：三大范式、表关系（一对一 / 一对多 / 多对多）、外键、索引设计
- **索引**：索引类型、B + 树结构、聚簇 / 非聚簇索引、最左前缀原则、索引失效场景、Explain 分析
- **事务**：ACID 特性、隔离级别、脏读 / 不可重复读 / 幻读、MVCC 原理
- **锁机制**：行锁、表锁、间隙锁、死锁
- **性能优化**：慢查询定位、SQL 优化、分库分表基础概念
- **存储引擎**：InnoDB 与 MyISAM 的区别
- **一主两从 二主5从 如何设计**
- **数据库优化**
- **1慢查询日志开启、explain 完整字段解读 (type/key/rows/Extra)** 
- **2 锁：MVCC 底层 undo/redo log、Read View、隔离级别实现原理** 
- **3 redo/undo 日志作用、崩溃恢复机制**
-  **4 分库分完整方案：水平 / 垂直分表、分片算法、跨分页、跨 join 解决方案** 
- **5 读写分离：主从同步原理、binlog 三种格式、延迟问题与优化**
-  **6 死锁排查、锁等待超时优化 7 百万级数据分页优化、大事务危害**

## Redis

- **数据类型：String/List/Hash/Set/ZSet 及对应应用场景**
- **核心特性：过期策略、内存淘汰机制、持久化（RDB/AOF）**
- **高可用：主从复制、哨兵模式、Cluster 集群**
- **常见问题：缓存穿透 / 击穿 / 雪崩、分布式锁、缓存与数据库一致性**
- **典型应用：验证码、购物车、排行榜、点赞关注、接口限流**
- **1RDB/AOF 混合持久化、AOF 重写机制**
-  **2 集群哈希槽、槽迁移、集群故障转移** 
- **3 布隆过滤器、HyperLogLog、Geo、BitMap 实战场景**
-  **4 Redis 事务、Lua 脚本原子操作**
-  **5 Redis 内存碎片优化、大 key 处理方案** 
- **6 Redisson 完整能力：可重入锁、读写锁、公平锁、信号量、分布式限流器**
- **集群验证**

#### 1. 基础入门

- 安装与启动：Windows / Linux / Docker
- Redis 五大数据类型及应用场景：
  - **String**：缓存对象、计数器、分布式锁、Session
  - **Hash**：对象缓存、购物车
  - **List**：消息队列、文章列表、排行榜
  - **Set**：去重、共同关注、点赞、标签
  - **ZSet**：排行榜、带权重的任务队列
- Java 客户端：Jedis vs Lettuce vs Redisson
- Spring Boot 整合 Redis：`spring-boot-starter-data-redis`
- RedisTemplate 配置：序列化策略（Jackson2JsonRedisSerializer）

#### 2. 核心特性

- 过期策略：定期删除 + 惰性删除
- 内存淘汰机制：8 种淘汰策略（LRU、LFU、随机等）
- 持久化：RDB、AOF、混合持久化
- 发布订阅：Pub/Sub

#### 3. 经典问题与解决方案

- **缓存穿透**：查询不存在的数据 → 布隆过滤器、缓存空值
- **缓存击穿**：热点 Key 过期 → 互斥锁、永不过期
- **缓存雪崩**：大量 Key 同时过期 → 随机过期时间、多级缓存
- **缓存与数据库一致性**：
  - Cache Aside 模式（旁路缓存）
  - 先更数据库还是先删缓存？
  - 延迟双删方案
  - 订阅 binlog 异步更新（Canal）

#### 4. 分布式锁

- Redis 分布式锁实现：`SET key value NX PX 30000`
- 锁的释放：Lua 脚本保证原子性
- 锁续期：看门狗机制
- Redisson 分布式锁：可重入锁、公平锁、读写锁、红锁
- **动手实践**：基于 Redis 实现一个防重复提交注解

#### 5. 项目中的应用

- 给项目加上缓存：热点数据缓存（用户信息、配置项）
- 验证码：登录注册验证码（String 类型 + 过期时间）
- 接口限流：基于 Redis + 注解实现接口限流
- 点赞/收藏功能：Set 数据结构实现
- 排行榜：ZSet 实现积分排行榜

#### 6. 进阶了解

- Redis 集群：主从复制、哨兵模式、Cluster 集群
- 布隆过滤器：RedisBloom 插件、Guava 布隆过滤器
- HyperLogLog：UV 统计
- Geo：地理位置、附近的人
- BitMap：用户签到、在线状态

## MongoDB

- 文档型数据库，BSON 格式
- 核心概念：Database、Collection、Document、Field
- 常用操作：CRUD、聚合管道（Aggregation Pipeline）
- 索引：单字段、复合、文本、地理空间索引
- 适用场景：日志存储、内容管理、物联网数据、灵活 schema 的业务
- 与 MySQL/Redis 的选型对比

# 数据结构算法

左程云 算法讲解  

黑马程序员算法

## 基础

1. 基础概念
   - 区分大 O（上界）、Ω（下界）、Θ（紧界）的定义，面试以大 O 时间 / 空间复杂度为核心
   - 区分**最坏情况、最好情况、平均情况**，分析算法默认说最坏情况
2. **复杂度排序**：牢记常见复杂度量级 `O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(2ⁿ) < O(n!)`
3. **摊还分析**：理解动态数组扩容、哈希表操作的平摊复杂度，能解释 “为什么动态数组插入是 O (1) 平摊”
4. **主定理**：快速计算递归算法的时间复杂度，覆盖二分查找、归并排序、二叉树遍历等经典递归
5. **能力要求**：看到任意一段代码，10 秒内说出时间、空间复杂度；能对比不同算法的优劣

![image-20260816160835430](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260816160835430.png)

## 基础数据结构

### 1. 数组（Arrays）

#### 核心掌握内容

- 底层原理：静态数组 vs 动态数组，动态数组的**扩容机制**（2 倍 / 1.5 倍扩容、内存重新分配、元素拷贝）
- 核心特性：随机访问 O (1) 的本质 —— 通过内存地址偏移直接定位
- 必练操作：原地删除元素、数组合并、二分查找、**双指针技巧**（快慢指针、左右指针、滑动窗口）
- 时间复杂度：访问 O (1)，中间插入 / 删除 O (n)，尾部插入平摊 O (1)
- 延伸：数组的缓存友好性，为什么数组比链表遍历更快

### 2. 链表（Linked Lists）

#### 核心掌握内容

- 结构分类：单链表、双向链表、循环链表的结构差异与适用场景
- 基础操作：增、删、查、改；熟练使用**虚拟头节点（dummy node）**简化边界处理
- 高频面试题型（必须手写无 bug）：
  - 反转链表（整体反转、局部反转）
  - 链表环检测、环入口节点（快慢指针法）
  - 删除倒数第 N 个节点
  - 合并两个 / 多个有序链表
  - 相交链表找交点
- 复杂度：随机访问 O (n)，头尾增删 O (1)（带尾指针），中间增删 O (n)
- 能清晰对比数组与链表的优劣势，说明各自适用场景

### 3. 堆栈（Stack）

#### 核心掌握内容

- 特性：后进先出（LIFO），能用数组、链表两种方式实现栈
- 经典应用场景：括号匹配、逆波兰表达式求值、函数调用栈模拟、**单调栈**
- 单调栈必刷题：每日温度、柱状图中最大矩形、接雨水
- 复杂度：入栈、出栈、取栈顶均为 O (1)

### 4. 队列（Queue）

#### 核心掌握内容

- 特性：先进先出（FIFO），能用循环数组、链表两种方式实现
- 扩展结构：双端队列（Deque）、优先队列、阻塞队列的概念
- 经典应用：BFS 层序遍历、**单调队列**（滑动窗口最大值）、任务调度
- 循环队列：掌握判空、判满条件，队头 / 队尾指针的计算方式
- 复杂度：入队、出队均为 O (1)

### 5. 哈希表（Hash table）

#### 核心掌握内容

- 哈希函数：设计原则，常见哈希方法，如何减少冲突
- 哈希冲突解决方案：
  - 链地址法（拉链法）：Java HashMap 采用
  - 开放寻址法：线性探测、二次探测
- 核心机制：负载因子、**扩容与重哈希**的过程与代价
- 高频应用：频次统计、数据去重、两数之和、LRU 缓存实现
- 复杂度：平均增删查 O (1)，最坏情况 O (n)（全冲突）
- 了解你常用语言的内置哈希结构底层（如 Python dict、Java HashMap）

## 三、基础算法

### 1. 二分查找（Binary search）

#### 核心掌握内容

- 前提条件：有序数组；熟练掌握**左闭右闭、左闭右开**两种模板，边界处理零错误
- 基础题型：查找目标值、查找左边界、查找右边界
- 扩展题型（面试高频）：
  - 旋转排序数组中查找
  - 山脉数组峰值
  - 二分答案类：最大化最小值、最小化最大值
- 细节：避免整数溢出的 mid 计算方式；时间复杂度 O (logn)

### 2. 按位运算（Bitwise operations）

#### 核心掌握内容

- 6 种基础运算符：`&` 与、`|` 或、`^` 异或、`~` 非、`<<` 左移、`>>` 右移
- 必背技巧：
  - 异或：找出现 1 次的数字、不使用临时变量交换两个数
  - `n & (n-1)`：消除最低位的 1 → 统计 1 的个数、判断 2 的幂
  - 位掩码：状态压缩、子集枚举
- 编码基础：原码、反码、补码的定义，补码的工程意义
- 区分：算术右移 vs 逻辑右移

------

## 四、树（Trees）

### 1. 树基础与遍历

#### 核心掌握内容

- 基础概念：根节点、叶子节点、深度、高度、层、度
- 四种遍历（必须会递归 + 迭代两种写法）：
  - 前序遍历：根 → 左 → 右
  - 中序遍历：左 → 根 → 右
  - 后序遍历：左 → 右 → 根
  - 层序遍历：BFS + 队列实现
- 遍历应用：中序遍历 BST 得有序数组；后序遍历做树形 DP；层序遍历求树的宽度
- 延伸：N 叉树的前序、后序、层序遍历

### 2. 二叉查找树（BST）

#### 核心掌握内容

- 核心性质：左子树所有节点值 < 根节点值 < 右子树所有节点值；**中序遍历结果有序**
- 基础操作：插入、查找、删除（删除分 3 种情况：叶子节点、单子节点、双子节点）
- 验证 BST 合法性：不能只判断左右子节点，必须传递上下界
- 寻找前驱节点、后继节点
- 复杂度：平均操作 O (logn)，最坏 O (n)（退化成链表）

### 3. 堆 / 优先级队列 / 二叉堆

#### 核心掌握内容

- 结构：大顶堆、小顶堆；数组存储的下标映射（父 i → 左 2i+1、右 2i+2）
- 核心操作：上浮（sift up）、下沉（sift down）的代码实现
- 建堆：自底向上建堆，时间复杂度 O (n)
- 堆排序：原理、实现、O (nlogn) 时间复杂度、不稳定排序
- 高频应用：TopK 问题、数据流中位数、任务优先级调度

### 4. 平衡搜索树（概念级）

#### 核心掌握内容

- 平衡树的意义：避免 BST 退化，保证操作复杂度稳定 O (logn)
- 常见类型与特点：
  - AVL 树：严格平衡，插入删除慢，查询快
  - 红黑树：近似平衡，插入删除快，工程更常用；掌握 5 条性质
  - B 树 / B + 树：多路平衡查找树；B + 树为什么适合数据库索引
- 面试不要求手写实现，但能说出核心区别与应用场景

### K叉树

### B树

### 树状数组

### 字典树

## KD树

------

## 五、排序（Sorting）

### 核心掌握内容

每种排序必须掌握：**原理 + 手写代码 + 时间 / 空间复杂度 + 稳定性 + 适用场景**

1. 基础排序
   - 冒泡排序：O (n²)，稳定，原地；仅作入门
   - 选择排序：O (n²)，不稳定，原地
   - 插入排序：O (n²)，稳定，原地；数据量小、接近有序时效率极高
2. 进阶排序（面试核心）
   - 归并排序：分治思想，O (nlogn)，稳定，空间 O (n)；延伸：求逆序对
   - 快速排序：分治 + 分区，平均 O (nlogn)、最坏 O (n²)，不稳定，原地；掌握分区点选择、三路快排优化
   - 堆排序：O (nlogn)，不稳定，原地
3. **排序稳定性**：定义是什么，稳定性有什么工程意义
4. **算法选型**：不同场景下选哪种排序最优
5. **线性排序（了解）**：计数排序、桶排序、基数排序；适用场景与局限性

------

## 六、图（Graphs）

### 核心掌握内容

1. **基础概念**：有向 / 无向图、加权 / 无权图、度、入度出度、连通分量

2. 存储方式

   ：

   - 邻接矩阵：适合稠密图，空间 O (n²)
   - 邻接表：适合稀疏图，空间 O (n+m)
   - 能对比两者优劣势，按需选择

3. 两大遍历（必须手写）

   - BFS：队列实现，层序扩展；求无权图最短路径
   - DFS：递归 / 栈实现；求连通性、环检测、拓扑排序

4. 最短路径

   - Dijkstra：单源最短路径，无负权加权图，贪心思想
   - Bellman-Ford：单源，支持负权，可检测负权环
   - Floyd-Warshall：多源最短路径，动态规划，O (n³)

5. 最小生成树

   - Kruskal：边排序 + 并查集，稀疏图更优
   - Prim：点扩展，稠密图更优

6. **拓扑排序**：仅适用于有向无环图（DAG）；掌握 Kahn 算法（BFS）、DFS 两种实现

7. 并查集（Union-Find）

   - 核心操作：合并、查询；路径压缩 + 按秩合并优化
   - 应用：连通性判断、朋友圈、岛屿数量、Kruskal 算法

8. **环检测**：有向图、无向图的 DFS 检测方法

## 七、进阶知识与计算机基础

### 1. 递归与回溯

#### 核心掌握内容

- 递归三要素：终止条件、本级逻辑、递归调用
- 能画递归树，分析递归的时间、空间复杂度，理解栈溢出风险
- 回溯算法通用模板：选择 → 递归 → 撤销选择
- 经典题型：子集、全排列、组合总和、括号生成、N 皇后、单词搜索
- 剪枝技巧：提前排除无效路径，大幅降低时间开销

### 2. 动态规划（Dynamic Programming）

#### 核心掌握内容

- DP 四步解题法：状态定义 → 状态转移方程 → 初始化 → 遍历顺序
- 核心特性：重叠子问题、最优子结构、无后效性
- 优化：空间压缩（滚动数组、降维）
- 高频题型分类（每类至少练 5 道）：
  - 线性 DP：爬楼梯、最大子数组和、打家劫舍
  - 子序列 DP：最长递增子序列、最长公共子序列、编辑距离
  - 背包问题：01 背包、完全背包、多重背包
  - 区间 DP：回文子串、戳气球
  - 树形 DP：二叉树最大路径和
- 区分：记忆化搜索（自顶向下） vs 递推（自底向上）



- 1 LRU 缓存 2 单例模式多版本 3 生产者消费者模型 4 树形结构递归组装 5 手写限流工具
- 队列算法

### 3. 字符串搜索和操作

#### 核心掌握内容

- 暴力匹配原理与复杂度
- KMP 算法：next 数组构建、匹配过程；O (n+m) 时间复杂度
- Rabin-Karp：滚动哈希思想，字符串匹配
- 高频题型：反转字符串、回文子串、字符串异位词、字符串转换

### 4. 字典树（Tries）

#### 核心掌握内容

- 结构：前缀树，以字符为路径的多叉树
- 操作：插入字符串、查找字符串、前缀匹配
- 应用场景：单词搜索、输入法联想、路由前缀匹配、词频统计
- 复杂度：插入、查询均为 O (L)，L 为字符串长度

### 5.缓存算法

### 6.贪心算法



#  前后端

## 前端基础

#### 2.1 HTML5 & CSS3

- HTML5 语义化标签、Canvas、Web Storage、Web Worker
- CSS3 选择器、Flexbox、Grid 布局、动画（transition/animation）、响应式设计
- CSS 预处理器：Less / Sass（选一个）

#### 2.2 JavaScript 深入

- 原型与原型链、闭包、作用域链、this 指向
- ES6+ 新特性：let/const、箭头函数、解构赋值、Promise、async/await、模块化 import/export、Map/Set、Proxy/Reflect、Symbol
- 事件循环：宏任务/微任务、EventLoop 机制
- 深浅拷贝、防抖节流、柯里化、偏函数

#### 2.3 Vue 3 全家桶（重点）

- 组合式 API：setup、ref、reactive、computed、watch、生命周期钩子
- 响应式原理：Vue2 Object.defineProperty vs Vue3 Proxy
- Vue Router 4：路由守卫、动态路由、路由懒加载
- Pinia 状态管理：替代 Vuex，更简洁
- Vite 构建工具：比 Webpack 快的原因
- 组件通信方式：props/emit、provide/inject、Pinia、事件总线

#### 2.4 TypeScript

- 类型系统：基本类型、接口、泛型、枚举、联合类型、类型守卫
- 在 Vue3 项目中使用 TS

#### 2.5 工程化与工具

- 包管理器：npm / pnpm / yarn
- 构建工具：Webpack / Vite / Rollup
- 代码规范：ESLint、Prettier、Husky + lint-staged

#### 2.6 主流 UI 组件库

- PC 端：Element Plus（Vue3）、Ant Design Vue
- 移动端：Vant、NutUI

## vue

### 5.1 Vue 3 基础

#### 1. Vue 3 核心

- 组合式 API：`setup` 语法糖
- 响应式：`ref`、`reactive`、`computed`、`watch`
- 生命周期：`onMounted`、`onUnmounted`、`onUpdated`
- 模板语法：插值、指令、事件绑定、双向绑定
- 组件基础：组件定义、Props、Emit、插槽

#### 2. Vue Router

- 路由配置、动态路由、嵌套路由
- 路由守卫：登录拦截、权限控制
- 路由懒加载

#### 3. Pinia 状态管理

- Store 定义、State、Getters、Actions
- 持久化插件

#### 4. TypeScript 基础

- 基本类型、接口、泛型、联合类型
- 在 Vue 3 中使用 TS

### 5.2 UI 组件库

- **Element Plus**：PC 端最常用
- 常用组件：表单、表格、对话框、分页、上传、树形控件
- 组件二次封装：通用表格、通用表单、通用弹窗

### 5.3 工程化

- Vite 构建工具
- 项目结构规范
- 接口封装：Axios 封装、请求拦截、响应拦截、错误处理
- 环境变量：开发环境、生产环境
- 代码规范：ESLint + Prettier

### 5.4 前端实战

- 登录注册页面
- 后台管理系统布局（侧边栏 + 顶栏 + 主内容区）
- 用户管理 CRUD 页面
- 角色权限管理
- 图表展示：ECharts
- AI 对话页面（流式输出、打字机效果）
- 图片生成页面
- 知识库管理页面

## WEB技术栈

  XML Tomcat HTTP协议   Nginx Git  等 Ajax JSON Jquery  Maven Docker Redis

#### XML / Tomcat / HTTP 协议

- XML：语法、约束（DTD/Schema）、解析方式（DOM/SAX）
- Tomcat：目录结构、部署项目、虚拟路径、端口配置、Connector 容器
- HTTP 协议：请求 / 响应结构、请求方法、状态码、请求头 / 响应头、HTTPS 基础、无状态特性

#### Nginx

- 核心功能：反向代理、负载均衡、动静分离、跨域配置
- 基础配置：server 块、location 匹配规则、upstream 配置
- 常用场景：前端项目部署、接口代理、静态资源缓存

#### Git

- 核心命令：初始化、提交、分支、合并、变基、回退、暂存、远程仓库交互
- 工作流：Git Flow、团队协作冲突解决、代码提交规范

#### Ajax / JSON / jQuery

- JSON：语法、与对象互转、前后端数据交互格式
- Ajax：原生 XMLHttpRequest、Fetch、Axios 发送异步请求
- jQuery：选择器、DOM 操作、事件、Ajax 封装（了解即可，主流已转向 Vue）

#### Maven

- 核心概念：坐标、依赖、仓库（本地 / 远程 / 中央）、生命周期
- 依赖管理：依赖范围、依赖传递、依赖排除、版本冲突解决
- 多模块项目：聚合与继承、私服 Nexus 基础使用

#### Docker

- **核心概念：镜像、容器、仓库、Dockerfile**
- **常用命令：镜像操作、容器启停、数据卷、端口映射**
- **镜像构建：Dockerfile 编写、镜像推送、Docker Compose 编排多容器**
- **自动化构建镜像流水线**
-  **Docker 镜像分层原理、联合文件系统** 
- **容器资源限制 CPU / 内存、容器隔离 namespace/cgroups**
-  **镜像安全、多阶段构建减小镜像体积**



## 后台三大组件

**Servlet**

- 生命周期、执行流程、ServletConfig、ServletContext
- 请求响应：HttpServletRequest/HttpServletResponse 常用方法
- 请求转发与重定向的区别、路径问题

**Filter**

- 过滤器生命周期、过滤器链、编码过滤、登录权限校验
- 过滤器执行顺序、url-pattern 匹配规则

**Listener**

- 三大域对象监听器（ServletContext/HttpSession/ServletRequest）
- 常见应用：在线人数统计、系统初始化参数加载

## 会话控制

- **Cookie**：原理、生命周期、域和路径、常用 API、禁用 Cookie 解决方案
- **Session**：原理、生命周期、钝化活化、销毁时机、Session 追踪机制
- Cookie 与 Session 的区别、分布式环境下 Session 共享方案

## 文件上传下载 & OSS 存储

- 本地文件上传：表单配置、MultipartFile 接收、文件大小限制、文件名防重
- 文件下载：响应头设置、字节流输出、中文文件名乱码处理
- 阿里云 OSS/MinIO：对象存储概念、SDK 集成、签名上传、文件访问权限管理

## API规范

- API 协议 / 风格：REST、JSON API、SOAP、gRPC、GraphQL、HATEOAS
- API 规范：开放 API 规格（OpenAPI）
- 身份认证方式：基本认证、Cookie 认证、令牌认证、OAuth、JWT、SAML、OpenID
- API 规范：统一返回码、参数校验、接口限流、版本控制、文档自动生成（Swagger/OpenAPI）
- 安全攻防落地：SQL 注入、XSS、CSRF、DDoS 防护、敏感数据脱敏、数据加密 AES/RSA
- 权限体系：RBAC 权限模型、接口黑白名单、接口鉴权链路设计

## 接口文档 

这份文档以思维导图形式完整覆盖**API 全生命周期设计体系**，整体分为**构建 API、API 认证与授权、API 安全、API 测试、API 文档与管理、配套工具**六大核心模块，逐层拆解 API 从概念定义到落地运维的完整知识体系，各板块详细梳理如下：

### 一、构建 API（API 基础开发与类型体系）

本模块是 API 入门核心，先厘清基础概念，再划分主流 API 技术类型、规范与架构风格。

1. **基础概念：什么是 API** 拆解 API 核心基础定义，区分三类基础接口模型：**公共接口、私有接口、内部接口**。
2. **HTTP 体系 API 细分** 梳理 HTTP 生态下各类 API：HTTP 接口、HTTP REST API、HTTP JSON API、HTTP 表单 API、HTTP 文件 API，覆盖日常开发绝大多数 HTTP 接口场景。
3. 主流 API 架构风格
   - RESTful API：当下最通用架构，配套规范包含资源命名、HTTP 动词、状态码、分页、过滤、排序、版本、缓存、错误处理；
   - GraphQL API：独立查询式接口方案，单独作为分支列出；
   - SOAP API：传统 XML 接口标准；
   - gRPC API：高性能二进制接口，配套 Protobuf 序列化；
   - RPC NODE/HTTP API：轻量 RPC 分支方案。
4. **API 分层设计规范** 包含分层、资源命名、参数规范、响应格式、错误处理五大落地设计细则。

### 二、API 认证与授权（接口身份校验 + 权限管控）

解决「谁能访问、能访问哪些资源」的安全准入问题，分为认证、授权两大分支。

1. **认证方法（身份核验）** 罗列行业主流认证方案：基础认证、密钥认证、Token 令牌认证、会话认证、证书认证。

2. 授权方法（权限分配）

   区分三套成熟授权协议 / 框架：

   - 基于密码的授权（OAuth1）；
   - 基于令牌的授权（OAuth2）；
   - 基于身份的授权（OIDC，OpenID Connect）； 配套衍生标准：API 密钥、JWT 令牌。

3. **配套扩展体系** 补充 HMAC-SHA 签名校验、API 访问限流、API 跨域访问控制（CORS）三大辅助准入机制。

### 三、API 安全（接口全链路防护体系）

覆盖接口传输、调用、数据全维度安全防护手段，分为通用防护、传输安全、数据安全三类：

1. **常规漏洞防御**：防注入攻击、防跨站伪造、防越权访问；
2. **传输层安全**：HTTPS 加密、TLS/SSL 证书、请求签名校验；
3. **数据层安全**：数据脱敏、敏感字段加密、接口请求频率限流。

### 四、API 测试（接口质量验证体系）

完整覆盖 API 全类型测试方案，从功能到自动化、性能全覆盖：

1. 测试分类
   - 功能测试：单元测试、集成测试、场景测试；
   - 性能测试：并发压测、负载测试、稳定性测试；
   - 自动化测试：脚本自动化、流水线集成测试、契约测试。
2. **配套测试工具** 列举主流测试客户端：Postman、Insomnia、SoapUI、JMeter。

### 五、API 文档与管理（接口标准化、生命周期运维）

负责 API 标准化交付、版本管控与线上治理，分为文档规范、版本管理、监控告警：

1. **API 文档规范** 主流文档标准：OpenAPI（Swagger）、RAML、API Blueprint；规范包含接口入参、出参、错误码、示例、权限说明。
2. **API 版本管理** 多套版本控制方案：URL 路径版本、请求头版本、参数版本；配套兼容、弃用、灰度发布策略。
3. 监控与运维治理
   - 监控指标：响应耗时、错误率、调用量、成功率；
   - 告警机制：异常阈值告警、日志追踪；
   - 流量治理：灰度、熔断、降级、限流、缓存策略。

### 六、配套工具与平台落地

梳理 API 设计、开发、调试、部署全流程工具链：

1. **调试工具**：Postman、Apifox、curl；
2. **网关平台**：Kong、Nginx、Spring Cloud Gateway；
3. **文档生成工具**：Swagger UI、Redoc；
4. **自动化 / CI 工具**：Jenkins、Git 流水线、自动化测试脚本；
5. **低代码 API 平台**：可视化 API 构建、在线调试平台

## 数据库文档

### 一、数据库模型设计原则（库表字段本身）

**1. 命名规范原则**

1. 表名、字段名**下划线命名**，全部小写，禁止驼峰、中文。
2. 表名按模块前缀区分：`sys_`系统，`biz_`业务，避免不同业务表混在一起。
3. 禁止使用 MySQL 关键字做字段名（如 `order`、`desc`）。
4. 主键统一 `id`；通用字段名字保持统一：`create_time`、`update_time`、`create_by`、`update_by`、`is_deleted`。

**2. 范式与反范式平衡**

- 三范式：减少冗余，避免数据重复更新异常；
- **允许适度反范式**：频繁关联查询的场景，适当冗余字段，减少 JOIN，换取查询性能。

> 不要盲目严格第三范式，业务优先。

**3. 字段类型选择原则**

1. 时间：使用 `datetime`，**禁止 varchar 存时间**；
2. 状态、标志位：用 `tinyint`，不用字符串；
3. 手机号、账号、编码用 `varchar`，不要数字类型；
4. 长文本使用 `text`，普通短字符串合理设置长度，不要全部 varchar (255)；
5. 主键用 `bigint`，数据量大避免 int 溢出；
6. 非空约束：能 NOT NULL 尽量 NOT NULL，设置合理默认值，减少 null 带来的查询坑。

**4. 删除策略：优先逻辑删除**

- 使用 `is_deleted tinyint 0未删除 1已删除`，**业务代码实现删除，少用物理删除**；
- 重要数据保留，方便排查、恢复；

> 注意：所有查询必须带上 `is_deleted=0`。

**5. 外键原则：业务层维护关联，数据库少用物理外键**

- 物理外键会影响性能、锁、迁移；
- **关联关系由应用程序代码保证**，文档中注明外键逻辑关系，数据库不建立物理外键约束。

**6. 索引设计原则**

1. where、join、order by、group by 的字段考虑建索引；
2. 索引不是越多越好，索引会降低插入更新性能；
3. 区分普通索引、唯一索引；联合索引遵循最左前缀；
4. 低基数字段（性别、状态）不建索引。

**7. 字符集与存储引擎**

- 统一引擎 InnoDB；字符集 `utf8mb4`，支持 emoji，不要用 utf8。

**8. 扩展性原则**

1. 预留扩展字段，如 `ext_json` json 字段存不确定的扩展属性；
2. 避免频繁修改表结构；大版本变更记录 DDL 脚本。

### 二、数据库**文档书写原则**（写文档，重点，考试 / 面试常考）

1. **业务易懂原则**：每张表必须写清楚业务含义；每个字段必须写注释，不能靠名字猜含义。
2. **枚举字典显性化**：状态、类型等数字枚举，文档中明确列出全部值含义，不要藏在代码里。
3. **体现关系**：提供 ER 图，写清表之间一对多、多对多逻辑关系；多对多必须中间表。
4. **索引完整记录**：文档写明每一个索引：索引名、字段、类型、建立原因。
5. **公共字段统一说明**：把 create_time、is_deleted 等公共字段统一说明，每张表不用重复解释。
6. **可追溯原则**：文档要有变更记录，记录每一次表结构改动、改动原因。
7. **和接口对齐**：数据库字段与后端实体、API 接口字段含义保持一致；数据库下划线 ↔ Java 驼峰。
8. **区分逻辑与物理**：文档写**逻辑外键关系**，同时注明数据库没有物理外键约束。
9. 不写模糊描述，例如 “状态：1 代表有效”，要完整全部枚举：`0禁用，1正常，2冻结`。
10. **每张表写清楚业务含义；**
11. **每个字段：类型、非空、默认值、注释；枚举全部列出来；**
12. **单独写索引，哪些字段建索引，为什么；**
13. **区分逻辑删除还是物理删除；**
14. **ER 图画出一对多关系（用户 1 对多订单）；**
15. **公共字段统一一套（create_time、is_deleted）。**

## 安全

- 网络安全基础：HTTPS、SSL/TLS、跨域资源共享（CORS）
- 服务器安全、API 安全最佳实践
- OWASP 安全风险
- 密码哈希算法：MD5、scrypt、bcrypt owasp 对称加密非对称加密

# 框架

## 1. Spring 6

- 核心思想：**IOC 控制反转**、**AOP 面向切面编程**
- IOC 容器：Bean 的定义、作用域、生命周期、依赖注入（构造 /setter/ 注解）
- 常用注解：@Component、@Autowired、@Qualifier、@Value、@Configuration、@Bean
- AOP 原理：动态代理（JDK/CGLIB）、通知类型、切入点表达式、切面执行顺序
- 事务管理：声明式事务 @Transactional、传播行为、隔离级别、失效场景
- 资源管理：国际化、事件机制、Bean 后置处理器
- 认证授权 OAuth2 JWT认证

## 2. SpringMVC

- 执行流程：九大组件、请求处理全链路
- 核心注解：@Controller、@RequestMapping、@RequestParam、@RequestBody、@ResponseBody
- 参数绑定：简单类型、POJO、集合、日期类型转换、数据校验 JSR303
- 返回值处理：页面跳转、JSON 返回、ModelAndView
- 拦截器：定义、配置、执行顺序、与过滤器的区别
- 异常处理：全局异常处理器 @RestControllerAdvice
- RESTful 风格：资源定位、HTTP 方法对应 CRUD

## 3. MyBatis / MyBatis-Plus

### MyBatis

- 核心原理：ORM 思想、SqlSessionFactory、Mapper 代理原理
- 映射文件：resultMap、动态 SQL（if/choose/foreach/sql 片段）
- 缓存机制：一级缓存、二级缓存、缓存失效场景
- 高级映射：一对一、一对多、多对多关联查询
- 分页插件、通用 Mapper 基础

### MyBatis-Plus

- 核心特性：无侵入 CRUD、条件构造器 QueryWrapper/LambdaQueryWrapper
- 主键策略、自动填充、逻辑删除、乐观锁
- 代码生成器、分页插件、多租户插件

#### 1.基础

**1. 快速上手**

- Spring Boot 整合 MyBatis-Plus（引入 Starter、配置文件）

- ```
  BaseMapper<T>
  ```

  ：内置的 CRUD 方法

  - `insert`、`deleteById`、`updateById`、`selectById`、`selectList`
  - `selectBatchIds`、`deleteBatchIds`

- ```
  IService<T>
  ```

  ```
  ServiceImpl<M, T>
  ```

  ：Service 层 CRUD

  - `save`、`remove`、`update`、`getById`、`list`、`page`

**2. 条件构造器**（最常用，必须熟练）

- ```
  QueryWrapper
  ```

  ：查询条件

  - `eq`、`ne`、`gt`、`ge`、`lt`、`le`（等于、不等于、大于、小于...）
  - `like`、`notLike`、`likeLeft`、`likeRight`
  - `in`、`notIn`、`between`
  - `orderByAsc`、`orderByDesc`
  - `select`（指定查询字段）
  - `and`、`or`（条件拼接）

- ```
  UpdateWrapper
  ```

  ：更新条件

  - `set`（设置更新字段）

- Lambda 版本

  （推荐，避免字段名写错）：

  - `LambdaQueryWrapper`、`LambdaUpdateWrapper`
  - `Wrappers.<User>lambdaQuery()` 快捷创建

**3. 分页插件**

- 分页配置类（`MybatisPlusInterceptor` + `PaginationInnerInterceptor`）
- `Page<T>` 对象使用
- `page(page, wrapper)` 分页查询
- 分页结果：`records`、`total`、`current`、`size`

**4. 常用注解**

- `@TableName`：指定表名
- `@TableId`：主键 + 主键策略（ASSIGN_ID 雪花算法、AUTO 自增）
- `@TableField`：字段映射、`exist = false`（非数据库字段）
- `@TableLogic`：逻辑删除

**5. 代码生成器**

- MyBatis-Plus Generator 或 CodeGenerator
- 一键生成 Entity、Mapper、Service、Controller
- 自定义模板（生成 VO、DTO、Query 等）

#### 2. 核心原理

- MyBatis-Plus 启动流程：如何自动注入 BaseMapper 方法
- Mapper 代理原理：JDK 动态代理 + MapperProxy
- SQL 注入原理：SqlInjector、AbstractMethod
- **动手实践**：自定义 BaseMapper 方法（比如批量插入 MySQL 专属方法）

#### 3. 条件构造器进阶

- QueryWrapper / UpdateWrapper / LambdaQueryWrapper / LambdaUpdateWrapper 区别
- 条件构造器的链式调用原理
- 自定义 SQL 条件：`apply`、`inSql`、`exists`
- 逻辑删除：全局配置、自定义删除值、联表查询注意事项
- 自动填充：`MetaObjectHandler`、创建时间/更新时间/创建人/更新人
- 乐观锁：`@Version` 注解、乐观锁插件、适用场景
- 多租户：TenantLineInnerInterceptor、租户隔离原理
- 动态表名：DynamicTableNameInnerInterceptor

#### 4. 分页插件

- 分页原理：Mybatis Plus 拦截器（InnerInterceptor）
- 物理分页 vs 逻辑分页
- 分页查询优化：count 查询优化、大页码优化
- **动手实践**：实现一个通用分页工具类，支持排序、条件查询

#### 5. 代码生成器

- MyBatis-Plus Generator 使用
- 自定义代码模板：Controller、Service、Mapper、Entity、VO、DTO
- 一键生成 CRUD 代码

#### 6. 高级映射

- 一对一、一对多、多对多关联查询
- `@TableName`、`@TableField`、`@TableId` 注解详解
- 字段类型处理器：TypeHandler、枚举处理器、JSON 处理器
- 主键策略：ASSIGN_ID（雪花算法）、AUTO、INPUT、UUID

#### **7. 自定义 SQL**

- 在 Mapper.xml 中写复杂 SQL
- `@Select`、`@Update` 注解写 SQL
- 条件构造器 + 自定义 XML 结合使用

#### 8.进阶了解（源码级理解）

- MyBatis-Plus 启动流程与注入原理
- `BaseMapper` 方法如何自动生成 SQL（SqlInjector）
- 拦截器链原理（InnerInterceptor）
- 分页插件源码分析
- 扩展自定义通用方法（比如自定义批量插入）

## 4. SSM 整合

- 整合思路：Spring 整合 MyBatis（SqlSessionFactoryBean、MapperScannerConfigurer）
- Spring 整合 SpringMVC、父子容器关系
- 事务配置、全注解式开发、配置类拆分

## 5. Spring Boot

- 核心原理：起步依赖、自动配置原理（@SpringBootApplication、SPI 机制、条件注解）
- 配置管理：application.yml/properties、多环境配置、配置绑定 @ConfigurationProperties
- 常用 Starter：Web、Validation、Redis、RabbitMQ、MyBatis-Plus
- 内嵌服务器、打包部署（jar/war）
- 监控与运维：Actuator 端点、Spring Boot Admin
- 异常处理、拦截器、跨域配置的最佳实践

### 基础

**0. 项目搭建与配置**

- 快速创建 Spring Boot 项目（Spring Initializr / IDEA）
- `application.yml` / `application.properties` 配置文件
- 多环境配置：dev / test / prod 切换
- 常用 Starter 引入（web、validation、redis、mybatis-plus 等）

**1. Web 开发**

- `@RestController`、`@RequestMapping`、`@GetMapping`、`@PostMapping`
- 参数接收：`@RequestParam`、`@PathVariable`、`@RequestBody`、`@RequestHeader`
- 统一返回格式：`Result<T>` 封装
- 全局异常处理：`@RestControllerAdvice` + `@ExceptionHandler`
- 参数校验：`@Valid` + `@NotBlank` + `@Length` + `@Email`
- 日期处理：`@DateTimeFormat`、`@JsonFormat`
- 文件上传下载：`MultipartFile`
- 跨域配置：CORS

**2. 常用注解**

- `@Component`、`@Configuration`、`@Bean`
- `@Value`、`@ConfigurationProperties`（配置绑定）
- `@Transactional`（事务）
- `@Slf4j`（Lombok 日志）

**3. 分层架构**

- Controller / Service / Mapper / Entity 四层结构
- Service 接口 + 实现类的写法
- `@Service`、`@Autowired`、`@Resource`

**4. 项目规范**

- VO / DTO / Query / BO 分层
- MapStruct 对象转换（替代 BeanUtils）
- 自定义业务异常 + 错误码枚举
- 日志规范 + MDC 链路追踪 ID

**5. 接口文档**

- Knife4j / SpringDoc OpenAPI 配置与使用
- 常用注解：`@ApiOperation`、`@ApiModel`

**6. 单元测试**

- JUnit5 + Mockito
- `@SpringBootTest` 测试 Service 层

**7. 打包部署**

- jar 包 / war 包
- 外置 Tomcat 部署
- 启动脚本编写

**8. 日志**

- SLF4J + Logback 基本使用
- `log.info()`、`log.error()` 打日志
- 日志级别配置

### 进阶

#### 1. 自动配置原理（面试必问）

- `@SpringBootApplication` 注解拆解
  - `@SpringBootConfiguration` → `@Configuration`
  - `@EnableAutoConfiguration` → 自动配置核心
  - `@ComponentScan` → 包扫描
- SPI 机制：`spring.factories` 文件作用
- 条件注解：`@ConditionalOnClass`、`@ConditionalOnMissingBean`、`@ConditionalOnProperty`
- 自动配置流程：Spring Boot 启动时如何加载自动配置类
- **动手实践**：自己手写一个 Starter（比如 `my-spring-boot-starter`）
- Spring Boot 启动流程（完整 7 步）
- 监听器与事件机制（ApplicationListener、ApplicationEvent）
- Spring Boot Admin 监控面板

#### 2. Spring Boot 启动流程

- 启动流程：创建 SpringApplication → 运行 run 方法 → 准备环境 → 创建上下文 → 刷新上下文 → 启动完成
- 监听器机制：ApplicationListener、SpringApplicationRunListener
- 启动过程中如何扩展：CommandLineRunner、ApplicationRunner

#### 3. 配置管理深化

- 配置文件优先级：命令行 > 环境变量 > application.yml > application.properties
- 多环境配置：`application-dev.yml`、`application-prod.yml`、如何切换
- 配置绑定：`@ConfigurationProperties`、`@Value`、`@PropertySource`
- 配置元数据：`additional-spring-configuration-metadata.json`
- 类型安全的配置属性

#### 4. Web 开发深化

- Spring MVC 自动配置：DispatcherServlet、ViewResolver、消息转换器
- 拦截器 vs 过滤器：区别、执行顺序、适用场景
- 全局异常处理：`@RestControllerAdvice` + `@ExceptionHandler`
- 统一返回格式：Result 封装、全局响应体处理
- 参数校验：`@Valid` + `@NotBlank` + `@Length` + 自定义校验注解
- 日期处理：`@DateTimeFormat`、`@JsonFormat`、全局日期格式化
- 文件上传下载：MultipartFile、大文件分片上传
- 跨域配置：`@CrossOrigin`、全局 CORS 配置、网关层跨域

#### 5. 事务管理深化

- `@Transactional` 事务原理：AOP 动态代理
- 事务传播行为：7 种传播机制，重点掌握 REQUIRED、REQUIRES_NEW、NESTED
- 事务隔离级别：读未提交、读已提交、可重复读、串行化
- **事务失效的 10 种场景**（重点）：
  1. 方法不是 public 的
  2. 自调用问题（同类中方法调用）
  3. 异常类型不对（默认只回滚 RuntimeException）
  4. 捕获了异常没抛出
  5. 数据库引擎不支持事务（MyISAM）
  6. 多线程下事务不生效
  7. 没有被 Spring 管理
  8. 传播行为设置错了
  9. 大事务问题
  10. 嵌套事务踩坑
- 编程式事务：TransactionTemplate

## 6. Spring AI

- 核心概念：模型抽象、Prompt 模板、输出解析、对话记忆
- 模型对接：接入 OpenAI / 通义千问 / 豆包等主流大模型
- 高级特性：函数调用、多模态输入、流式输出
- 场景落地：智能问答、内容生成、结构化数据提取

### 大模型 API 接入

### 一、必须掌握（核心基础，开发天天用）

#### 1. Spring AI 核心概念与模型接入

- **Spring AI 是什么**：Spring 生态的 AI 开发框架，统一各种大模型 API

- **核心抽象**：`ChatModel`（聊天模型）、`Prompt`（提示词）、`ChatResponse`（响应）

- 接入主流大模型

  （国内推荐优先学）：

  - 通义千问（阿里云）
  - 豆包（字节火山）
  - DeepSeek
  - OpenAI（国外）

- 依赖引入 + 配置文件（API Key、模型名称、Base URL）

- 最简单的调用：`chatModel.call("你好")` 一句话调用

#### 2. Prompt 模板与结构化提示

- `PromptTemplate` 提示词模板

  ：动态填充变量

  Java

  ```java
  PromptTemplate template = new PromptTemplate("你是一个{role}，请回答：{question}");
  Prompt prompt = template.create(Map.of("role", "Java 专家", "question", "什么是多线程"));
  ```

- **System Prompt**：设定 AI 人设、角色、输出规范
- **消息列表**：`SystemMessage` + `UserMessage` + `AssistantMessage` 构造多轮对话
- **提示词设计基础**：清晰指令、角色设定、示例引导（Few-shot）

#### 3. 输出解析

- **`BeanOutputConverter`**：把 AI 返回的 JSON 转成 Java 对象
- **`ListOutputConverter`**、`MapOutputConverter` 等常用解析器
- 结构化输出：让 AI 按指定格式返回数据（比如 JSON、列表）

#### 4. 流式输出（SSE）

- 为什么需要流式：提升用户体验，不用等全部生成完
- `chatModel.stream()` 流式 API
- **SSE（Server-Sent Events）**：后端向前端流式推送
- 前端配合：`EventSource` 或 `fetch + ReadableStream`
- 流式响应的异常处理

#### 5. 对话记忆（多轮对话）

- **`ChatMemory`**：对话记忆接口
- **`MessageChatMemory`**：内存版对话历史
- 记忆管理：保留最近 N 条、Token 限制、对话清空
- 多用户对话隔离（按用户 ID 存不同的 ChatMemory）

------

### 二、建议掌握（做项目必备）

#### 6. Embedding 与向量检索

- **`EmbeddingModel`**：文本向量化模型接口

- 接入 Embedding 模型：通义 embedding、BGE、M3E

- 向量相似度计算（余弦相似度）

- 向量数据库集成

  ：

  - Redis Vector（简单方便）
  - Milvus（专业、高性能）
  - Chroma / Pinecone（了解）

- 基本操作：向量写入、相似度查询、Top-K 召回

#### 7. RAG 检索增强生成

- RAG 完整流程：文档加载 → 文本分块 → 向量化 → 存储 → 检索 → 生成

- 文档加载器

  ：

  - PDF 解析（Apache PDFBox、Tika）
  - Word 解析（Apache POI）
  - Markdown / TXT 解析

- **文本分块**：固定大小分块、按标题分块

- **检索 + 生成**：把召回的上下文拼到 Prompt 里，让大模型基于文档回答

- **引用溯源**：返回答案引用了哪些文档片段

#### 8. 多模态能力

- 图片理解

  ：多模态模型调用（qwen-vl、gpt-4o）

  - 图片输入方式：URL、Base64
  - 应用：图片描述、OCR、图表分析

- 图片生成

  ：调用文生图 API（通义万相、DALL·E）

  - 提示词工程（图片生成的提示词技巧）
  - 图片保存与返回

#### 9. 函数调用 / 工具调用（Function Calling）

- 什么是 Function Calling：让 AI 调用你写的 Java 方法
- Spring AI 函数调用方式：
  - `@Description` 注解定义工具函数
  - 函数注册到 ChatModel
  - 函数调用流程：AI 决定调用 → 执行函数 → 结果返回给 AI → AI 生成最终答案
- 实用工具例子：
  - 查询数据库（查订单、查用户信息）
  - 调用外部 API（查天气、查快递）
  - 计算器、时间工具

#### 10. Token 计数与成本控制

- Token 计费原理：输入 + 输出都收费
- 成本估算：不同模型的价格对比
- 成本优化手段：
  - 选合适的模型（简单问题用便宜模型）
  - 控制上下文长度（摘要压缩历史对话）
  - 缓存常用问答

------

### 三、进阶了解（高级玩法 / 面试加分）

#### 11. AI Agent 开发

- Agent 核心组成：大模型 + 工具调用 + 记忆 + 规划
- ReAct 模式（推理 + 行动）
- Spring AI Agent 模块
- 多 Agent 协作：多个 AI 分工合作
- 任务规划（Task Planner）

#### 12. RAG 高级优化

- **重排序（Rerank）**：用 Rerank 模型对召回结果二次排序
- **混合检索**：关键词检索（BM25）+ 向量检索融合
- **查询改写**：用大模型改写用户问题，提升召回率
- **多轮对话 RAG**：历史对话 + 查询重构
- 知识库增量更新、版本管理
- RAG 效果评估（召回率、准确率）

#### 13. 模型路由与降级

- 多模型管理：根据任务难度选择不同模型
- 故障降级：一个模型挂了自动切另一个
- 负载均衡：多个 API Key 轮询使用

#### 14. 安全与合规

- 输入校验：防止 Prompt 注入攻击
- 输出审核：敏感词过滤、内容安全
- 数据隐私：敏感数据不上传大模型
- 调用日志与审计

#### 15. 高级特性

- **结构化输出**：JSON Schema 约束输出格式
- **图像生成高级**：批量生成、风格控制
- **语音能力**：TTS 语音合成、ASR 语音识别
- **视频生成**：异步任务处理、状态轮询

## 代码规范

**完整链路请求：**  SpringBoot 接收请求 → 参数校验 → 登录鉴权 (JWT) →会话管理 → 业务逻辑 → 调用数据库 → 返回结果，全局异常处理→ 返回前端渲染

#### 1. 分层架构

- 标准分层：Controller → Service → Mapper → Entity
- 扩展分层：
  - VO（View Object）：返回给前端的对象
  - DTO（Data Transfer Object）：前端传入的对象
  - BO（Business Object）：业务对象
  - Query：查询条件对象
  - Convert：对象转换工具
- **为什么需要分层？** 解耦、易维护、可复用、安全（不暴露数据库字段）

#### 2. 工具类与规范

- **Lombok**：`@Data`、`@NoArgsConstructor`、`@AllArgsConstructor`、`@Builder`、`@Slf4j`
- **Hutool**：字符串、日期、文件、加密、验证码等工具
- **MapStruct**：实体转换，替代 BeanUtils（高性能，编译期生成代码）
- **Hutool 工具类使用练习**：至少掌握 20 个常用工具方法

#### 3. 异常处理规范

- 自定义业务异常：`BusinessException`
- 错误码枚举：`ErrorCodeEnum`（统一管理错误码）
- 全局异常捕获：分类处理业务异常、参数校验异常、系统异常
- 异常日志规范：什么级别打什么日志

#### 4. 日志规范

- SLF4J + Logback 配置
- 日志级别：TRACE、DEBUG、INFO、WARN、ERROR
- 日志格式规范：时间、级别、线程、类名、消息
- 敏感信息脱敏：手机号、身份证、密码不能明文打日志
- MDC 日志链路追踪：TraceId 贯穿全链路

# 第三方框架

## 一、安全权限框架（核心：Spring Security）

安全权限框架（核心：Spring Security）

Spring Security（企业首选认证授权框架）

Sa‑Token

### 接口文档 Knife4j / SpringDoc（3 天）

- Knife4j 配置与使用
- 常用注解：`@Api`、`@ApiOperation`、`@ApiModel`、`@ApiModelProperty`
- JWT 调试配置
- 离线文档导出
- SpringDoc OpenAPI3（Spring Boot 3 推荐）

## 二、工具 & 通用集成框架

**1. Hutool / Apache Commons**

通用工具包，封装字符串、日期、加密、文件、验证码、UUID 等，减少重复工具类。

**2. Lombok**

简化实体类代码，@Data/@NoArgsConstructor 等，消除 get/set/ 构造器，所有后端项目必引。

**3. MapStruct**

实体转换框架（DTO↔Entity↔VO），替代手动 set，高性能无反射，企业规范必备。

**4. Validation（Jakarta Validation / Hibernate Validator）**

参数校验框架，@NotBlank/@Length/@Email 等注解，你 AI 项目参数校验依赖此框架。

**5. EasyExcel / POI**

Excel 导入导出：

- POI：传统，内存易 OOM；
- EasyExcel：阿里开源，低内存百万级读写，项目报表导出首选。

**6. POI-TL Word 模板导出**

基于模板填充 Word 合同、报表，政务 / 电商后台常用。

**7. Caffeine（本地缓存）**

JVM 进程内高性能缓存，搭配 Redis 二级缓存，减轻中间件压力。

## 三、定时任务 & 异步框架

**1. Spring Task（Spring 内置）**

简单定时，@Scheduled，轻量单体项目够用；缺点不支持分布式。

**2. XXL-JOB（分布式定时调度）**

替代原生定时、替代阿里 SchedulerX，可视化后台、任务分片、失败重试、日志追踪，微服务通用。

**3. Quartz**

老牌定时框架，支持持久化任务，复杂度高，现在大多被 XXL-JOB 替代。

**4. Spring Async**

@Async 异步线程池，解耦耗时操作（日志保存、消息推送、文档向量化，AI 项目大量使用）。

## 四、文件、图片、多媒体处理框架

1. MinIO：私有对象存储，对标 OSS，本地私有化文件存储，无需依赖云厂商。
2. Hutool-Img / Thumbnails：图片压缩、水印、裁剪。
3. Tika：文档解析 PDF/Word/TXT，RAG 知识库文档上传解析必备（AI Agent 项目）。
4. FFmpeg 工具包：视频转码、音频提取，AIGC 多媒体项目使用。

## 五、数据库增强框架

1. PageHelper：纯 Mybatis 分页插件（无 MP 时使用）
2. Sharding-JDBC：分库分表、读写分离，海量数据场景
3. Flyway / Liquibase：数据库版本管理，自动执行 SQL 脚本，团队协同开发必备

## 六、接口文档 & 前后端协作

1. Knife4j（增强 Swagger）：SpringBoot 主流，UI 美观、支持 JWT 调试、导出文档
2. SpringDoc OpenAPI3：新一代 Swagger，兼容 Spring6/3
3. ApiPost / Postman：客户端工具，非代码框架

## 七、监控、链路、运维框架

**1. Spring Boot Actuator**

项目内置监控端点：健康、内存、线程池、接口指标，配合监控面板使用。

**2. Spring Boot Admin**

可视化监控面板，统一查看所有服务健康、日志、JVM 状态。

**3. SkyWalking / Pinpoint / Sleuth+Zipkin**

分布式链路追踪：记录完整调用链、耗时、异常，微服务排查瓶颈必备； 搭配 EFK 日志形成完整观测体系。

**4. Prometheus + Grafana**

时序指标监控，采集 QPS、响应时间、错误率，配置告警。

## 八、消息队列配套封装

1. Spring Cloud Stream：统一消息抽象，一套代码切换 RocketMQ/RabbitMQ/Kafka
2. RabbitTemplate / RocketMQTemplate：Spring 原生消息封装模板

## 九、AI 相关配套框架（AI Agent 项目专用）

1. Spring AI：对接各大 LLM、向量库、RAG、Function Call（你的 AI 项目核心）
2. LangChain4j：Java 版 LangChain，知识库、Agent 工具调用
3. Milvus SDK / RedisSearch：向量数据库操作框架

## 十、测试框架（开发 / 单元测试必备）

1. JUnit5：单元测试基础
2. Mockito：Mock 依赖，隔离数据库 / 中间件
3. TestContainers：本地启动 MySQL/RocketMQ 容器做集成测试
4. RestAssured：接口自动化测试 
5. jmeter压力测试

## 十一、防重复、限流、灰度专用

1. Sentinel：阿里流量防护，限流熔断（微服务标配）
2. Guava RateLimiter：单机限流
3. 分布式防重框架：基于 Redis 注解封装

## 十二、短信、邮件、第三方对接

1. Spring Mail：项目邮件发送（告警、验证码）
2. 阿里云 / 腾讯云 SMS SDK：短信验证码、通知
3. 微信开放平台 SDK：小程序、公众号登录推送

# 微服务理论

**1）接入层（流量入口）**

- 四层负载均衡：LVS / F5 / Keepalived
- 七层网关：Nginx、OpenResty、Spring Cloud Gateway、Kong、Ingress‑Nginx

> 能力：负载均衡、SSL、限流、灰度、路由、鉴权、WAF 防火墙

**2）缓存层（多级缓存）**

- 本地缓存：Caffeine、Guava Cache
- 分布式缓存：Redis

> 解决热点、降低 DB 压力；问题：穿透、击穿、雪崩、一致性

**3）消息中间件（异步通信）**

- RocketMQ、Kafka、RabbitMQ、Pulsar

> 能力：异步、解耦、削峰、事件通知；关注点：丢失、重复、积压、顺序

**4）服务治理（微服务）**

- 注册发现：Nacos / Eureka / Consul
- 配置中心：Nacos / Apollo / Spring‑Cloud‑Config
- RPC 调用：OpenFeign、Dubbo、gRPC
- 熔断降级：Sentinel、Hystrix

**5）存储层**

- 关系库：MySQL（主从、分库分表 Sharding‑JDBC）
- NoSQL：Redis、MongoDB
- 对象存储：MinIO、OSS

**6）可观测组件（监控链路日志）**

- 链路追踪：SkyWalking、Pinpoint、Jaeger（TraceId）
- 监控告警：Prometheus + Grafana
- 日志系统：EFK（Elasticsearch + Filebeat + Kibana）

**7）安全组件**

- 认证授权：Spring‑Security、Sa‑Token、OAuth2
- 接口水印、敏感词过滤、WAF、防重幂等

**8）定时任务**

- XXL‑Job、Spring‑Task、Elastic‑Job

## 什么是微服务

1. **通用设计流程** 澄清需求 → 规模估算 → 顶层架构 → 细化组件 → 瓶颈优化 → 容错扩容
2. 核心理论
   - CAP 定理：一致性、可用性、分区容错性三者不可兼得
   - BASE 理论：基本可用、软状态、最终一致性
   - 水平扩展 vs 垂直扩展
3. 核心组件
   - 负载均衡：作用、常见算法（轮询、加权、IP 哈希、一致性哈希）
   - 消息队列：三大作用（解耦、异步、削峰）；常见产品对比
   - 数据库：分库分表策略、读写分离、索引优化
   - 分布式缓存：Redis 集群架构、缓存分层
4. 经典题型要点
   - 短链接系统：发号器、哈希映射、301/302 重定向
   - 限流系统：令牌桶、漏桶、固定窗口、滑动窗口
   - 秒杀系统：限流、削峰、库存防超卖、防重复提交
   - 分布式 ID：雪花算法、号段模式、数据库自增
5. **分布式事务**：2PC、3PC、TCC、最终一致性方案

```
1 CAP、BASE 理论详细推导，什么时候选 CP/AP
2 一致性算法：Raft（Nacos 底层）简单原理
3 分布式 ID 全套方案：雪花算法、号段、Redis 自增、数据库自增优缺点对比
4 分布式会话三种实现：Redis 共享、session 同步、JWT 无状态
5 分布式锁完整方案：Redis 锁、Zookeeper 锁、数据库锁优缺点
6 分布式定时任务对比：SpringTask/XXL-Job/SchedulerX
7 接口幂等全套实现：唯一索引、Redis 令牌、状态机、防重 Token
```

## SpringCloudAibaba 

1. Nacos
   - 服务注册发现：服务提供者 / 消费者接入、服务心跳、健康检查
   - 动态配置中心：配置管理、命名空间 / 分组 / DataId、配置热更新、灰度发布
   - 集群部署、数据持久化、AP+CP 模式切换
2. Sentinel
   - 流量控制：QPS / 线程数限流、流控模式（直接 / 关联 / 链路）、冷启动 / 匀速排队
   - 熔断降级：慢调用比例、异常比例、异常数策略
   - 热点参数限流、系统自适应保护、授权规则
   - 持久化规则、与 Feign/Gateway 整合
3. Dubbo
   - RPC 架构：服务提供者 / 消费者、注册中心、监控中心
   - 调用流程、负载均衡策略、集群容错机制
   - 序列化方式、超时与重试、服务分组与版本
4. RocketMQ
   - 核心架构：NameServer/Broker/Producer/Consumer
   - 消息发送：同步 / 异步 / 单向、批量消息、顺序消息、延迟消息
   - 消息消费：集群 / 广播模式、消息重试、死信队列
   - 事务消息、消息幂等性、消息可靠性保证
5. Seata
   - 分布式事务理论：CAP、BASE、柔性事务
   - 四大模式：AT（自动补偿）、TCC、SAGA、XA 模式对比与适用场景
   - AT 模式落地：TC/TM/RM 角色、一阶段提交 / 二阶段回滚、undo_log
   - 常见问题：脏写、悬挂、空回滚
6. Alibaba Cloud OSS
   - 对象存储 SDK 集成、服务端签名直传、图片处理
   - 权限控制、生命周期管理、跨域配置
7. Alibaba Cloud SchedulerX
   - 分布式定时任务、Cron 表达式、任务分片
   - 失败重试、任务监控、工作流编排
8. Spring Cloud Gateway
   - 核心概念：路由、断言、过滤器
   - 路由配置：路径断言、参数 / 请求头断言、动态路由
   - 过滤器：局部 / 全局过滤器、鉴权、日志、限流、跨域处理
   - 集成 Sentinel 网关限流、集成 Nacos 服务发现

**补充微服务核心知识**

- OpenFeign：远程调用、参数传递、超时配置、日志配置、与 Sentinel 整合降级
- 服务调用链路：OpenFeign + LoadBalancer 负载均衡
- 分布式问题：分布式锁、分布式 ID、分布式缓存、分布式会话
- 微服务架构设计：服务拆分原则、领域驱动设计入门、接口设计规范

## 分布式事务

> 问题：跨服务 / 多数据库，保证数据一致性。分**强一致、最终一致**。

### 1）2PC（两阶段提交 XA）

- 准备阶段：协调者让参与者执行事务，不提交，返回 ok/fail
- 提交阶段：全 ok 就提交；有失败就全部回滚 ✅优点：强一致，业务侵入小 ❌缺点：同步阻塞、协调者单点、存在数据不一致风险、性能差 👉适用：少量数据库，低并发，**生产很少直接用**

### 2）3PC

CanCommit→PreCommit→DoCommit，解决部分阻塞；依然有一致性问题，几乎不用。

### 3）TCC Try‑Confirm‑Cancel

- Try：校验、预留资源
- Confirm：确认执行业务
- Cancel：补偿回滚释放资源 ✅优点：最终一致，性能较好 ❌缺点：**业务侵入大，要手写补偿代码** 👉适用：金融、资金扣减；框架：Hmily、ByteTCC

### 4）Seata AT（互联网最常用）

一阶段本地提交，记录 undo_log 快照；二阶段根据 undo_log 提交 / 回滚 ✅优点：几乎零业务侵入 ❌缺点：隔离级别有限，生成 undo_log 👉适用：微服务普通业务

### 5）SAGA

大事务拆成多个本地子事务，每个事务配补偿操作；失败反向执行补偿回滚 👉适用：长流程业务，订单履约

### 6）本地消息表 / RocketMQ 事务消息

业务 + 消息落库，异步重试投递，**最终一致性** 👉适用：异步场景，积分、通知，不能做强一致

**选型口诀**

> 强一致选 2PC；金融资金选 TCC；普通微服务选 Seata‑AT；长流程选 SAGA；异步解耦选事务消息。

------

## 分布式锁

> 问题：多服务实例争抢共享资源，保证同一时刻只有一个线程执行。核心：互斥、防死锁、可重入、防误删。

### 1.Redis（Redisson，主流）

加锁：`SET key val NX EX`；释放用 Lua 脚本。 Redisson：看门狗自动续期、可重入锁、公平锁、读写锁。 ❌问题：主从切换锁丢失；锁超时业务未跑完。

### 2.Zookeeper(Curator)

创建临时有序节点，最小节点获得锁，监听前序节点；会话断开自动释放锁。 ✅可靠性高；❌性能一般。

### 3. 数据库

- 悲观锁 `select for update`：阻塞，高并发差
- 乐观锁 version 版本号：无阻塞，读多写少
- 唯一索引：插入冲突代表抢锁失败

### 4.etcd

租约 lease，云原生 k8s 场景。

**选型口诀**

> 高并发业务优先 Redisson；追求可靠性选 ZK；无中间件用数据库乐观锁；k8s 用 etcd。 常见坑：锁超时、锁误释放、主从丢锁、不可重入。

------

## 分布式缓存

> 作用：减轻 DB 压力，加速热点数据。常用：Redis、Memcached；架构：本地缓存 + 分布式缓存两级（Caffeine+Redis）

### Redis 部署模式

单机 → 主从 → Sentinel 哨兵（自动故障转移）→ Redis‑Cluster 分片集群

### 四大缓存问题

1. **缓存穿透**：查询不存在的数据 解决：布隆过滤器、缓存空值设置短过期时间
2. **缓存击穿**：热点 key 过期，大量请求打 DB 解决：互斥锁、热点 key 永不过期
3. **缓存雪崩**：大量 key 同时过期 / Redis 宕机 解决：过期时间加随机值、集群高可用、限流降级
4. **缓存 DB 一致性** 方案：更新库删缓存、延迟双删、Canal 监听 binlog 更新缓存

### 组件对比

- Redis：支持丰富数据结构、持久化、集群，工业首选
- Memcached：简单 KV，无持久化，现在少用
- Caffeine：高性能本地缓存，和 Redis 组成两级缓存；缺点多实例本地缓存不一致

**选型口诀**

> 通用业务：Caffeine 本地缓存 + Redis‑Cluster 两级缓存。

# AI应用

## 1. Agent 开发与使用

- 核心组成：大模型大脑 + 规划能力 + 工具调用 + 记忆模块
- 典型框架：LangChain / LlamaIndex / Spring AI Agent
- 核心能力：多轮对话、工具调用（API / 数据库 / 搜索引擎）、任务拆解、自主规划
- 落地场景：智能客服、代码助手、数据分析 Agent、工作流自动化
- 什么是 Agent：大模型 + 工具调用 + 记忆 + 规划
- Agent 的核心能力：
  - 工具调用（Function Calling）
  - 任务规划（Planning）
  - 记忆（Memory）
  - 反思（Reflection）
- **函数调用**什么是 Function Calling：让大模型调用你提供的工具Spring AI 函数调用：`@Description` 注解定义工具工具注册与调用流程**动手实践**：给 AI 加几个工具（查天气、查数据库、调用计算器）
- 多 Agent 协作：多个 Agent 分工合作

## 2. AIGC 多模态生成

- 文本生成：文案写作、摘要提取、代码生成、结构化输出
- 图片生成：Stable Diffusion / 文心一格 / DALL・E 调用、参数调优
- 视频生成：文字生成视频、数字人播报、视频脚本生成
- 音频生成：语音合成（TTS）、语音识别（ASR）、声音克隆
- 工程落地：API 调用封装、流式响应、内容审核、成本优化

## 3. 向量数据库

- 核心概念：向量嵌入（Embedding）、相似度检索、向量索引
- 主流产品：Milvus、Pinecone、Chroma、Redis Vector
- 核心操作：向量写入、相似度查询、元数据过滤、索引构建
- 应用场景：语义检索、推荐系统、图像检索、RAG 知识库

## 4. RAG 检索增强生成

- 完整流程：文档加载 → 文本分块 → 向量化 → 向量存储 → 检索召回 → 大模型生成
- 关键优化：分块策略、召回率优化、重排序（Rerank）、多轮对话上下文
- 工程落地：知识库管理、增量更新、引用溯源、问答效果评估
- 常见框架：LangChain RAG、LlamaIndex、Spring AI RAG 实现

## 5. 大模型运作原理

- 数据处理流程：原始文本 → 清洗 → 分词（Tokenizer）→ 词表构建
- 表示层：词嵌入（Embedding）、位置编码、语义向量空间
- 核心架构：Transformer 结构、自注意力机制（Self-Attention）、多头注意力
- 训练过程：预训练（语言建模）→ 监督微调（SFT）→ 人类反馈强化学习（RLHF）
- 推理过程：输入编码 → 注意力计算 → 逐词生成 → 概率采样
- 核心概念：上下文窗口、幻觉、温度系数、Top-P/Top-K 采样

## 6.RAG智能客服实战平台

做一个完整的 AI 应用项目，包含以下功能：

- [ ] 智能对话：流式对话、多轮对话、对话历史
- [ ] 图片生成：文生图、图片保存、我的作品
- [ ] 图片理解：上传图片提问
- [ ] 视频生成：文生视频、异步任务、状态查询
- [ ] 知识库问答：
  - [ ] 文档上传（PDF/Word/Markdown）
  - [ ] 文档解析与向量化
  - [ ] 语义检索问答
  - [ ] 引用溯源
- [ ] 我的助手：创建自定义 AI 助手（设定人设、提示词）
- [ ] 用户系统：注册登录、积分/额度管理
- [ ] 管理后台：用户管理、模型配置、知识库管理

# 面经

## 核心

- **八股背诵**：Java 基础、JVM、并发、MySQL、Redis、Spring、微服务、计算机网络分模块梳理
- **算法刷题**：按题型分类突破，保持每日 2-3 题手感
- **项目深挖**：每个项目准备技术选型理由、架构图、难点解决方案、性能优化成果
- **场景题**：分布式事务、缓存一致性、秒杀、限流、高可用等经典场景题准备
- **HR 面**：自我介绍、职业规划、离职原因、优缺点、薪资谈判等常规问题准备

## 进阶

### 设计模式

- **单例模式**：饿汉式、懒汉式、双重校验锁、静态内部类、枚举实现；线程安全问题
- **工厂模式**：简单工厂、工厂方法、抽象工厂；适用场景差异
- **策略模式**：封装算法族，运行时可替换
- **观察者模式**：发布 - 订阅模型，事件通知机制
- **装饰器模式**：动态扩展对象功能，继承的替代方案
- **适配器模式**：接口兼容，新旧系统对接
- **代理模式**：控制对象访问，静态代理 vs 动态代理
- 要求：能画出 UML 简图，说出核心思想与典型应用场景

### 线上故障复盘模板

缓存雪崩、消息堆积、数据库慢 SQL、服务 OOM、大模型超时故障等完整复盘思路

**回答方案**

1. **故障现象**（用户侧、监控侧、时间）
2. **影响范围**（用户数、接口、时长、降级与否）
3. **根因定位**（表层原因 + 真实代码 / 架构根因）
4. **临时止血方案**（当时怎么快速恢复）
5. **长期根治方案**（架构 / 代码 / 配置优化）
6. **预防机制 + 监控兜底**

**例子**

```
# 消息队列（RocketMQ）消息堆积故障・详细完整复盘

> 
> 场景背景：电商订单业务，订单完成后投递 RocketMQ 消息，消费者处理：更新订单状态、发送短信通知、调用物流第三方接口推送物流信息。
> 环境：SpringBoot + RocketMQ，消费者单实例，单线程消费。

## 1. 故障现象

1. 监控告警触发：RocketMQ Broker 该 Topic 消息堆积量持续上涨，短时间堆积**14.6 万条消息**。
2. 消费 TPS 只有 8‑12 条 /s，生产 TPS 稳定在 80‑100 条 /s，消费速度远跟不上生产速度。
3. 用户侧现象：订单已经支付完成，但是**订单状态长时间不更新、短信通知延迟 10 分钟～30 分钟才收到**，部分用户反馈收不到短信。
4. RocketMQ 控制台显示：consumer lag 持续走高，没有回落趋势。

## 2. 影响范围

1. 业务模块：订单状态更新、用户短信通知、物流推送全部受影响。
2. 用户：全部下单用户，高峰期约 8000 + 用户受到延迟影响。
3. 故障持续时长：从发现告警到消息全部排空，共计 32 分钟。
4. 无数据丢失，只是业务处理延迟，没有资金资损。

## 3. 根因定位（排查过程 + 表层 + 根本）

### 排查过程

1. 查看 RocketMQ 控制台：生产正常，堆积持续上涨；消费 TPS 很低。
2. 查看消费者服务日志：每条消息消费耗时平均 **800‑1200ms**，大量耗时卡在调用外部物流第三方 HTTP 接口。
3. 当前配置：**单实例、消费线程数 5，没有开启批量消费，每次只消费 1 条消息**。

- **表层原因**：单条消息消费耗时长，整体消费能力不足，跟不上生产速率，消息持续堆积。
- **根本原因**
  1. 消费业务逻辑里面**同步调用外部第三方物流接口**，第三方接口网络波动，响应不稳定，单次调用耗时最高可达 1.5s；MQ 消费者线程被同步 IO 阻塞。
  2. 未开启批量消费，每次只处理 1 条消息，吞吐上限低。
  3. 消费者集群只部署 1 个实例，没有水平扩容；线程池配置偏小。
  4. 第三方调用没有超时控制，个别慢请求占住消费线程，进一步压低整体消费速度。

> 
> 伪代码还原问题消费代码

​```
// 有问题的消费代码
@Override
public void consumeMessage(MessageExt message) {
    OrderDTO order = parseMsg(message);
    orderService.updateOrderStatus(order);
    // 同步调用第三方物流接口，阻塞消费线程！
    logisticsFeign.pushLogisticsInfo(order); 
    smsService.sendSms(order);
}
​```

## 4. 临时止血（线上紧急操作，按执行顺序）

> 
> 目标：快速提升消费能力，尽快把堆积消息消化掉，恢复业务时效性。

1. **扩容消费者实例**：把消费者从 1 实例扩容到 4 实例，提升消费线程总数，消费 TPS 从 10 提升至 45 左右。
2. **临时降级第三方同步调用**：修改配置开关，消费阶段不再同步调用物流接口；把物流推送任务转存到本地数据库任务表，先保证订单状态更新、短信通知快速执行，解除消费线程阻塞。
3. **暂停非优先级 Topic 生产者**：关闭运营活动产生的非核心消息，避免 Broker 压力继续增大，优先保障订单 Topic 资源。
4. 业务侧启动**后台补偿 Job**：读取数据库任务表，异步分批调用物流第三方接口，补推堆积的物流数据。
5. 持续观察 RocketMQ 控制台 lag 指标，等待堆积消息逐步下降至 0。

> 
> 执行效果：操作完成约 20 分钟后，Topic 堆积清零，用户短信、订单状态恢复正常；物流数据由补偿 Job 慢慢补完。

## 5. 长期根治优化方案（代码 + 配置 + 架构改造，可落地）

### 5.1 改造消费逻辑，消除消费内部同步第三方调用

> 
> MQ 消费逻辑原则：**消费者内部不做耗时的同步外部 IO**。
> 改造方案：

1. MQ 消费者只做快速本地业务：更新订单状态；**不再同步调用物流接口**。
2. 消费者处理完成后，发送一条新的「物流推送事件」到另一个独立 MQ Topic `topic‑logistics‑push`。
3. 单独的消费者消费物流 Topic，专门处理调用第三方物流接口；与主订单消费逻辑物理隔离。

伪代码改造后：

​```
// 订单主消费者（轻量，快速完成）
public void consumeMessage(MessageExt message) {
    OrderDTO order = parseMsg(message);
    orderService.updateOrderStatus(order);
    smsService.sendSms(order);
    // 投递新消息，把慢操作剥离出去
    rocketMQProducer.send("topic‑logistics‑push", order);
}
// 独立消费者专门处理物流推送，慢逻辑隔离
​```

### 5.2 开启 RocketMQ 批量消费，提升吞吐

消费者配置开启批量拉取，一次拉取多条消息处理。

​```
# RocketMQ消费者配置
consumer.pullBatchSize=32
​```

> 
> 注意：业务要保证单条消费逻辑幂等，批量消费会增大重复消费概率。

### 5.3 资源隔离：慢消费链路独立线程池

物流推送消费者配置独立线程池，设置合理超时时间，第三方调用增加超时、熔断（Sentinel），第三方慢不会拖垮其他业务。

- Feign 调用设置 connectTimeout=500，readTimeout=1000；
- 第三方接口异常不阻塞线程，失败进入重试死信队列。

### 5.4 死信队列处理

消费失败的消息进入死信 DLQ，不要无限重试阻塞消费线程；人工可以对死信消息做重放。

### 5.5 集群部署约束

核心业务消费者至少部署 2 个以上实例，避免单点。

## 6. 预防与监控告警体系

### 6.1 监控指标采集（接入 Prometheus + Grafana）

监控如下指标：

1. Topic 消息堆积量 `consumer_lag`
2. 消费 TPS、生产 TPS
3. 单条消息平均消费耗时、最大消费耗时
4. 死信队列消息数量

### 6.2 告警阈值配置

1. 堆积 lag > 5000：发送邮件、钉钉警告告警；
2. 堆积 lag > 30000：电话紧急告警，需要人工介入；
3. 单条消费平均耗时 >500ms 触发告警，提前发现消费变慢苗头；
4. 死信队列消息大于 100 立即告警。

### 6.3 运维预案

1. 制定预案文档：消息堆积的标准处理步骤：扩容实例、剥离慢逻辑、暂停非核心流量、死信重放、数据补偿。
2. 自动弹性扩容：接入 K8s HPA，当 lag 超过阈值自动扩容消费者 Pod，一定程度实现自动抗堆积。

## 7. 事后复盘总结（会议纪要片段，面试可直接复述）

1. MQ 消费者中**严禁同步调用耗时高的第三方接口**，外部 IO 要二次投递 MQ 做隔离，否则极易造成消费阻塞堆积。
2. 不能只关注消息丢失，同样要重视消费耗时指标；消费耗时上涨是堆积的前兆。
3. 核心业务消费者不能单点部署；配置死信队列，避免失败消息无限重试占满线程。
4. 监控不能只看堆积数量，消费耗时、死信数量也是重要预警信号。

> 
> 常见踩坑补充：
> ① 不要盲目无限扩容实例，如果消费逻辑本身很慢，扩容收益有限，**优先优化消费逻辑**；
> ② 堆积消息不要直接删除，删除会造成业务数据丢失，优先排空、补偿。
```



### 基础设计方法论

需求梳理 → 流量存储估算 → 分层架构 → 瓶颈分析 → 容灾扩容

### 高频系统设计题

1 短链接系统设计

 2 高并发秒杀系统 

3 统一网关平台

 4 分布式日志 EFK 平台 

5 限流系统 

6 海量用户 IM 聊天

7 RAG 知识库平台

### 通用架构组件

1 负载均衡四层 / 七层区别 

2 多级缓存：本地 Caffeine + Redis 分布式缓存 

3 消息队列削峰、异步解耦落地方案 

4 限流四大算法：固定窗口、滑动窗口、漏桶、令牌桶

## 计算机基础

**1. 计算机组成原理**

1. CPU：寄存器、ALU、缓存 L1/L2/L3、缓存行、缓存失效、伪共享
2. 内存：虚拟内存、分页、页表、缺页中断、交换分区
3. IO 模型：阻塞 IO、非阻塞 IO、IO 多路复用 (select/poll/epoll)、异步 IO、Java NIO 底层对应关系
4. 存储层级：寄存器 > 高速缓存 > 内存 > 磁盘 > 网络，速度差距与程序优化思路

**2. 计算机网络**

1. TCP 详解：三次握手、四次挥手、滑动窗口、拥塞控制、超时重传、粘包拆包、Nagle 算法
2. UDP：适用场景，和 TCP 对比
3. HTTP1.1 / HTTP2 / HTTP3 区别：多路复用、头部压缩、QUIC
4. HTTPS 完整流程：TLS 握手、非对称加密 + 对称加密、CA 证书、中间人攻击
5. DNS 递归 / 迭代查询、CDN 原理
6. Socket 编程：TCP 服务端 / 客户端简易实现
7. 网络安全：XSS、CSRF、SQL 注入、DDOS 攻击防护
8. 网络模型：OSI 七层、TCP/IP 四层；各层作用与代表协议
9. TCP vs UDP：核心差异、优缺点、适用场景
10. TCP 可靠性：三次握手、四次挥手、序列号、确认应答、超时重传
11. TCP 拥塞控制：慢启动、拥塞避免、快重传、快恢复
12. HTTP/HTTPS：区别、HTTPS 加密流程（对称 + 非对称 + 数字证书）
13. HTTP 基础：常见状态码、请求方法、请求头 / 响应头作用
14. DNS 解析：从域名到 IP 的完整解析流程

**3. 操作系统（面试高频）**

1. 进程、线程、协程三者本质区别、切换开销
2. 进程调度算法、内存管理（分页 / 分段）
3. 文件系统：inode、块、软硬链接
4. Linux 高频命令：网络 (netstat/ss/tcpdump)、磁盘 (df/du)、进程 (top/htop)、日志 (grep/tail)
5. 信号、孤儿进程、僵尸进程
6. 用户态 / 内核态切换原理
7. 进程 vs 线程：定义、资源分配、调度、开销的本质区别
8. 进程通信（IPC）：管道、消息队列、共享内存、信号量、Socket
9. 线程同步互斥：互斥锁、信号量、条件变量、读写锁
10. 死锁：4 个必要条件；死锁的预防、避免、检测与解除
11. 延伸：协程的概念，与线程的差异；上下文切换的开销

**4. 缓存**

- 核心思想：空间换时间，降低下游负载
- 淘汰策略：LRU、LFU、FIFO、随机淘汰；**手写 LRU 缓存（哈希表 + 双向链表）**
- 三大缓存问题：穿透、击穿、雪崩；各自成因与解决方案
- 更新策略：Cache Aside、Write Through、Write Back 的区别与适用场景

## JVM

**1. 运行时数据区（内存模型）**

1. 五大区域：**程序计数器、虚拟机栈、本地方法栈、堆、元空间 Metaspace**
2. 线程私有 / 线程共享区分
   - 私有：程序计数器、虚拟机栈、本地方法栈
   - 共享：堆、元空间
3. 每个区域作用、会抛出什么异常
   - 程序计数器：**唯一没有 OOM 的区域**，记录字节码执行行号
   - 虚拟机栈：存放栈帧；递归过深抛出 `StackOverflowError`；线程过多会 OOM
   - 堆：对象实例，GC 主战场，分年轻代、老年代
   - 元空间：存储类信息、常量、静态变量；使用本地内存，替代 JDK8 之前永久代 PermGen
4. 栈帧组成：局部变量表、操作数栈、动态链接、方法返回地址
5. Java 对象内存布局：**对象头 (MarkWord、类型指针)、实例数据、对齐填充**
6. MarkWord 内容：hashCode、分代年龄、锁状态标记
7. 逃逸分析、栈上分配、标量替换：优化，对象分配到栈减少 GC

**2. 垃圾回收 GC**

**2.1 判断对象死亡**

1. 引用计数法：原理、**循环引用缺陷**，JVM 不用
2. 可达性分析（JVM 使用）
   - GC‑Roots 有哪些
   - 从 GC Roots 向下搜索，不可到达即为垃圾
3. 四种引用
   - 强引用、软引用 SoftReference（缓存）、弱引用 WeakReference（WeakHashMap）、虚引用 PhantomReference（堆外内存回收通知）

**2.2 堆分代模型**

1. 年轻代 Eden : S0 : S1 = 8:1:1，为什么需要两块 Survivor
2. 对象晋升老年代条件
   - 存活年龄达到 15
   - Survivor 放不下，分配担保
   - 大对象直接进入老年代
3. Minor GC / Major GC / Full GC 概念、触发条件、区别
4. 空间分配担保机制

**2.3 GC 算法**

- 标记‑清除：简单，产生内存碎片
- 标记‑复制：无碎片，需要预留内存
- 标记‑整理：无碎片，移动对象，STW 时间长

**2.4 垃圾收集器（高频）**

表格

| 收集器         | 特点                                              |
| -------------- | ------------------------------------------------- |
| Serial         | 单线程，全部 STW，客户端                          |
| Parallel GC    | 多线程，**吞吐量优先，JDK8 默认**                 |
| CMS            | 并发标记清除，低停顿；碎片多、浮动垃圾；JDK8 废弃 |
| G1 (JDK9 默认) | Region 分块，可预测停顿；Humongous 大对象区       |
| ZGC(JDK11+)    | 染色指针，几乎零 STW，支持 TB 级堆                |

1. CMS 四个阶段：初始标记 (STW)、并发标记、重新标记 (STW)、并发清除；浮动垃圾产生原因、CMS 缺点
2. G1 Region、Humongous 大对象
3. ZGC 染色指针、读屏障，为什么停顿极低
4. **STW Stop‑The‑World**：含义，哪些阶段会发生 STW

**3. 类加载机制**

1. 类加载 5 个阶段：

   加载 → 验证 → 准备 → 解析 → 初始化

   ，每个阶段做什么

   - 准备：静态变量赋**默认零值**
   - 初始化：执行静态代码块，赋值程序员写的值

2. 类加载器

   - Bootstrap、Extension、AppClassLoader、自定义类加载器

3. 双亲委派模型

   - 原理、好处
   - 破坏双亲委派的场景：Tomcat、SPI、自定义 ClassLoader

4. 类的初始化时机，什么时候会触发类加载

**4. JVM 参数**

1. 堆：`‑Xms`初始堆，`‑Xmx`最大堆；生产建议 Xms=Xmx
2. `‑Xmn`年轻代大小
3. 元空间：`‑XX:MetaspaceSize`、`‑XX:MaxMetaspaceSize`
4. OOM 自动 dump：`‑XX:+HeapDumpOnOutOfMemoryError`
5. GC 日志打印参数
6. 大对象阈值：`‑XX:PretenureSizeThreshold`

**5. OOM 各种场景（高频故障题）**

1. `Java heap space`：堆内存溢出，内存泄漏 / 对象过多
2. `Metaspace`：动态生成大量类
3. `Direct buffer memory`：堆外内存溢出 NIO
4. `Unable to create new native thread`：创建线程太多

> 区分：**内存泄漏 VS 内存溢出**

- 内存泄漏：无用对象仍持有引用，GC 无法回收，日积月累最终 OOM

**6. JVM 排查工具 & 线上故障（必问）**

**工具命令**

- jps：查看 Java 进程
- jstat ‑gc：实时查看 GC 统计
- jmap ‑dump：dump 堆快照
- jstack：打印线程栈，排查死锁、死循环、阻塞

> 可视化：MAT 内存分析工具；线上神器 Arthas

**故障排查流程**

1. CPU 飙高排查步骤
   1. top 拿到进程 pid
   2. top‑H 找到高消耗线程 tid
   3. tid 转 16 进制，jstack 输出栈定位代码
2. 频繁 FullGC 排查思路
   - jstat 看 GC 指标；dump 堆快照 MAT 分析
   - 可能原因：内存泄漏、对象快速晋升老年代、元空间满、人为调用 System.gc
3. OOM 故障复盘思路
4. 内存抖动：大量临时对象，频繁 MinorGC

## 并发

## 系统设计

#### 2.1 系统设计方法论

1. **需求澄清**：功能需求 + 非功能需求（QPS、DAU、延迟、可用性、数据量）
2. **顶层设计**：整体架构图、核心模块、数据流
3. **细化设计**：各模块详细设计、数据库设计、接口设计
4. **瓶颈分析**：性能瓶颈、单点故障、扩展性瓶颈
5. **优化方案**：缓存、异步、分片、扩容、降级

#### 2.2 必掌握的核心系统设计题

| 题目                 | 核心考察点                                                  |
| -------------------- | ----------------------------------------------------------- |
| 短链接系统           | 发号器（雪花/号段）、哈希冲突、301/302、数据存储、防刷      |
| 秒杀系统             | 限流、库存防超卖、异步削峰、防重复提交、热点隔离            |
| 设计 Twitter/微博    | feed 流（推/拉/推拉结合）、关注关系、点赞评论               |
| 设计聊天系统（IM）   | 长连接、消息投递、离线消息、已读回执、群聊                  |
| 设计搜索系统         | 倒排索引、分词、相关性排序、高亮、推荐                      |
| 设计支付系统         | 支付状态机、幂等、对账、容错、资金安全                      |
| 设计限流系统         | 四大算法实现、分布式限流、多级限流                          |
| 设计日志平台         | 采集（Filebeat）→ 队列（Kafka）→ 存储（ES）→ 展示（Kibana） |
| 设计 RAG 知识库      | 文档处理→向量化→检索→生成→评估，全链路设计                  |
| 设计分布式 ID 生成器 | 雪花算法、号段模式、数据库自增、UUID、Redis 自增            |

#### 2.3 通用架构设计原则

- 高可用：冗余、故障转移、熔断降级、限流隔离
- 高性能：缓存、异步、并行、就近访问、读写分离
- 可扩展：水平扩展、无状态服务、模块化、微服务拆分
- 可观测：日志、指标、链路追踪三位一体

# 面试

## 简历优化

- 简历结构：个人信息、专业技能、项目经历、工作经历、教育背景
- 项目写法：STAR 法则（背景 - 任务 - 行动 - 结果）、量化成果、突出技术难点
- 技能匹配：针对 JD 关键词优化、技术栈分层呈现
- 避坑原则：不写不熟悉的技术、项目真实可深挖、简历一页纸原则

## 面试流程

- [ ] [如何在2021年通过工程师面试](https://davidbyttow.medium.com/how-to-pass-the-engineering-interview-in-2021-45f1b389a1)
- [ ] [揭秘技术招聘过程](https://www.youtube.com/watch?v=N233T0epWTs)
- [ ] 如何在四大科技巨头公司中找到工作：
  - [ ] [如何在四大科技巨头公司中找到工作 - 亚马逊、Facebook、谷歌和微软（视频）](https://www.youtube.com/watch?v=YJZCUhxNCv8)
  - [ ] [如何在四大科技巨头公司中找到工作1（后续视频）](https://www.youtube.com/watch?v=6790FVXWBw8&feature=youtu.be)
- [ ] 《破解编程面试》第一集：
  - [ ] [Gayle L McDowell - Cracking The Coding Interview（视频）](https://www.youtube.com/watch?v=rEJzOhC5ZtQ)
  - [ ] [与作者Gayle Laakmann McDowell一起破解编程面试（视频）](https://www.youtube.com/watch?v=aClxtDcdpsQ)
- [ ] 破解Facebook编程面试：
  - [ ] [方法论](https://www.youtube.com/watch?v=wCl9kvQGHPI)
  - [ ] [问题演示](https://www.youtube.com/watch?v=4UWDyJq8jZg)
- 面试准备课程：
  - [数据结构、算法和面试的Python课程（付费课程）：](https://www.udemy.com/python-for-data-structures-algorithms-and-interviews/):
    - 以Python为中心的面试准备课程，涵盖数据结构、算法、模拟面试等内容。
  - [使用Python的数据结构和算法简介（Udacity免费课程）：](https://www.udacity.com/course/data-structures-and-algorithms-in-python--ud513):
    - 一个免费的以Python为中心的数据结构和算法课程。
  - [数据结构和算法纳米学位！（Udacity付费纳米学位）：](https://www.udacity.com/course/data-structures-and-algorithms-nanodegree--nd256):
    - 提供超过100个数据结构和算法练习的实际操作体验，并得到专属导师的指导，以帮助您为面试和实际工作做准备。
  - [Grokking行为面试（Educative免费课程）：](https://www.educative.io/courses/grokking-the-behavioral-interview):
    - 很多时候，阻碍您获得梦想工作的不是您的技术能力，而是您在行为面试中的表现。
  - [AlgoMonster（付费课程，提供免费内容）：)](https://algo.monster/?utm_campaign=jwasham&utm_medium=referral&utm_content=coding-interview-university&utm_source=github):
    - LeetCode的速成课程。涵盖了从成千上万的问题中提炼出的所有模式。

模拟面试：

- [Gainlo.co：来自大公司的模拟面试官](http://www.gainlo.co/#!/) - 我用过这个，帮助我放松进行电话和现场面试。
- [Pramp：与同行进行模拟面试](https://www.pramp.com/) - 同行模式的实践面试。
- [interviewing.io：与资深工程师进行模拟面试 ](https://interviewing.io) - 匿名算法/系统设计面试，与FAANG公司的资深工程师进行。
- [Meetapro：与顶级FAANG面试官进行模拟面试 ](https://meetapro.com/?utm_source=ciu) - 类似Airbnb的模拟面试/指导平台。
- [Hello Interview：与专家教练和人工智能模拟面试](https://www.hellointerview.com/?utm_source=ciu) - 直接与人工智能或 FAANG 员工工程师和经理面试。
- [Codemia：通过人工智能或社区解决方案和反馈来练习系统设计问题](https://codemia.io/?utm_source=ciu) - 通过AI练习工具来解决系统设计问题。与社区分享你的解决方案，以获得反馈。.

## 面试问题

- 你为什么想得到这份工作？
- 你解决过的最有难度的问题是什么？
- 面对过的最大挑战是什么?
- 见过的最好或者最坏的设计是怎么样的?
- 对某个产品提出改进建议。
- 你作为一个个体同时也是团队的一员，如何达到最好的工作状态?
- 你的什么技能或者经验是你的角色中不可或缺的，为什么？
- 你在某份工作或某个项目中最享受的是什么?
- 你在某份工作或某个项目中面临过的最大挑战是什么?
- 你在某份工作或某个项目中遇到过的最硬的 Bug 是什么样的？
- 你在某份工作或某个项目中学到了什么？
- 你在某份工作或某个项目中哪些地方还可以做的更好？

## 你问面试官的问题

- 我会问的一些：(可能我已经知道了答案但我想听听面试官的看法或者了解团队的前景):

- 团队多大规模?
- 开发周期是怎样的? 会使用瀑布流/极限编程/敏捷开发么?
- 经常会为截止日期（deadlines）加班么? 或者是有弹性的?
- 团队里怎么做技术选型?
- 每周平均开多少次会?
- 你觉得工作环境有助于员工集中精力吗?
- 目前正在做什么工作?
- 喜欢这些事情吗?
- 工作期限是怎么样的?
- 工作生活怎么平衡?

# 实战

## JavaFX

## 魂斗罗游戏

## 题目抽卡项目实战

Javase+vue 

## SPringBoot项目实战

### 0.功能实现

1. 把你现有的 CRUD 项目升级为企业级标准项目：

- [ ] 统一返回格式（Result）
- [ ] 全局异常处理
- [ ] 参数校验
- [ ] 统一分页工具
- [ ] VO/DTO 分层（MapStruct 转换）
- [ ] 自动填充（创建时间、更新时间）
- [ ] 逻辑删除
- [ ] 代码生成器一键生成 CRUD
- [ ] 多环境配置（dev/test/prod）
- [ ] 日志配置 + MDC 链路 ID
- [ ] 接口文档（Knife4j / SpringDoc）
- [ ] 单元测试（JUnit5 + Mockito）

2. #### 4. 项目中的应用

   - 异步解耦：用户注册后异步发送短信/邮件
   - 日志收集：操作日志异步写入数据库
   - 订单超时取消：延迟消息实现
   - 流量削峰：秒杀场景下订单异步处理

   #### 5. 最佳实践

   - 消息幂等性：如何保证消息不重复消费
     - 数据库唯一索引
     - Redis 去重
     - 状态机判断
   - 消息可靠性：
     - 生产者确认机制
     - 消费者手动 ACK
     - 持久化配置

   3. 做一个完整的 AI 应用项目，包含以下功能：

      - [ ] 智能对话：流式对话、多轮对话、对话历史
      - [ ] 图片生成：文生图、图片保存、我的作品
      - [ ] 图片理解：上传图片提问
      - [ ] 视频生成：文生视频、异步任务、状态查询
      - [ ] 知识库问答：
        - [ ] 文档上传（PDF/Word/Markdown）
        - [ ] 文档解析与向量化
        - [ ] 语义检索问答
        - [ ] 引用溯源
      - [ ] 我的助手：创建自定义 AI 助手（设定人设、提示词）
      - [ ] 用户系统：注册登录、积分/额度管理
      - [ ] 管理后台：用户管理、模型配置、知识库管理

   4. ### 前端实战

      - 登录注册页面
      - 后台管理系统布局（侧边栏 + 顶栏 + 主内容区）
      - 用户管理 CRUD 页面
      - 角色权限管理
      - 图表展示：ECharts
      - AI 对话页面（流式输出、打字机效果）
      - 图片生成页面
      - 知识库管理页面

### 1. 项目定位

标准企业级后端单体项目，覆盖后端开发全流程规范，主打**RBAC 权限体系 + 通用业务 CRUD + 工程化规范**，可直接作为简历项目，也可无缝升级为微服务架构。

### 2. 完整技术栈

| 分类       | 技术选型                                              |
| ---------- | ----------------------------------------------------- |
| 核心框架   | Spring Boot 3.2.x + Java 17                           |
| ORM 框架   | MyBatis-Plus 3.5.x + 分页插件 + 代码生成器            |
| 数据库     | MySQL 8.0 + 事务 + 索引优化                           |
| 缓存中间件 | Redis 7.x（缓存、限流、幂等、Token 存储）             |
| 鉴权方案   | Spring Security + JWT + Redis 双 Token 机制           |
| 参数校验   | Jakarta Validation + 自定义校验注解                   |
| 接口文档   | Knife4j 4.x（增强版 Swagger，在线调试 + 分组 + 导出） |
| 日志体系   | SLF4J + Logback + ELK 日志采集                        |
| 工具组件   | Hutool、Lombok、Apache Commons                        |
| 文件存储   | 本地存储 + 阿里云 OSS 兼容方案                        |
| 部署方式   | Docker + Docker Compose                               |

### 3. 核心业务模块

1. 系统权限模块（RBAC）
   - 用户管理：新增、编辑、删除、状态禁用、重置密码
   - 角色管理：角色分配、菜单权限绑定、数据权限配置
   - 菜单管理：树形菜单、按钮级权限、目录 / 菜单 / 按钮三级分类
   - 部门管理：树形部门结构、数据权限按部门隔离
2. 系统通用模块
   - 操作日志：记录所有接口操作人、请求参数、耗时、结果
   - 登录日志：记录登录设备、IP、地点、登录状态
   - 字典管理：系统枚举值统一维护，前端动态渲染
   - 系统配置：参数配置化，无需硬编码
3. 业务实战模块（商品订单体系）
   - 商品管理：分类、规格、上下架、库存管理
   - 订单管理：创建订单、库存扣减、订单状态流转
   - 文件管理：图片 / 文件上传、预览、删除
4. 基础设施模块
   - 统一返回结果、全局异常处理、接口限流、接口幂等性

### 4. 标准请求全链路（企业级标准流程）

```
前端请求
  → Nginx 反向代理 + 跨域处理
  → 过滤器链：字符编码 → CORS → 请求体重复读
  → 拦截器：JWT 鉴权 → Redis 校验 Token 有效性 → 获取当前用户
  → AOP 切面：操作日志记录 → 接口限流校验 → 幂等令牌校验
  → Controller 层：参数校验（@Valid） → 接收请求 → 调用 Service
  → Service 层：业务逻辑处理 → 事务控制 → 读写 Redis 缓存
  → Mapper 层：MyBatis-Plus 操作 MySQL → 数据返回
  → 结果封装：统一 Result<T> 包装 → 异常统一捕获转换
  → 响应返回前端
```

### 5. 企业级工程特性（含你要求的日志 / 文档 / 异常）

**（1）全局异常处理**

- 异常分层：参数校验异常、业务异常、权限异常、第三方调用异常、系统未知异常
- 统一错误码：按模块定义错误码（如 10xxx 用户模块、20xxx 订单模块），附带错误信息与解决方案提示
- 实现方式：`@RestControllerAdvice` + 自定义业务异常类 `BusinessException`
- 异常日志：完整打印异常栈 + 请求链路 ID，方便排查

**（2）ELK 日志处理体系**

- **采集端**：项目集成 Logback，按级别 / 模块拆分日志文件，MDC 注入全局链路 ID（traceId），贯穿单次请求全流程
- **传输存储**：Filebeat 采集日志文件 → 输出到 Elasticsearch，按天创建索引
- **可视化**：Kibana 搭建日志大盘，支持按链路 ID、用户 ID、接口路径、错误级别检索，配置错误日志告警
- 日志规范
  - 入口出口必打日志（请求参数、响应结果、耗时）
  - 错误日志必打上下文（用户、入参、异常栈）
  - 敏感字段自动脱敏（手机号、密码、身份证）

**（3）API 接口文档（Knife4j）**

- 接口分组：系统接口、业务接口、公开接口分组展示
- 在线调试：支持携带 Token 直接调试，替代 Postman 基础功能
- 实体注释：所有 DTO/VO 字段加注释，自动生成文档说明
- 离线导出：支持导出 Markdown/HTML 格式接口文档

**（4）其他企业级能力**

- **接口幂等性**：基于 Redis + 令牌机制，防止重复提交
- **接口限流防刷**：自定义注解 + Redis 计数器，限制单用户单位时间调用次数
- **数据权限**：基于部门 / 角色的数据行级隔离，自定义注解 + SQL 拦截器实现
- **多环境配置**：dev/test/prod 三套配置，分别对应开发、测试、生产
- **代码生成器**：MyBatis-Plus 代码生成器，一键生成 Entity、Mapper、Service、Controller、基础 CRUD

### 6. 标准项目分层结构

```
├── common          // 通用模块：统一返回、异常、常量、工具类
├── config          // 配置类：Security、Redis、Knife4j、MyBatis-Plus
├── annotation      // 自定义注解：限流、幂等、操作日志、数据权限
├── aop             // 切面实现：日志、限流、幂等
├── exception       // 异常体系：自定义异常、全局异常处理器
├── system          // 系统模块：用户、角色、菜单、部门、日志
├── business        // 业务模块：商品、订单
├── dto/vo          // 请求/响应对象
├── entity          // 数据库实体
├── mapper          // DAO 层
├── service         // 业务层（接口 + 实现类）
└── controller      // 控制层
```

### 7 SpringBoot RBAC 后台项目新增功能

1 数据脱敏统一处理

 2 多租户隔离（MyBatis-Plus 多租户插件）

 3 导入导出百万级 Excel（EasyExcel）

 4 定时任务 XXL-JOB 集成（报表定时生成）

 5 邮件、短信通知模块

 6 全局水印、接口操作水印 

7 数据库版本管理 Flyway

## AIAgnes项目实战

### 1. 项目定位

企业级 AI 对话服务，完整落地你指定的业务链路，同时具备 Agent 工具调用、多轮对话记忆、RAG 知识库能力，是简历中 AI + 后端结合的核心亮点项目。

### 2. 完整技术栈

表格

| 分类       | 技术选型                                       |
| ---------- | ---------------------------------------------- |
| 核心框架   | Spring Boot 3.3.x + Java 17                    |
| AI 框架    | Spring AI 1.0.x（统一大模型抽象）              |
| 大模型接入 | 通义千问 / 豆包 / OpenAI（兼容 OpenAI 协议）   |
| 数据库     | MySQL 8.0（用户、对话、消息记录）              |
| 缓存       | Redis 7.x（Token、对话上下文、限流、热点缓存） |
| 向量数据库 | Milvus 2.4.x（RAG 知识库向量存储与检索）       |
| 鉴权       | JWT + Redis 双 Token 机制                      |
| 流式输出   | SSE（Server-Sent Events）                      |
| 异步处理   | RabbitMQ（文档向量化、异步任务）               |
| 接口文档   | Knife4j 4.x                                    |
| 日志       | SLF4J + ELK 全链路日志                         |

### 3. 核心业务模块

1. **用户认证模块**：注册、登录、刷新 Token、登出、Token 黑名单
2. **对话管理模块**：会话创建、会话列表、历史消息查询、删除会话
3. **基础对话模块**：单轮问答、多轮上下文对话、流式输出
4. **Agent 工具模块**：大模型函数调用，内置天气查询、计算器、数据库查询、第三方 API 调用工具
5. **RAG 知识库模块**：文档上传、文本分块、向量化、检索增强生成、知识库管理

### 4. 指定业务全链路（逐环节拆解）

严格按照你要求的链路实现，每个环节明确职责与实现方式：

**第 1 步：接收请求**

- Controller 层接收 POST 请求，对话请求使用 DTO 封装
- 流式对话接口返回 `SseEmitter`，普通问答返回统一结果
- 支持上下文关联：携带会话 ID 自动关联历史消息

**第 2 步：参数校验**

- 基于 Jakarta Validation 做基础校验：会话 ID 非空、消息内容长度限制（最大 4096 字符）
- 自定义校验：敏感词检测、用户对话频次校验
- 校验不通过直接抛出参数异常，由全局异常处理器统一返回

**第 3 步：登录鉴权（JWT + Redis）**

- 拦截器解析请求头中的 `Authorization` Bearer Token
- 校验 Token 合法性、过期时间，同时校验 Redis 黑名单（登出后的 Token 直接拒绝）
- 解析出用户 ID 存入 ThreadLocal，业务层直接获取当前用户
- 双 Token 机制：access_token 有效期 2 小时，refresh_token 有效期 7 天，自动续期

**第 4 步：MySQL 持久化**

- **用户表**：存储用户基础信息、注册时间、状态
- **会话表**：每个对话会话一条记录，关联用户 ID、会话标题、创建时间
- **消息表**：每条对话消息一条记录，区分用户提问 / AI 回答，存原始内容、Token 消耗量、耗时
- 业务层事务控制：用户提问先落库，AI 回答生成完成后更新消息记录

**第 5 步：Redis 核心作用**

- 存储登录 Token 与黑名单，控制登录态
- 存储用户短期对话上下文（最近 10 轮），减少 MySQL 查询压力
- 接口限流：控制单用户每日 / 每分钟对话次数
- 缓存热点知识库向量检索结果，提升响应速度

**第 6 步：业务逻辑处理**

- 上下文组装：从 Redis/MySQL 取出历史消息，拼接成完整对话上下文
- Prompt 工程：注入系统提示词、角色设定、工具描述，构建最终 Prompt
- Agent 决策：判断是否需要调用工具，执行工具调用后再把结果喂给大模型
- RAG 召回：开启知识库时，检索向量库中相关文档片段，拼接到 Prompt 中

**第 7 步：调用大模型 LLM**

- 基于 Spring AI 统一封装，底层可无缝切换大模型厂商
- 支持两种模式：
  - 同步模式：等待完整回答后一次性返回
  - 流式模式：SSE 逐字推送，前端打字机效果
- 失败重试：大模型调用失败自动重试 2 次，配置超时时间
- 降级策略：大模型不可用时，返回预设兜底回复，记录异常日志

**第 8 步：结果返回 + 全局异常处理**

- 正常响应：统一 Result 封装，包含消息 ID、内容、耗时、Token 消耗
- 异常统一捕获：
  - 鉴权异常 → 401 状态码 + 提示重新登录
  - 参数异常 → 400 状态码 + 具体错误字段
  - 大模型调用异常 → 500 + 友好提示 + 完整错误日志
  - 限流异常 → 429 + 提示稍后再试
- 所有异常携带 traceId，可在 ELK 中一键检索全链路日志

### 5. Agent 进阶能力

1. **工具调用（Function Calling）**：大模型自主判断是否调用工具，支持扩展任意第三方 API
2. **多轮记忆管理**：短期记忆（Redis）+ 长期记忆（MySQL），支持自动摘要压缩上下文
3. **流式响应**：SSE 逐字输出，支持中途中断对话
4. **内容审核**：输入输出双重审核，拦截违规内容
5. **Token 统计**：按用户 / 按会话统计 Token 消耗，支持额度控制

### 6. 企业级工程特性

- **ELK 全链路日志**：每条请求带 traceId，贯穿「请求→鉴权→业务→大模型调用→响应」全流程，记录大模型耗时、Token 消耗、错误详情
- **Knife4j 接口文档**：所有接口分组展示，流式接口提供调试示例
- **接口幂等**：同一条消息重复提交自动去重
- **监控埋点**：统计对话量、大模型成功率、平均响应时长
- **配置化管理**：模型参数（温度、最大 Token）、Prompt 模板可配置，无需改代码

### 新增功能

1 内容安全审核（敏感词、违规文本拦截）

 2 用户对话额度限流（每日 Token 上限） 

3 对话记录导出 Excel 

4 知识库权限隔离（不同用户可见不同文档）

 5 大模型多厂商负载均衡、故障自动切换 

6 对话数据统计看板（对话量、成功率、Token 消耗）

## 微服务实战项目

###  微服务实战项目

- 推荐项目方向：电商后台、在线教育、企业级管理系统
- 完整技术栈落地：
  - 前端：Vue3 + Element Plus + Axios
  - 后端：Spring Boot + Spring Cloud Alibaba + MyBatis-Plus
  - 中间件：MySQL + Redis + RocketMQ + Nacos + Sentinel + Seata + Gateway
  - 基础设施：Docker 容器化部署、Nginx 反向代理
- 必做模块：用户认证、商品管理、订单支付、库存扣减、消息通知、后台权限管理
- 核心难点：分布式事务扣减库存、接口限流熔断、统一异常处理、接口幂等性

**黑马程序员 天机学堂微服务版+ai agent  启动加自定义功能**

# 额外学习内容

## springcloudalibaba

1. **Nacos**：集注册中心 + 配置中心于一体，服务注册发现、动态配置管理。
2. **Sentinel**：流量控制组件，做服务限流、熔断降级、系统自适应保护。
3. **Dubbo**：高性能 RPC 调用框架，实现微服务之间远程方法调用。
4. **RocketMQ**：阿里消息中间件，提供异步解耦、削峰填谷的消息队列能力。
5. **Seata**：分布式事务组件，解决微服务跨库跨服务的数据一致性问题。
6. **Alibaba Cloud OSS**：对象存储，用于文件上传下载云端存储。
7. **Alibaba Cloud SchedulerX**：分布式任务调度，定时任务分布式执行。
8. **Spring Cloud Gateway**（网关，属于 SpringCloud，SCAlibaba 适配）：API 网关，请求路由、鉴权过滤、流量转发。

## Kubernetes

#### 3.1 Kubernetes 核心概念

- 架构：Master 节点（API Server、Controller Manager、Scheduler、etcd）+ Node 节点（Kubelet、Kube-proxy、Container Runtime）
- 核心资源：Pod、Deployment、Service、Namespace、ConfigMap、Secret、Volume
- 工作负载：Deployment（无状态）、StatefulSet（有状态）、DaemonSet、Job/CronJob

#### 3.2 服务网络与存储

- Service 类型：ClusterIP、NodePort、LoadBalancer、ExternalName
- Ingress：Nginx Ingress Controller、域名路由、HTTPS 配置
- 存储：PV、PVC、StorageClass、常见存储插件

#### 3.3 配置与安全

- ConfigMap / Secret 的使用方式
- RBAC 权限控制：Role、ClusterRole、RoleBinding
- ServiceAccount

#### 3.4 Helm 包管理

- Chart 结构、常用命令、自定义模板

#### 3.5 实践操作

- 本地集群：Minikube / Kind
- kubectl 常用命令
- 应用部署流程：编写 Deployment + Service YAML → kubectl apply → 验证
- 日志查看与排障：kubectl logs、kubectl describe、kubectl exec

## CI/CD流水线实战

- 完整技术栈：Git/GitLab → WebHook → Jenkins Pipeline → Maven 构建 → SonarQube 代码检测 → Docker 镜像构建 → Harbor 镜像仓库 → Docker/K8s 部署
- 具体掌握：
  - Jenkins 安装与插件配置、凭据管理
  - Jenkinsfile 声明式流水线编写、多阶段划分
  - WebHook 触发配置、参数化构建
  - SonarQube 质量门禁集成、不通过阻断流水线
  - Docker 镜像自动化构建与推送、版本号管理
  - 部署阶段：远程服务器执行脚本更新容器、滚动发布
  - 流水线通知：构建结果邮件 / 企业微信推送

## EFKS

- - **Elasticsearch**：分布式存储与检索、索引设计、分片副本、查询优化
  - **Filebeat/Fluentd**：日志采集、多行日志合并、字段解析、过滤清洗
  - **Kafka**：日志缓冲削峰、解耦采集与存储、消费积压处理
  - **Kibana**：日志查询、可视化仪表盘、告警配置、索引模式管理
- 完整数据流：应用输出日志 → Filebeat 采集 → Kafka 缓冲 → Logstash/Fluentd 清洗 → Elasticsearch 存储 → Kibana 可视化
- 实战落地：微服务链路追踪集成、错误日志告警、日志分级、全链路日志排查

### ES

#### 7.1 基础概念

- 索引（Index）、文档（Document）、字段（Field）、映射（Mapping）
- 分片（Shard）、副本（Replica）、节点（Node）、集群（Cluster）
- 倒排索引原理：分词 → 词项字典 → 倒排列表

#### 7.2 核心操作

- DSL 查询：match、term、range、bool（must/should/must_not/filter）
- 聚合查询：terms 分组、avg/sum 度量、date_histogram 日期直方图
- 分页：from+size（浅分页）、search_after（深分页）、scroll（批量导出）

#### 7.3 进阶知识

- 分词器：IK 分词器、自定义词典
- 索引优化：分片数规划、mapping 设计、refresh_interval 调整
- 集群架构：节点角色（Master/Data/Ingest/Coordinating）
- 性能优化：批量写入、路由优化、缓存策略
- 与 MySQL 数据同步：Logstash、Canal、Flink CDC

## 中间件

### 消息队列

| 中间件       | 特点                                                        | 适用场景                         |
| :----------- | :---------------------------------------------------------- | :------------------------------- |
| **RocketMQ** | 阿里开源，Java 开发，高吞吐、低延迟，支持事务消息、延迟消息 | 电商订单、金融支付、异步解耦     |
| **Kafka**    | 高吞吐百万级，分布式消息引擎，生态完善                      | 日志收集、大数据流处理、事件溯源 |
| **RabbitMQ** | Erlang 开发，路由灵活，支持多种交换机模式                   | 业务解耦、复杂路由、延迟任务     |
| **Pulsar**   | 存算分离架构，支持多租户，云原生                            | 大规模消息、流批一体             |
| **ActiveMQ** | Java 老牌 MQ，支持多种协议                                  | 传统企业集成、遗留系统           |

### EFK日志

### 缓存

| 中间件        | 特点                                       | 适用场景                              |
| :------------ | :----------------------------------------- | :------------------------------------ |
| **Redis**     | 内存数据库，支持丰富数据类型，单线程高性能 | 热点缓存、分布式锁、排行榜、限流      |
| **Memcached** | 纯内存 KV，简单轻量，多线程                | 简单缓存场景（逐渐被 Redis 替代）     |
| **Caffeine**  | JVM 本地缓存，高性能，支持多种过期策略     | 进程内一级缓存、与 Redis 组成多级缓存 |
| **Ehcache**   | Java 老牌本地缓存，支持持久化              | 单体应用、Hibernate 二级缓存          |

## 框架源码

黑马程序员 spring高级原理

## 系统架构

![image-20260816160547726](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260816160547726.png)

## 计算机如何工作的

![image-20260816160421772](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260816160421772.png)

## 算法

DFA

[附加学习](#附加学习)

- [编译器](#编译器)
- [Emacs and vi(m)](#emacs-and-vim)
- [Unix 命令行工具](#unix-命令行工具)
- [信息论](#信息论-视频)
- [奇偶校验位 & 汉明码 (视频)](#奇偶校验位--汉明码-视频)
- [系统熵值](#系统熵值Entropy)
- [密码学](#密码学)
- [压缩](#压缩)
- [计算机安全](#计算机安全)
- [垃圾回收](#垃圾回收)
- [并行编程](#并行编程)
- [消息传递，序列化和队列化的系统](#消息传递序列化和队列系统)
- [A*搜索算法](#a*搜索算法)
- [快速傅里叶变换](#快速傅里叶变换)
- [布隆过滤器](#布隆过滤器)
- [HyperLogLog](#hyperloglog)
- [局部敏感哈希](#局部敏感哈希)
- [van Emde Boas 树](#van-emde-boas-树)
- [增强数据结构](#增强数据结构)
- [平衡查找树](#平衡查找树balanced-search-trees)
  - AVL 树
  - 伸缩树（Splay tree）
  - 2-3 查找树
  - 2-3-4 树(也称 2-4 树)
  - N-ary (K-ary, M-ary)树
  - B 树
- [k-D 树](#k-d树)
- [跳表](#跳表)
- [网络流](#网络流)
- [不相交集 & 联合查找](#不相交集--联合查找)
- [快速处理的数学](#快速处理的数学)
- [树堆 (Treap)](#树堆-treap)
- [线性规划](#线性规划linear-programming视频)
- [几何：凸包（Geometry, Convex hull）](#几何凸包geometry-convex-hull视频)
- [离散数学](#离散数学)

## 框架

java函数式编程

## 中间件

1. **消息代理**：RabbitMQ、Kafka，用于服务异步解耦、流量削峰
2. **搜索引擎**：Elasticsearch、Solr，实现全文检索能力
3. **实时数据通信**：WebSocket、服务器发送事件（SSE）、长轮询、短轮询，支撑实时交互场景

## 脚本

python 

shell

## C++

## 互联网原理

- 互联网的基本工作原理
- 超文本传输协议（HTTP）
- 域名、网站托管的基本概念
- 域名系统（DNS）的作用与工作原理
- 浏览器的运行机制

## AI

ZOMI酱部分系列视频 

跟李沫学AI 动手学深度学习v2  

 跟李沫学AI实用机器学习中文版

# 数学

深度学习   线性代数 微积分

 3Blue1Brown全系列视频

傅里叶变换

# 书籍

[算法第四版](https://algs4.cs.princeton.edu/code/) 

[算法导论](https://sites.math.rutgers.edu/~ajl213/CLRS/CLRS.html)

   onjava

[凤凰架构](https://icyfenix.cn/)

[zh-v2.d2l.ai](https://zh-v2.d2l.ai/chapter_preface/index.html)

# 论文

查询论文网站

https://arxiv.org/

1000张论文

https://github.com/0voice/computer_expert_paper