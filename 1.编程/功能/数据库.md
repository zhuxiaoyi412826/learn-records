# MySQL

# Redis

## 什么是Redis

官网

https://redis.io

数据类型

https://redis.io/docs/data-types/

我们说的数据类型是value的数据类型，key的类型都是字符串

命令都是不区分大小写的但是 key有大小写   按下tab键补全  

输入info查看当前数据库版本

在连接redis客户端如果想让redis客户端支持中文  需要在连接命令后面加上  --raw



**key常用的命令**

keys * 查看所有的key

EXISTS k1  查看k1是否存在 存在返回1 不存在返回0

EXISTS k1 k2 k3 存在几个key就返回的数字为几

type  查看key是什么数据类型

lpush list 1 2 3   为list赋值 

del k3  删除指定的key  阻塞的删除 

unlink key  非阻塞删除从keyspace元数据中删除 真正的删除会在后续的异步操作中进行

ttl key  查看key还有多少秒过期  -1表示永不过期 -2 表示已过期

expire key   为给定的key设置过期时间

move key dbinde[0-15] 将当前的数据库移动到别的数据库

select dbindex[0-15]   切换数据库

dbsize 查看当前数据库key的剩余量

flushdb 清空当前库

flushall  通杀全部库

## 十大数据类型

http://www.redis.cn/commands.html#string

中文API查找手册

help @ 类型 帮助命令

### String

String是redis最基本的数据类型，一个key对应一个value

String类型是二进制安全的，意思是redis的string可以包含任何数据 

一个redis中字符串value最多可以是512M

**命令格式**

**set key value**
set key value [NX|XX][GET][EX seconds|PX milliseconds EXAT unix-time-seconds|PXAT
unix-time-milliseconds|KEEPTTL]
SET命令有EX、PX、NX、XX以及KEEPTTL五个可选参数，其中KEEPTTL为6.O版本添加的可选参数
·EX seconds:以秒为单位设置过期时间
·Pxmi11 iseconds:以毫秒为单位设置过期时间
EXAT timestamp:设置以秒为单位的NIX时间戳所对应的时间为过期时间
PXAT milliseconds--timestamp:设置以毫秒为单位的UNIX时间戳所对应的时间为过期时间
·NX:键不存在的时候设置键值
·XX:键存在的时候设置键值
KEEPTTL：保留设置前指定键的生存时间  GET:返回指定键原本的值，若键不存在时返回ni1
SET命令使用EX、PX、NX参数，其效果等同于SETEX、
PSETEX、SETNX 命令

返回值
设置成功则返回oK;返回ni1为未执行sET命令，如不满足N区、x条件等。
若使用GET参数，则返回该键原来的值，或在键不存在时返回ni1。

**mset**  

设置多个key 和值

**mseinx** 如果不存在才存在  设置多个值 全部成功才成功

**getrange**   截取值的第几位到第几位    GETRANGE k1 0 3

 **setrange**  设置值的第几位替换成什么值  SETRANGE k1 1 xxyy

**incr** 值加一   一定是要数字才能进行加减

**incrby** 加多少有自己设定  incrby k1 3

**DECR** 递减

**DECRBY** 

**append k1 xxx**  对k1的增加xxx

getset  先获取之前的值在覆盖  

**分布式锁**

setnx key value 



### List 

Redis列表是最简单的字符串列表，按照插入顺序排序，它的底层是个双端链表，最多可以包含2^32-1个元素 40亿个数据

### Hash

Redis hash是一个String类型的field字段和value值的映射表，用来存储对象，每个hash可以存储2^32-1个键值对

### Set

Redis 的set是String类型的无须集合。集合成员是唯一的，不能出现重复的数据，集合对象的编码可以是intset或者是hashtable

Redis中的set集合是通过哈希表实现的，所以添加，删除，查找，复杂度都是0(1)最大存储存储2^32-1

### Zset

Redis zset和set一样也是string类型元素的集合，且不能重复

不同的的是每个元素都会关联一个double类型的分数，redis正是通过分数为集合中的成员进行从小到大的排序，

zset的成员是唯一的，但分数score却可以重复

zset集合是通过哈希表实现的，添加和删除，查找复杂度都是0(1)最大存储存储2^32-1

### GEO     

redis地理空间

### HyperLogLOg

redis基数统计  在输入元素的数量或者体积非常非常很大时候，计算基数所需的空间总是固定且是很小的

### bitmap

位图  每一个各种里面只能存放1或者是0

### bitfield

一次性操作多个比特位域

### Stream

Redis Stream是Redis5.0版本新增加的数据结构。
Redis Stream主要用于消息队列(MQ,Message Queue),Redis本身是有一个Redis
发布订阅(Pub/Sub)来实现消息队列的功能，但它有个缺点就是消息无法持久化，如果出现
网络断开、Redis宕机等，消息就会被丢弃。
简单来说发布订阅(pub/Sub)可以分发消息，但无法记录历史消息。
而Redis Stream提供了消息的持久化和主备复制功能，可以让任何客户端访问任何时刻的
数据，并且能记住每一个客户端的访问位置，还能保证消息不丢失

## SpringBoot操作Redis

在使用java程序操作redis数据库的时候 需要注意的是

1 bind配置注释掉

2 保护模式设置为no

3 Linux系统防火墙的设置

4 redis的ip和1密码

 5 redis的服务端口和auth密码

###  jedis

老系统在用

### lettuce

是一个jar包

### RdisTemplate

#### 单机连接

1 解决序列化乱码问题

键key值value   都是通过Spring提供的Serializer序列化连接数据库的

RedisTemplate默认使用的jdkSerializationRedisSerializer

StringRedisTemplate默认使用的是StringRedisSerializer

key就会被序列化的很难受

使用这个类 

```
@Resource private RedisTemplate redisTemplate;
```

如果使用这个类解决徐丽华问题 

```
@Resource private StringRedisTemplate StringRedisTemplate;
```

在RedisConfig配置文件中这样配置替换jdk序列化配置 

```
 /**
     * redis序列化的工具配置类，下面这个请一定开启配置
     * 127.0.0.1:6379> keys *
     * 1) "ord:102"  序列化过
     * 2) "\xac\xed\x00\x05t\x00\aord:102"   野生，没有序列化过
     * this.redisTemplate.opsForValue(); //提供了操作string类型的所有方法
     * this.redisTemplate.opsForList(); // 提供了操作list类型的所有方法
     * this.redisTemplate.opsForSet(); //提供了操作set的所有方法
     * this.redisTemplate.opsForHash(); //提供了操作hash表的所有方法
     * this.redisTemplate.opsForZSet(); //提供了操作zset的所有方法
     * @param lettuceConnectionFactory
     * @return
     */
    @Bean
    public RedisTemplate<String, Object> redisTemplate(LettuceConnectionFactory lettuceConnectionFactory)
    {
        RedisTemplate<String,Object> redisTemplate = new RedisTemplate<>();

        redisTemplate.setConnectionFactory(lettuceConnectionFactory);
        //设置key序列化方式string
        redisTemplate.setKeySerializer(new StringRedisSerializer());
        //设置value的序列化方式json，使用GenericJackson2JsonRedisSerializer替换默认序列化
        redisTemplate.setValueSerializer(new GenericJackson2JsonRedisSerializer());

        redisTemplate.setHashKeySerializer(new StringRedisSerializer());
        redisTemplate.setHashValueSerializer(new GenericJackson2JsonRedisSerializer());

        redisTemplate.afterPropertiesSet();

        return redisTemplate;
    }
```

#### 集群连接

在yml文件中配置

```
spring.redis.password=111111
# \u83B7\u53D6\u5931\u8D25 \u6700\u5927\u91CD\u5B9A\u5411\u6B21\u6570
spring.redis.cluster.max-redirects=3
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-wait=-1ms
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0
spring.redis.lettuce.cluster.refresh.adaptive=true  # 支持集群拓扑动态刷新，默认false关闭 
#\u5B9A\u65F6\u5237\u65B0
spring.redis.lettuce.cluster.refresh.period=2000
spring.redis.cluster.nodes=192.168.111.185:6381,192.168.111.185:6382,192.168.111.172:6383,192.168.111.172:6384,192.168.111.184:6385,192.168.111.184:6386
```

如果出现了 主节点宕机了 从节点选择为主节点 

微服务客户端进行访问会转圈圈  SpringBoot 感知不到rediclusts最新的信息 

SpringBoot 2.x redis默认连接池采用lettuce 当redis集群节点发送变化后，letture默认ss不会刷新节点拓扑的

解决方案 

1 排除lettuce 采用jedis 不推荐

2  刷新节点集群拓扑动态感应  

spring.redis.lettuce.cluster.refresh.adaptive=true  # 支持集群拓扑动态刷新，默认false关闭 

# 面试题

## Reids

